node_modules

# Output
dist
.output
.vercel
.netlify
.wrangler
/build
# `/.svelte-kit` was root-anchored when the repo was a single package; the
# generated dir now lives inside the Svelte package.
.svelte-kit
# svelte-package emits declarations for test files too; CommandPalette.test.ts
# imports the teardown drain from test-setup.ts, which drags a stray .d.ts next
# to it on every build.
packages/svelte/src/test-setup.d.ts

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Local tooling artifacts (Claude Code + Playwright MCP)
.claude/
.playwright-mcp/

# Playwright browser smoke-test artifacts (the browsers themselves live in the
# shared ms-playwright cache, not the repo; these are per-run outputs)
/test-results/
/playwright-report/
/blob-report/
/.playwright/

# kb-go local store (stray — canonical store is ~/.knowledge-base/)
.knowledge-base/
