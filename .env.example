# Set ONE of the two below. The /showcase/agentic route prefers the auth
# token (Bearer / OAuth) and falls back to the API key.

# ANTHROPIC_AUTH_TOKEN=...   # Bearer token (e.g. from Claude Code OAuth flow)
# ANTHROPIC_API_KEY=sk-ant-...
