<!-- docs/wiki/index.md — auto-generated by build-wiki-index.py. Do not hand-edit. -->

# ripple — Wiki Index

_239 articles · indexed at `99d979bc26af` · generated 2026-04-24T03:18:33+00:00_

Auto-generated from each article's YAML frontmatter. See [README.md](./README.md) for the update workflow.

## By Category (107 categories)

### `accessibility` — 30 article(s)

- [Button Input Widget](./button-input-widget.md) — A fully-featured button widget for Ripple's generative UI runtime, supporting six visual variants, four sizes, loading state with spinner, slot-based leading/trailing icons, and form integration. Designed to prevent double-submission and expose machine-readable state via data attributes.
- [Card Layout Widget](./card-layout-widget.md) — A versatile card container widget with five visual variants, two density modes, and optional interactive behavior. It dynamically renders as either a `<div>` or a `<button>` depending on whether an `onclick` handler is provided alongside `interactive: true`, ensuring semantic HTML and full keyboard accessibility.
- [Checkbox — Accessible Three-State Toggle with Indeterminate Support](./checkbox-accessible-three-state-toggle-with-indeterminate-support.md) — The Checkbox component wraps bits-ui's `Checkbox.Root` primitive to provide a fully accessible toggle with three visual states: unchecked, checked, and indeterminate. It uses lucide icons as indicators and applies extensive Tailwind classes for focus rings, validation states, and disabled group context.
- [Dialog Close — Type-Safe Close Trigger with Default Button Semantics](./dialog-close-type-safe-close-trigger-with-default-button-semantics.md) — DialogClose is a minimal wrapper around bits-ui's `Dialog.Close` primitive. Its sole substantive addition is defaulting `type` to `"button"` — a defensive fix that prevents form submission side effects when the close trigger is rendered inside a `<form>` element.
- [Dialog Description — Accessible Subtitle with Auto-Styled Link Formatting](./dialog-description-accessible-subtitle-with-auto-styled-link-formatting.md) — DialogDescription wraps bits-ui's `Dialog.Description` primitive to render the dialog's accessible subtitle text. It automatically styles inline anchor links with underlines and hover effects, and exposes its content to ARIA via the primitive's `aria-describedby` wiring.
- [Dialog Title — Accessible Heading with Typography Defaults](./dialog-title-accessible-heading-with-typography-defaults.md) — Wraps bits-ui's `DialogPrimitive.Title` to render the dialog's primary heading with consistent typographic styling. The primitive handles ARIA role assignment automatically, making the title accessible to screen readers without manual `role` or `aria-labelledby` wiring.
- [Popover Description — Muted Supporting Text Block](./popover-description-muted-supporting-text-block.md) — A minimal layout component that renders supporting text inside a popover with muted color styling. It is a plain `<div>` wrapper — unlike its dialog counterpart, it does not use the bits-ui `Description` primitive and therefore does not set `aria-describedby` automatically.
- [Radio Group Item — Accessible Option with Visual Check Indicator](./radio-group-item-accessible-option-with-visual-check-indicator.md) — A styled radio button option that wraps bits-ui's `RadioGroup.Item` primitive, rendering a circular border that fills with the primary color when selected, with a `CircleIcon` indicator that appears conditionally via Svelte 5 snippets. It handles checked, focused, disabled, and invalid states through data attributes and ARIA-driven Tailwind variants.
- [Select Group Heading — Accessible Label for Option Groups](./select-group-heading-accessible-label-for-option-groups.md) — A styled wrapper around bits-ui's `SelectPrimitive.GroupHeading` that renders small, muted text as a non-interactive section header within grouped select options. It links to its parent `SelectGroup` via ARIA and is excluded from the keyboard navigation sequence.
- [Select Scroll Down Button — Overflow Navigation Indicator](./select-scroll-down-button-overflow-navigation-indicator.md) — Renders a chevron-down affordance anchored to the bottom of a select listbox when the list overflows its container. It signals to the user that more options exist below the visible area and provides a scroll target managed by bits-ui.
- [Select Scroll Up Button — Top Overflow Navigation Indicator](./select-scroll-up-button-top-overflow-navigation-indicator.md) — Renders a chevron-up affordance pinned to the top of a select listbox when hidden options exist above the visible scroll area. It is the symmetric counterpart to `SelectScrollDownButton`, completing the bidirectional scroll navigation pattern.
- [Select Separator — Visual Divider Between Option Groups](./select-separator-visual-divider-between-option-groups.md) — A horizontal rule that visually segments groups of options inside a select dropdown. It reuses the shared Separator primitive with select-specific margins so dividers align flush with the popover edges.
- [Select Trigger — The Dropdown Open/Close Button](./select-trigger-the-dropdown-openclose-button.md) — The visible button element users click to open or close a select dropdown. It combines value display, size variants, validation states, and a chevron icon into a single polished interactive control that integrates with bits-ui's accessibility layer.
- [Separator Component — Accessible Horizontal and Vertical Divider](./separator-component-accessible-horizontal-and-vertical-divider.md) — A styled wrapper around bits-ui's `Separator.Root` that renders a 1px divider line in either horizontal or vertical orientation. It applies design-token colors and includes a deliberate deviation from the upstream shadcn/ui pattern to fix a self-stretch layout bug.
- [Sheet Close — Accessible Panel Dismiss Button](./sheet-close-accessible-panel-dismiss-button.md) — A minimal passthrough to bits-ui's `Dialog.Close` primitive that renders a dismiss button inside a sheet panel. It wires up the accessibility contract for closing the sheet via keyboard or click without requiring application-level event handling.
- [Sheet Description — Accessible Panel Summary Text](./sheet-description-accessible-panel-summary-text.md) — Renders the descriptive subtitle text inside a sheet panel with muted styling. It maps to bits-ui's `Dialog.Description`, which wires `aria-describedby` between this element and the sheet content so screen readers can announce the panel's purpose.
- [Sheet Title — Accessible Heading for Slide-in Panels](./sheet-title-accessible-heading-for-slide-in-panels.md) — SheetTitle wraps the bits-ui Dialog.Title primitive to provide a consistently styled, accessible heading element for sheet panels. It merges design-system typography tokens with an optional `className` override and exposes a bindable `ref` for imperative DOM access.
- [Sheet Trigger — Open Control for Slide-in Panels](./sheet-trigger-open-control-for-slide-in-panels.md) — SheetTrigger wraps the bits-ui Dialog.Trigger primitive to provide a typed, ref-aware open control for sheet components. It delegates all open/close state management to the bits-ui context and exposes `data-slot` for consistent component targeting.
- [Switch Component — Accessible Toggle with Size Variants](./switch-component-accessible-toggle-with-size-variants.md) — The Switch component wraps bits-ui's SwitchPrimitive to deliver an accessible, animated toggle control with `sm` and `default` size variants, two-way `checked` binding, validation error state, and RTL support. It uses CSS data attributes and Tailwind variants for all visual states, keeping zero state logic in the component itself.
- [Table Caption — Accessible Table Description Element](./table-caption-accessible-table-description-element.md) — TableCaption renders a styled HTML `<caption>` element that provides an accessible, programmatically associated description for a table. Positioned below the table by default via `margin-top`, it uses muted foreground and small text to visually distinguish itself from table data.
- [Table Head — Column Header Cell with Checkbox Spacing](./table-head-column-header-cell-with-checkbox-spacing.md) — TableHead renders a styled `<th>` element for column header cells, applying consistent height, horizontal padding, left-aligned text, and the same checkbox-column padding fix used in TableCell. It ensures header cells align with their body counterparts in selection columns.
- [Tabs Content Panel Component](./tabs-content-panel-component.md) — The content panel for an individual tab, delegating visibility control to the `bits-ui` Tabs primitive. Adds consistent typography sizing, flex growth, and outline removal while forwarding all primitive props.
- [Tabs Trigger Button Component](./tabs-trigger-button-component.md) — The clickable tab button that activates a content panel, with an extensive Tailwind class composition covering active state, focus visibility, dark mode, orientation adaptation, and an animated line-variant underline indicator.
- [Test Suite for Card Layout Widget](./test-suite-for-card-layout-widget.md) — Vitest + Testing Library test suite covering the Card widget's rendering, structural conditionals, variant/density data attributes, interactive button behavior, and ARIA semantics. The suite validates both the happy path and defensive edge cases like non-interactive cards with onclick handlers.
- [Test Suite for Input Widget](./test-suite-for-input-widget.md) — Comprehensive Vitest + Testing Library test suite for the Input text widget, covering rendering, label/id association, size and state data attributes, type forwarding, oninput/onchange event firing, error and helper message rendering, accessibility attributes (aria-invalid, aria-describedby), and disabled/required/readOnly attribute forwarding.
- [Text Input Widget with Widget Registry Integration](./text-input-widget-with-widget-registry-integration.md) — A fully-featured text input widget that supports labels, error/helper messages, prefix/suffix icon slots, and size variants. Uniquely, it opts into Ripple's WidgetRegistry when given an `id`, enabling the `invoke` flow action to call `focus()` on the input remotely from a spec action handler.
- [Textarea Input Component](./textarea-input-component.md) — A styled multi-line text input that auto-sizes to content, handles validation states visually, and supports dark mode. Exposes bindable `value` and `ref` props and forwards all native textarea attributes.
- [Tooltip Component Public API Index](./tooltip-component-public-api-index.md) — The barrel file for the Tooltip component family, re-exporting five sub-components — Root, Trigger, Content, Provider, and Portal — under both short and verbose names. Forms the single import surface for all tooltip functionality.
- [Tooltip Content Bubble with Arrow and Animations](./tooltip-content-bubble-with-arrow-and-animations.md) — The visible tooltip bubble component, composing portal rendering, directional entry/exit animations, keyboard shortcut badge support, and a custom arrow indicator. Delegates positioning and accessibility to `bits-ui`'s Tooltip.Content primitive.
- [Tooltip Trigger Component](./tooltip-trigger-component.md) — A minimal wrapper around `bits-ui`'s `Tooltip.Trigger`, adding a `data-slot` identifier and exposing a bindable `ref`. Acts as the hover/focus target that activates the tooltip.

### `actions` — 2 article(s)

- [Reorderable — Drag-to-Reorder Svelte Action](./reorderable-drag-to-reorder-svelte-action.md) — The `reorderable` Svelte action attaches zero-dependency pointer-event drag-and-drop reordering to any container element, supporting grid, masonry, and flex layouts. It uses a ghost element for visual drag feedback, a configurable dead zone to prevent accidental drags, and nearest-edge drop detection with a maximum proximity threshold.
- [Svelte Actions Barrel Export](./svelte-actions-barrel-export.md) — The `actions/index.ts` file is a barrel module that re-exports all Svelte actions from the actions directory as a single import surface. Currently it exports the `reorderable` action and its associated `ReorderableOptions` type.

### `actor` — 1 article(s)

- [C4 Person Node — Human Actor Node for C4 Context Diagrams](./c4-person-node-human-actor-node-for-c4-context-diagrams.md) — A SvelteFlow custom node that renders a C4 Person element with a circular avatar icon and name/description labels. Distinguishes internal users (blue) from external actors (gray) and supports optional drill-down navigation.

### `agent-ui` — 1 article(s)

- [Soul Status Display Widget](./soul-status-display-widget.md) — A compact or expanded display widget that surfaces an AI agent's soul state — including name, role, mood, energy level, memory count, and last action. Designed for dashboards that need to show multiple agents at a glance, with an inline energy bar and status dot in compact mode and a full detail panel in expanded mode.

### `algorithm` — 1 article(s)

- [autoLayout — BFS Workflow Node Positioning](./autolayout-bfs-workflow-node-positioning.md) — A pure TypeScript function that computes x/y coordinates for workflow nodes using breadth-first search from root nodes, assigning depth-based horizontal positions and vertically centering each depth column. Includes cycle detection to prevent infinite BFS loops in graphs with back-edges.

### `analytics` — 1 article(s)

- [Stat Metric Widget with Delta and Direction](./stat-metric-widget-with-delta-and-direction.md) — A data metric widget for dashboards that renders a labeled numeric value with optional change indicator (delta chip). Handles locale-aware number formatting across four modes — number, currency, percent, and compact — and supports a sophisticated direction/sentiment system that decouples visual arrow direction from color meaning.

### `animation` — 6 article(s)

- [Dialog Overlay — Animated Backdrop with Backdrop Filter](./dialog-overlay-animated-backdrop-with-backdrop-filter.md) — Wraps the bits-ui `DialogPrimitive.Overlay` to produce a full-viewport dimmed backdrop behind an open dialog. Applies entrance/exit animations and a conditional blur effect, with progressive enhancement for browsers that support the backdrop-filter CSS property.
- [Popover Content — Animated Floating Panel with Portal Integration](./popover-content-animated-floating-panel-with-portal-integration.md) — Renders the floating panel of a popover inside a portal, with entrance/exit animations keyed to open/close state and configurable positioning. Automatically wraps content in `PopoverPortal` to escape ancestor stacking contexts.
- [Progress Bar — Animated Linear Progress Indicator](./progress-bar-animated-linear-progress-indicator.md) — A styled wrapper around bits-ui's `Progress.Root` that renders a horizontal progress bar with a CSS transform-based fill animation. It accepts `value` (current progress) and `max` (ceiling, default 100) props, renders an inner indicator div styled as the primary color, and uses `translateX` rather than `width` changes to animate smoothly.
- [Select Content — Animated Floating Panel with Scroll Controls](./select-content-animated-floating-panel-with-scroll-controls.md) — The floating content panel for the select dropdown, composing a portal, scroll buttons, a viewport, and animated entry/exit transitions. It defaults to preventing page scroll when open (`preventScroll = true`) and uses a 4px side offset from the trigger, with directional slide animations driven by `data-[side]` attributes.
- [Sheet Content — Animated Side Panel with Side-Aware Layout](./sheet-content-animated-side-panel-with-side-aware-layout.md) — The core panel component of the Sheet system that renders a fixed-position drawer sliding in from any of the four viewport edges. It composes the portal, overlay, and close button into a single cohesive unit and applies side-aware entrance/exit animations via data attributes.
- [Sheet Overlay — Backdrop Scrim with Blur Effect](./sheet-overlay-backdrop-scrim-with-blur-effect.md) — Renders a semi-transparent backdrop behind an open sheet panel that blocks interaction with underlying page content and provides a subtle blur effect when the browser supports it. It is always rendered inside `SheetContent` and delegates close-on-click behavior to bits-ui's `Dialog.Overlay`.

### `architecture` — 2 article(s)

- [Display Widget Barrel Export](./display-widget-barrel-export.md) — The barrel export file for Ripple's display widget category, re-exporting all eleven display primitives through a single import path. This indirection layer lets consumers and the widget registry import from `./display/index.js` without tracking individual component file paths.
- [Widget Registry — Central Component Map and Registration API](./widget-registry-central-component-map-and-registration-api.md) — The top-level widget registry for Ripple's generative UI runtime. It maintains a mutable map of string type names to Svelte components, provides a CRUD API for runtime registration, and serves as the single import point for all 40+ built-in widgets across layout, display, input, data, control, composite, overlay, research, workflow, and C4 categories.

### `avatar` — 7 article(s)

- [Avatar Badge — Status Indicator Overlay](./avatar-badge-status-indicator-overlay.md) — AvatarBadge renders a small circular overlay anchored to the bottom-right corner of an avatar, used to convey status (online, offline, notifications). It reads the parent avatar's size via CSS group data attributes and scales itself proportionally, hiding its icon content on small avatars where it would otherwise be illegible.
- [Avatar Component Public API and Barrel Export](./avatar-component-public-api-and-barrel-export.md) — The avatar index barrel file is the single import point for the entire Avatar component family, exporting all six sub-components under both short primitive names and fully-qualified prefixed aliases. This enables consistent, import-friendly consumption across Ripple's generative UI runtime.
- [Avatar Fallback — Image Load Failure Display](./avatar-fallback-image-load-failure-display.md) — AvatarFallback is the graceful-degradation layer rendered when an avatar image fails to load or is unavailable. It wraps bits-ui's headless `AvatarPrimitive.Fallback` to inject Ripple's design tokens and size-responsive typography.
- [Avatar Group Count — Overflow Indicator for Avatar Stacks](./avatar-group-count-overflow-indicator-for-avatar-stacks.md) — AvatarGroupCount renders the overflow indicator at the end of an avatar stack — the '+5' or similar count shown when a group has more members than the display limit. It mirrors the circular shape and sizing of sibling avatars while reading the group's size tier from a parent CSS group selector.
- [Avatar Group — Stacked Avatar Row Container](./avatar-group-stacked-avatar-row-container.md) — AvatarGroup is the layout container that stacks multiple Avatar components into the familiar overlapping-circles row. It establishes the CSS context that child avatars and the overflow count indicator query for size coordination, and applies negative spacing to create the overlapping effect.
- [Avatar Image — Profile Picture Renderer with Load Tracking](./avatar-image-profile-picture-renderer-with-load-tracking.md) — AvatarImage renders the actual profile picture inside an avatar, delegating load-state tracking to bits-ui's headless primitive. It enforces circular cropping and aspect-ratio preservation so arbitrarily-sized source images always fit the circular frame correctly.
- [Avatar Root — Core Container with Size and Load State](./avatar-root-core-container-with-size-and-load-state.md) — Avatar is the root component in the avatar family, providing the circular container, size tier selection, load status binding, and the pseudo-element border overlay. It coordinates sizing context for child components (image, fallback, badge) and exposes loadingStatus as a bindable prop for parent consumption.

### `barrel` — 3 article(s)

- [Composite Widgets Barrel Export](./composite-widgets-barrel-export.md) — A single-line barrel file that re-exports all composite widgets from the `composite/` directory under a unified import path. Currently exports `Terminal`; the barrel pattern means consumers need only update their import path once when new composite widgets are added.
- [Control Widgets Barrel Export](./control-widgets-barrel-export.md) — Barrel file that aggregates Ripple's structural control-flow widgets — `If` and `Each` — under a single import path. These two widgets form the core of Ripple's declarative conditionals and list rendering in schema-driven UIs.
- [Data Widgets Barrel Export](./data-widgets-barrel-export.md) — Barrel file that re-exports Ripple's data-display widgets — `Table` and `Chart` — from a single import path. These two widgets cover the primary structured-data visualization needs in Ripple-generated UIs.

### `card` — 8 article(s)

- [Card Action — Grid-Positioned Action Slot in Card Header](./card-action-grid-positioned-action-slot-in-card-header.md) — CardAction is a specialised layout sub-component that places action controls (buttons, menus) in the top-right corner of a card header using CSS grid positioning. It uses explicit grid coordinates to float above card title and description content without affecting their flow.
- [Card Component Index — Dual-Name Export Barrel](./card-component-index-dual-name-export-barrel.md) — The card `index.ts` barrel file re-exports all seven card sub-components under both short semantic names (`Root`, `Header`, `Content`) and fully-qualified PascalCase names (`Card`, `CardHeader`, `CardContent`). This dual-export pattern supports both namespace-style and destructured imports.
- [Card Content — Main Body Region with Size-Responsive Padding](./card-content-main-body-region-with-size-responsive-padding.md) — CardContent is the main body slot of a card, providing horizontal padding that tightens automatically when the parent card is in its small size variant. It enforces consistent internal spacing without requiring consumers to manage padding manually.
- [Card Description — Muted Supporting Text for Card Headers](./card-description-muted-supporting-text-for-card-headers.md) — CardDescription renders secondary descriptive text beneath a card title using muted foreground color and small type. It uses a semantic `<p>` element to preserve document structure and applies design token classes that adapt to light and dark themes automatically.
- [Card Footer — Styled Bottom Section of a Card](./card-footer-styled-bottom-section-of-a-card.md) — CardFooter is the bottom-zone wrapper inside a card layout. It applies a muted background, rounded bottom corners, a top border, and responsive padding that automatically adjusts when the parent card is in `size=sm` mode.
- [Card Header — Adaptive Grid Layout for Card Titles and Actions](./card-header-adaptive-grid-layout-for-card-titles-and-actions.md) — CardHeader is the top section of a card component. It uses CSS Grid with container queries and group-data selectors to dynamically adapt its layout based on which child slots (title, description, action) are present.
- [Card — Root Container with Size Variants and Image-Aware Layout](./card-root-container-with-size-variants-and-image-aware-layout.md) — The root Card component is the orchestrating container for the card system. It exposes a `size` prop that propagates layout hints to all descendant sub-components via `data-size`, and it uses CSS selectors to handle image children, footer presence, and ring styling without any JavaScript.
- [Card Title — Size-Aware Heading Text for Card Components](./card-title-size-aware-heading-text-for-card-components.md) — CardTitle renders the primary heading within a card header. It is a thin styled wrapper that responds to the parent card's size variant, reducing text size from `text-base` to `text-sm` when the card is in compact mode.

### `chart` — 5 article(s)

- [Chart Component Index — Public API Barrel for Chart System](./chart-component-index-public-api-barrel-for-chart-system.md) — The chart `index.ts` barrel exports `ChartContainer` and `ChartTooltip` as the public surface of the chart system, alongside `ChartConfig` type and `getPayloadConfigFromPayload` utility for consumer-level payload handling.
- [Chart Container — LayerChart Integration Host with CSS Override Layer](./chart-container-layerchart-integration-host-with-css-override-layer.md) — ChartContainer is the root wrapper for all chart widgets. It establishes chart context, generates a unique scoped chart ID, injects theme-based CSS variables, and applies a comprehensive set of Tailwind overrides that normalize LayerChart's default visual behavior.
- [Chart Style — Dynamic CSS Variable Injection for Theme-Aware Chart Colors](./chart-style-dynamic-css-variable-injection-for-theme-aware-chart-colors.md) — ChartStyle generates and injects a `<style>` block at runtime that maps chart series identifiers to CSS custom properties, supporting both light and dark theme variants. It is a headless utility component — it produces no visible DOM, only scoped CSS.
- [Chart Tooltip — Rich Data Series Overlay with Configurable Indicators](./chart-tooltip-rich-data-series-overlay-with-configurable-indicators.md) — ChartTooltip is a styled hover overlay for chart widgets that reads active data series from LayerChart's tooltip context, resolves each series label and color from `ChartConfig`, and renders rows with configurable indicator shapes (dot, line, or dashed). It supports custom formatters via Svelte snippets.
- [Chart Utils — Config Types, Context Management, and Payload Resolution](./chart-utils-config-types-context-management-and-payload-resolution.md) — This utility module defines the `ChartConfig` type and the Svelte context pair (`setChartContext` / `useChart`) that share chart configuration across the component tree. It also exports `getPayloadConfigFromPayload`, a defensive resolver that extracts per-series config from LayerChart's polymorphic tooltip payload.

### `citations` — 3 article(s)

- [Citation: Inline Source Reference Chip with Favicon](./citation-inline-source-reference-chip-with-favicon.md) — Citation renders a compact inline chip showing a source name, its favicon, and an optional superscript citation number — behaving as a clickable link when a URL or click handler is provided. Favicon loading failures are handled gracefully with a colored dot fallback, keeping the component visually consistent even when icon fetching fails.
- [SourceCard: Compact Source Attribution Card with Headline](./sourcecard-compact-source-attribution-card-with-headline.md) — SourceCard is a small card that pairs a favicon (with dot fallback) and source name with a headline — designed for horizontal citation strips where sources are surfaced as individual tappable cards. It follows the same favicon-error-fallback and conditional-interactivity patterns as Citation and NewsCard.
- [SourcesBar: Aggregated Sources Summary Bar with Copy and Share](./sourcesbar-aggregated-sources-summary-bar-with-copy-and-share.md) — SourcesBar renders a compact row showing stacked source favicons and a count label alongside copy and share action buttons — serving as a persistent summary of all sources cited in a research response. It integrates with the Ripple event system to dispatch toast notifications and supports the Web Share API with clipboard fallback.

### `component-api` — 4 article(s)

- [Alert Component Public API and Barrel Export](./alert-component-public-api-and-barrel-export.md) — The alert index file is the single public entry point for the entire alert component family, re-exporting all sub-components under both short-form and fully-qualified names. This dual-alias pattern gives consumers flexibility to use destructured imports or prefixed names without creating ambiguity.
- [Avatar Component Public API and Barrel Export](./avatar-component-public-api-and-barrel-export.md) — The avatar index barrel file is the single import point for the entire Avatar component family, exporting all six sub-components under both short primitive names and fully-qualified prefixed aliases. This enables consistent, import-friendly consumption across Ripple's generative UI runtime.
- [Badge Component Public API Export](./badge-component-public-api-export.md) — The badge index file is a minimal barrel that re-exports the Badge component and its variant utilities from the single implementation file. It provides the stable import path for the Badge family and makes the `badgeVariants` function and `BadgeVariant` type available for external use.
- [Button Component Public API Export](./button-component-public-api-export.md) — The button index barrel exports the Button component and all associated types and variant utilities. It follows the dual-alias pattern and additionally surfaces `Props` as a convenience alias for `ButtonProps`, enabling clean generic wrapper typings.

### `component-system` — 2 article(s)

- [Separator Component Index — Dual Export for Flexible Imports](./separator-component-index-dual-export-for-flexible-imports.md) — The barrel export file for the Separator UI component that exposes the root implementation under both a plain `Root` alias and a semantic `Separator` alias. This dual-export pattern lets consumers choose between namespace-style and direct-import ergonomics.
- [Sheet Component Index — Barrel Export for Slide-Out Panel System](./sheet-component-index-barrel-export-for-slide-out-panel-system.md) — The barrel export for Ripple's Sheet (slide-out panel) component family. It exports all ten sub-components under both short primitive names and longer semantic `Sheet*` names, giving consumers the flexibility to import either style while maintaining a single source of truth.

### `composite` — 2 article(s)

- [Composite Widgets Barrel Export](./composite-widgets-barrel-export.md) — A single-line barrel file that re-exports all composite widgets from the `composite/` directory under a unified import path. Currently exports `Terminal`; the barrel pattern means consumers need only update their import path once when new composite widgets are added.
- [Terminal Widget — Read-Only and Interactive Log Display](./terminal-widget-read-only-and-interactive-log-display.md) — A styled terminal emulator widget for Ripple that renders sequential log lines with per-type color coding (stdout, stderr, info, command) and optionally exposes a command input field. Auto-scrolls to the bottom when new lines arrive, making it suitable for streaming agent output, build logs, or shell session playback.

### `configuration` — 2 article(s)

- [SvelteKit App Type Declarations](./sveltekit-app-type-declarations.md) — The `app.d.ts` file is the SvelteKit-generated ambient type declaration file that extends the global `App` namespace. It provides a central place to type platform-specific interfaces like error shapes, locals, page data, page state, and platform context.
- [Vitest Test Setup — jest-dom Matchers](./vitest-test-setup-jest-dom-matchers.md) — A single-line Vitest global setup file that imports the @testing-library/jest-dom/vitest extension, injecting DOM-aware assertion matchers like toBeInTheDocument, toHaveClass, and toBeVisible into every test in the suite.

### `content` — 2 article(s)

- [Callout: Contextual Highlight Block with Semantic Variants](./callout-contextual-highlight-block-with-semantic-variants.md) — Callout renders a left-bordered highlight block with an icon badge, supporting four semantic variants — info, success, warning, and insight — each with distinct color theming. It exists to draw reader attention to important conclusions, caveats, or findings within AI-generated research content.
- [DiscoverCard: Media Card for Discoverable Content Links](./discovercard-media-card-for-discoverable-content-links.md) — DiscoverCard renders a compact card with a 16:10 thumbnail image, title, description, and source label — designed to be placed in horizontal scroll strips to surface recommended articles, topics, or related content. It activates as a button when a URL or click handler is provided.

### `control` — 3 article(s)

- [Control Widgets Barrel Export](./control-widgets-barrel-export.md) — Barrel file that aggregates Ripple's structural control-flow widgets — `If` and `Each` — under a single import path. These two widgets form the core of Ripple's declarative conditionals and list rendering in schema-driven UIs.
- [Each — Declarative List Iteration Control Widget](./each-declarative-list-iteration-control-widget.md) — A minimal control-flow wrapper that exposes Svelte's native `{#each}` iteration pattern as a named widget usable in Ripple's schema-driven UI. It accepts an `items` prop (used by the schema layer) and a `children` snippet that it unconditionally renders, delegating actual iteration to the caller.
- [If — Declarative Conditional Rendering Control Widget](./if-declarative-conditional-rendering-control-widget.md) — A minimal control-flow widget that wraps Svelte's `{#if}` block as a named, schema-addressable component, enabling conditional rendering in Ripple's JSON spec format. When `condition` is false (or a spec expression resolves falsy), the children snippet is suppressed entirely.

### `core` — 7 article(s)

- [Core Module Barrel — lib/core/index.ts](./core-module-barrel-libcoreindexts.md) — The `lib/core/index.ts` barrel re-exports all public symbols from Ripple's four core subsystems: StateManager, EventDispatcher, expression resolver utilities, and the spec normalizer. It provides a single stable import path for the core engine without exposing internal module structure.
- [EventDispatcher — Multistep Flow Action Execution Engine](./eventdispatcher-multistep-flow-action-execution-engine.md) — The EventDispatcher is Ripple's core runtime for executing declarative event handlers against a StateManager. It supports 12 action types including composite multi-step flows, async API chaining, conditional branching, user confirmation dialogs, validation guards, and direct widget method invocation.
- [Expression Resolver — Template Binding and Condition Evaluation](./expression-resolver-template-binding-and-condition-evaluation.md) — The expression resolver evaluates `{state.x}` style binding expressions found in widget props, event handler values, and condition strings. It supports dot-notation paths, comparison operators, logical operators, ternary expressions, and template string interpolation without relying on `eval` or `Function`.
- [Ripple Package Entry Point — lib/index.ts](./ripple-package-entry-point-libindexts.md) — The top-level `lib/index.ts` is the public API surface of the Ripple package. It re-exports the main `Ripple` Svelte component, the full core engine (StateManager, EventDispatcher, expression resolver, normalizer), widget registry utilities, Zod-backed schemas, the `reorderable` action, and all public TypeScript types.
- [Spec Normalizer — UniversalSpec Input Coercion](./spec-normalizer-universalspec-input-coercion.md) — The normalizer transforms any raw spec input into a valid `UniversalSpec` object before rendering. It handles three input shapes: fully-formed `UniversalSpec` (pass-through), legacy `UISpec` format (has `ui` but no `intent`), and completely invalid input (returns a safe empty-container fallback).
- [StateManager — Reactive Widget State with Svelte 5 Runes](./statemanager-reactive-widget-state-with-svelte-5-runes.md) — StateManager is the reactive state container for a Ripple widget instance. It wraps a Svelte 5 `$state` object and exposes dot-notation path-based get, set, update, delete, and reset operations. Every mutation triggers Svelte's fine-grained reactivity, causing only the affected widget subtrees to re-render.
- [WidgetRegistry — Runtime Widget Method Registry for Invoke Actions](./widgetregistry-runtime-widget-method-registry-for-invoke-actions.md) — WidgetRegistry is a per-Ripple-instance registry that allows widget components to expose named methods so declarative `invoke` flow actions can call them programmatically. It uses a nested `Map<string, Map<string, WidgetMethod>>` structure keyed by widget ID and method name, with safe unregister semantics designed for Svelte 5 `$effect` cleanup.

### `dashboard` — 5 article(s)

- [Dashboard Auto-Fill Grid Widget (Legacy)](./dashboard-auto-fill-grid-widget-legacy.md) — A legacy CSS grid layout widget that automatically fills columns based on a minimum width using `repeat(auto-fill, minmax(...))`. This component is the static, non-draggable predecessor to the Muuri-based `DashboardRenderer` and is retained for simpler use cases that do not need interactive reordering.
- [Dashboard Grid Slot Widget](./dashboard-grid-slot-widget.md) — A grid cell wrapper used inside the `Dashboard` layout widget that controls column span and prevents child overflow from breaking the grid. Each slot carries identity metadata (`slotId`, `itemId`) intended for future drag-and-drop or serialization use.
- [DashboardManager — Reactive Widget List State for Persistent Dashboards](./dashboardmanager-reactive-widget-list-state-for-persistent-dashboards.md) — DashboardManager is the reactive state container for a mutable dashboard widget list. It exposes a CRUD API (add, remove, update, move, reorder) plus an `onChange` subscription for notifying parent components when the spec changes, supporting persistence in platforms that save dashboard layouts across sessions.
- [DashboardRenderer — Muuri-Powered Drag-and-Drop Dashboard Layout](./dashboardrenderer-muuri-powered-drag-and-drop-dashboard-layout.md) — DashboardRenderer renders `intent='dashboard'` specs as an interactive grid using Muuri for drag-and-drop, dynamic resizing, and auto-packing. It bridges the declarative `DashboardSpec` widget list with Muuri's DOM-level layout engine, handling widget normalization, multi-format data coercion, and per-widget controls.
- [Test Suite: DashboardManager — Widget CRUD, Reorder, and Change Events](./test-suite-dashboardmanager-widget-crud-reorder-and-change-events.md) — Comprehensive unit tests for DashboardManager covering widget add, remove, update, move, reorder, bulk load, ID generation, and the onChange subscription system. Tests verify that mutations produce correct state and reliably fire change callbacks, while load() deliberately does not trigger callbacks.

### `data` — 4 article(s)

- [Chart Widget — Multi-Type Data Visualization with ECharts](./chart-widget-multi-type-data-visualization-with-echarts.md) — A single-component wrapper around Apache ECharts that supports ten chart types — bar, line, area, pie, donut, candlestick, sparkline, heatmap, gauge, and radar — all within a consistent theme-aware, resize-reactive API. The component lazy-loads ECharts on mount, adapts colors to the active CSS theme, and accepts deep-merge overrides for advanced customization.
- [Data Widgets Barrel Export](./data-widgets-barrel-export.md) — Barrel file that re-exports Ripple's data-display widgets — `Table` and `Chart` — from a single import path. These two widgets cover the primary structured-data visualization needs in Ripple-generated UIs.
- [Metric Widget — KPI Display with Trend Badge and Layout Variants](./metric-widget-kpi-display-with-trend-badge-and-layout-variants.md) — A focused display widget for key performance indicators, rendering a labeled numeric value alongside an optional trend badge that automatically applies semantic color (positive green, negative red, neutral gray) based on the trend string's leading sign character. Supports three layout variants for different dashboard density requirements.
- [Table Widget — Schema-Driven Data Grid with Row Interaction](./table-widget-schema-driven-data-grid-with-row-interaction.md) — A flexible data table widget that renders structured row/column data with automatic column detection, multiple visual variants, an optional status indicator column, and full Ripple event system integration for row-click actions. It normalizes several column definition formats to accommodate the range of shapes that AI-generated specs may produce.

### `data-binding` — 1 article(s)

- [Expression Resolver — Template Binding and Condition Evaluation](./expression-resolver-template-binding-and-condition-evaluation.md) — The expression resolver evaluates `{state.x}` style binding expressions found in widget props, event handler values, and condition strings. It supports dot-notation paths, comparison operators, logical operators, ternary expressions, and template string interpolation without relying on `eval` or `Function`.

### `data-display` — 9 article(s)

- [KvTable: Key-Value Data Grid with Single or Two-Column Layout](./kvtable-key-value-data-grid-with-single-or-two-column-layout.md) — KvTable renders an array of key-value pairs in a bordered, optionally-striped grid that can be displayed in one or two columns — with automatic midpoint splitting for the two-column case. It exists to present dense structured data (financial metrics, specs, metadata) in a compact tabular layout without requiring a full HTML table.
- [Table Body — Data Row Container with Last-Row Border Suppression](./table-body-data-row-container-with-last-row-border-suppression.md) — TableBody renders a styled `<tbody>` element that wraps the data rows of a table, applying a targeted CSS rule to remove the bottom border from the last row. This prevents a double-border artifact at the boundary between `<tbody>` and `<tfoot>` (or the table edge).
- [Table Caption — Accessible Table Description Element](./table-caption-accessible-table-description-element.md) — TableCaption renders a styled HTML `<caption>` element that provides an accessible, programmatically associated description for a table. Positioned below the table by default via `margin-top`, it uses muted foreground and small text to visually distinguish itself from table data.
- [Table Cell — Data Cell with Checkbox Padding Adjustment](./table-cell-data-cell-with-checkbox-padding-adjustment.md) — TableCell renders a styled `<td>` element with consistent padding, vertical alignment, and a defensive CSS rule that removes right padding when the cell contains a checkbox. This prevents double-spacing in selection column cells without requiring conditional props or wrapper divs.
- [Table Footer — Summary Row Container with Subtle Background](./table-footer-summary-row-container-with-subtle-background.md) — TableFooter renders a styled `<tfoot>` element for housing summary or aggregate rows at the bottom of a table, applying a muted semi-transparent background and a top border while removing the bottom border from its last row to prevent double borders.
- [Table Head — Column Header Cell with Checkbox Spacing](./table-head-column-header-cell-with-checkbox-spacing.md) — TableHead renders a styled `<th>` element for column header cells, applying consistent height, horizontal padding, left-aligned text, and the same checkbox-column padding fix used in TableCell. It ensures header cells align with their body counterparts in selection columns.
- [Table Header — Column Group Container with Row Border Rules](./table-header-column-group-container-with-row-border-rules.md) — TableHeader renders a styled `<thead>` element that groups all column header rows, applying a CSS rule that adds a bottom border to every `<tr>` it contains. This ensures header rows are always visually separated from the data body without requiring individual row styling.
- [Table Index — Full Component Family Public API](./table-index-full-component-family-public-api.md) — The table index module assembles all eight table sub-components — Root, Body, Caption, Cell, Footer, Head, Header, Row — into a single importable surface with both short canonical names and full descriptive aliases. This barrel file is the only import consumers need for complete table functionality.
- [Ticker: Stock Ticker Strip with Symbol, Price, and Change](./ticker-stock-ticker-strip-with-symbol-price-and-change.md) — Ticker renders one or more stock quotes in a horizontally-scrollable pill strip — each showing symbol, price, and color-coded change value with optional percentage. It uses string prefix detection to determine bullish/bearish coloring, matching how financial data APIs deliver pre-formatted change strings.

### `data-model` — 2 article(s)

- [C4 Widget Type Definitions — Data Model for All Four C4 Levels](./c4-widget-type-definitions-data-model-for-all-four-c4-levels.md) — The TypeScript type definitions for the entire C4 widget subsystem, covering all four C4 hierarchy levels (Person, System, Container, Component), relationships, diagram spec, and the internal SvelteFlow node data payload. These types are the contract between diagram data authors and the rendering engine.
- [Workflow Widget Type Definitions](./workflow-widget-type-definitions.md) — TypeScript interfaces and union types that define the data contract for the workflow widget's nodes and edges, enabling type-safe pocket spec authoring and enforcing the six valid node types and five execution status states.

### `data-resolution` — 1 article(s)

- [favicon.ts: Source-to-Favicon URL Resolver](./faviconts-source-to-favicon-url-resolver.md) — The `favicon.ts` utility exports a single `faviconUrl()` function that maps a source name string to a Google Favicon Service URL. It maintains a hand-curated domain map for well-known publishers (particularly Indian financial and news sources) and falls back to heuristic domain inference for unknown sources.

### `data-transform` — 1 article(s)

- [Pattern Detector — Quiz, Results, and Chart Data Transforms](./pattern-detector-quiz-results-and-chart-data-transforms.md) — The pattern detector identifies specialized data shapes — quizzes, results summaries, and chart data — from generic intent specs and normalizes them into typed structures that their respective renderer components expect. It was extracted from IntentRenderer to keep detection logic modular and independently testable.

### `data-visualization` — 10 article(s)

- [AnalystBar: Analyst Rating Distribution Visualization](./analystbar-analyst-rating-distribution-visualization.md) — AnalystBar renders a proportional color-segmented bar showing the breakdown of analyst ratings (Buy, Hold, Sell) for a financial instrument, along with consensus label and target price. It uses Svelte 5 `$derived` reactivity to compute segment widths from raw counts, gracefully handling the zero-total edge case.
- [Chart Component Index — Public API Barrel for Chart System](./chart-component-index-public-api-barrel-for-chart-system.md) — The chart `index.ts` barrel exports `ChartContainer` and `ChartTooltip` as the public surface of the chart system, alongside `ChartConfig` type and `getPayloadConfigFromPayload` utility for consumer-level payload handling.
- [Chart Container — LayerChart Integration Host with CSS Override Layer](./chart-container-layerchart-integration-host-with-css-override-layer.md) — ChartContainer is the root wrapper for all chart widgets. It establishes chart context, generates a unique scoped chart ID, injects theme-based CSS variables, and applies a comprehensive set of Tailwind overrides that normalize LayerChart's default visual behavior.
- [Chart Style — Dynamic CSS Variable Injection for Theme-Aware Chart Colors](./chart-style-dynamic-css-variable-injection-for-theme-aware-chart-colors.md) — ChartStyle generates and injects a `<style>` block at runtime that maps chart series identifiers to CSS custom properties, supporting both light and dark theme variants. It is a headless utility component — it produces no visible DOM, only scoped CSS.
- [Chart Tooltip — Rich Data Series Overlay with Configurable Indicators](./chart-tooltip-rich-data-series-overlay-with-configurable-indicators.md) — ChartTooltip is a styled hover overlay for chart widgets that reads active data series from LayerChart's tooltip context, resolves each series label and color from `ChartConfig`, and renders rows with configurable indicator shapes (dot, line, or dashed). It supports custom formatters via Svelte snippets.
- [RangeBar: Position Marker on a Min-Max Value Track](./rangebar-position-marker-on-a-min-max-value-track.md) — RangeBar renders a labeled track with a colored fill and a circular position marker showing where a current value falls within a min-max range — commonly used for 52-week price ranges, analyst target ranges, or any metric with natural bounds. It clamps the marker position defensively and falls back to the midpoint when min equals max.
- [Spec Showcase — Card + Chart + Stat Composition Demo](./spec-showcase-card-chart-stat-composition-demo.md) — A showcase page that renders four analytics dashboard cards (revenue, latency, signups, error share) using the Ripple spec format, demonstrating how Chart, Stat, and Card compose together with slot injection to build production-style metric panels.
- [Stat Metric Widget with Delta and Direction](./stat-metric-widget-with-delta-and-direction.md) — A data metric widget for dashboards that renders a labeled numeric value with optional change indicator (delta chip). Handles locale-aware number formatting across four modes — number, currency, percent, and compact — and supports a sophisticated direction/sentiment system that decouples visual arrow direction from color meaning.
- [Stat Showcase — Visual QA for Metric Display Widget](./stat-showcase-visual-qa-for-metric-display-widget.md) — A comprehensive visual QA page for the Ripple Stat widget, covering all three sizes, five number formats, the full direction-semantics matrix, label/alignment options, and Card composition.
- [Timeline: Chronological Event List with Color-Coded Dots](./timeline-chronological-event-list-with-color-coded-dots.md) — Timeline renders a vertical list of dated events connected by a continuous rail line — each event has a colored dot, date label, title, and optional detail text. It supports five semantic dot types plus custom color overrides, truncation via maxItems, and a trailing overflow indicator.

### `demo` — 2 article(s)

- [Home Page — Pocket Gallery with Detail View](./home-page-pocket-gallery-with-detail-view.md) — The Ripple app's main landing page, which renders a gallery of interactive pocket demos (Research, Market Watch, Weather, and others) in an accordion layout and expands any selected pocket into a full detail view powered by the Ripple component.
- [Showcase Index — Full Widget Catalog](./showcase-index-full-widget-catalog.md) — The primary showcase page for the Ripple widget library, rendering every core widget type through inline pocket specs that are fed to the Ripple component, serving as both a visual regression reference and a live usage guide.

### `design-tokens` — 1 article(s)

- [Separator Component — Accessible Horizontal and Vertical Divider](./separator-component-accessible-horizontal-and-vertical-divider.md) — A styled wrapper around bits-ui's `Separator.Root` that renders a 1px divider line in either horizontal or vertical orientation. It applies design-token colors and includes a deliberate deviation from the upstream shadcn/ui pattern to fix a self-stretch layout bug.

### `diagram` — 14 article(s)

- [C4 Component Node — SvelteFlow Custom Node for Service Components](./c4-component-node-svelteflow-custom-node-for-service-components.md) — A SvelteFlow custom node that renders a C4 Component element as a compact blue box with a four-square icon, subtype label, technology badge, and optional knowledge base link. URL sanitization prevents XSS via `javascript:` or `data:` scheme injection in the KB link.
- [C4 Container Node — SvelteFlow Custom Node with Drill-Down Support](./c4-container-node-svelteflow-custom-node-with-drill-down-support.md) — A SvelteFlow custom node that renders a C4 Container element (API servers, databases, queues, etc.) as a blue-700 box with technology label. Supports drill-down navigation from Container to Component level, and includes URL-sanitized knowledge base links.
- [C4 Database Node — Cylinder-Shaped SvelteFlow Node for Storage Elements](./c4-database-node-cylinder-shaped-svelteflow-node-for-storage-elements.md) — A SvelteFlow custom node that renders a C4 database container using a CSS-composed cylinder shape (SVG ellipse cap + rectangular body). Visually distinct from other node types with a purple color scheme, and includes URL-sanitized KB links.
- [C4 Diagram Widget — SvelteFlow + ELK Architecture Visualizer](./c4-diagram-widget-svelteflow-elk-architecture-visualizer.md) — The top-level C4 Model diagram widget that uses SvelteFlow for rendering and ELK.js for automatic layout. It supports all four C4 levels (Context, Container, Component, Code) with drill-down navigation, pan/zoom, and a minimap.
- [C4 Group Node — System Boundary Container for SvelteFlow Parent Nodes](./c4-group-node-system-boundary-container-for-svelteflow-parent-nodes.md) — A SvelteFlow parent/group node that renders a C4 system boundary as a dashed border container. It acts as the visual wrapper for nested container nodes, providing the "system boundary box" pattern from C4 architecture diagrams.
- [C4 Node Components Barrel — Exports All SvelteFlow Custom Node Types](./c4-node-components-barrel-exports-all-svelteflow-custom-node-types.md) — The barrel file for the C4 node component directory, re-exporting all seven custom SvelteFlow node types under a single import path. This keeps `C4Diagram.svelte`'s node type registration clean and allows individual node components to be imported independently by consumers.
- [C4 Person Node — Human Actor Node for C4 Context Diagrams](./c4-person-node-human-actor-node-for-c4-context-diagrams.md) — A SvelteFlow custom node that renders a C4 Person element with a circular avatar icon and name/description labels. Distinguishes internal users (blue) from external actors (gray) and supports optional drill-down navigation.
- [C4 Queue Node — Parallelogram-Shaped Node for Message Brokers](./c4-queue-node-parallelogram-shaped-node-for-message-brokers.md) — A SvelteFlow custom node that renders C4 message queue and broker elements using an amber CSS parallelogram shape. Uses `clip-path` for the skewed visual, includes URL-sanitized KB links, and fires click handlers for element selection.
- [C4 System Node — Top-Level Architecture Block with Drill-Down](./c4-system-node-top-level-architecture-block-with-drill-down.md) — A SvelteFlow custom node that renders a C4 System element as a rounded rectangle, with blue styling for internal systems and gray dashed borders for external ones. Supports multi-level drill-down from Context through Component, and carries reactive style bindings for all color properties.
- [C4 Widget Public API Barrel — Exports for Component, Types, and Layout](./c4-widget-public-api-barrel-exports-for-component-types-and-layout.md) — The barrel export file for the C4 diagram widget subsystem. It re-exports the `C4Diagram` Svelte component, layout utilities, and all TypeScript type definitions from a single import path, establishing the public API surface of the C4 module.
- [C4 Widget Type Definitions — Data Model for All Four C4 Levels](./c4-widget-type-definitions-data-model-for-all-four-c4-levels.md) — The TypeScript type definitions for the entire C4 widget subsystem, covering all four C4 hierarchy levels (Person, System, Container, Component), relationships, diagram spec, and the internal SvelteFlow node data payload. These types are the contract between diagram data authors and the rendering engine.
- [ELK-Based Auto-Layout Engine for C4 Diagrams](./elk-based-auto-layout-engine-for-c4-diagrams.md) — Converts a `C4Diagram` data structure into ELK graph nodes and edges, runs the ELK layered layout algorithm, and returns a flat `Map<id, LayoutPosition>` of computed positions. Replaced an earlier grid-based layout to provide professional, collision-free hierarchical placement.
- [Test Suite for C4 ELK Layout Utilities](./test-suite-for-c4-elk-layout-utilities.md) — Vitest unit tests for `elk-layout.ts`, covering the `getNodeType`, `isGroupNode`, and `computeElkLayout` functions. Tests verify correct C4 element classification and that ELK produces valid node positions for realistic diagram fixtures.
- [Workflow Widget — Interactive Node-Based Diagram](./workflow-widget-interactive-node-based-diagram.md) — A Svelte 5 widget that renders interactive workflow diagrams using SvelteFlow (`@xyflow/svelte`). It accepts a simplified `WorkflowNodeData[]` and `WorkflowEdgeData[]` spec and handles auto-layout, type mapping, and interactivity controls transparently.

### `dialog` — 11 article(s)

- [Dialog Close — Type-Safe Close Trigger with Default Button Semantics](./dialog-close-type-safe-close-trigger-with-default-button-semantics.md) — DialogClose is a minimal wrapper around bits-ui's `Dialog.Close` primitive. Its sole substantive addition is defaulting `type` to `"button"` — a defensive fix that prevents form submission side effects when the close trigger is rendered inside a `<form>` element.
- [Dialog Component Index — Dual-Name Export Surface](./dialog-component-index-dual-name-export-surface.md) — The public barrel file for the Ripple dialog system. It exports all ten sub-components under two naming conventions — short names (`Root`, `Title`, etc.) for destructured imports and prefixed names (`Dialog`, `DialogTitle`, etc.) for namespace imports — giving consumers flexibility in how they reference components.
- [Dialog Content — Animated Modal Panel with Portal and Optional Close Button](./dialog-content-animated-modal-panel-with-portal-and-optional-close-button.md) — DialogContent assembles the visible dialog panel. It wraps children in a portal (for z-index and stacking context isolation), renders the background overlay, and optionally includes an `X` close button in the top-right corner. Enter/exit animations are driven by bits-ui's `data-open`/`data-closed` attributes.
- [Dialog Description — Accessible Subtitle with Auto-Styled Link Formatting](./dialog-description-accessible-subtitle-with-auto-styled-link-formatting.md) — DialogDescription wraps bits-ui's `Dialog.Description` primitive to render the dialog's accessible subtitle text. It automatically styles inline anchor links with underlines and hover effects, and exposes its content to ARIA via the primitive's `aria-describedby` wiring.
- [Dialog Footer — Action Bar with Optional Close Button](./dialog-footer-action-bar-with-optional-close-button.md) — Renders the bottom action area of a dialog, laying out child elements (typically buttons) in a responsive row. Optionally injects a Close button wired to the bits-ui dialog primitive for zero-configuration dismiss behavior.
- [Dialog Header — Title and Description Container](./dialog-header-title-and-description-container.md) — A minimal layout wrapper that vertically stacks title and description elements at the top of a dialog panel. It provides consistent vertical spacing and a `data-slot` marker for structural targeting.
- [Dialog Overlay — Animated Backdrop with Backdrop Filter](./dialog-overlay-animated-backdrop-with-backdrop-filter.md) — Wraps the bits-ui `DialogPrimitive.Overlay` to produce a full-viewport dimmed backdrop behind an open dialog. Applies entrance/exit animations and a conditional blur effect, with progressive enhancement for browsers that support the backdrop-filter CSS property.
- [Dialog Portal — DOM Teleportation Wrapper](./dialog-portal-dom-teleportation-wrapper.md) — A one-line pass-through component that delegates to bits-ui's `DialogPrimitive.Portal`, teleporting its children out of the current DOM tree and into the document body. This prevents z-index conflicts caused by stacking contexts in ancestor elements.
- [Dialog Root — Open State Provider and Context Root](./dialog-root-open-state-provider-and-context-root.md) — The root component of the Ripple dialog system. It wraps bits-ui's `DialogPrimitive.Root` and provides a bindable `open` prop that allows parent components to externally control dialog visibility. All other dialog components must be rendered as descendants of this root.
- [Dialog Title — Accessible Heading with Typography Defaults](./dialog-title-accessible-heading-with-typography-defaults.md) — Wraps bits-ui's `DialogPrimitive.Title` to render the dialog's primary heading with consistent typographic styling. The primitive handles ARIA role assignment automatically, making the title accessible to screen readers without manual `role` or `aria-labelledby` wiring.
- [Dialog Trigger — Open Button Wired to Dialog State](./dialog-trigger-open-button-wired-to-dialog-state.md) — A thin wrapper around bits-ui's `DialogPrimitive.Trigger` that opens the parent dialog when activated. Defaults `type` to `"button"` to prevent accidental form submissions, and exposes a bindable ref for DOM access.

### `display` — 15 article(s)

- [Avatar Widget — User Image with Fallback Initials](./avatar-widget-user-image-with-fallback-initials.md) — A display widget that renders a user or entity avatar using shadcn's Avatar component, automatically falling back to a placeholder character when the image is absent or fails to load. Supports inline style objects passed as records, which it serializes to a CSS style string.
- [Badge Widget — Status and Category Label with Variant Extension](./badge-widget-status-and-category-label-with-variant-extension.md) — A display widget that renders inline badges for status labels, categories, and tags, extending shadcn's Badge with two semantic variants (`success` and `warning`) that shadcn doesn't natively support. The extension is implemented through CSS class injection rather than shadcn variant registration, keeping the component self-contained.
- [Display Widget Barrel Export](./display-widget-barrel-export.md) — The barrel export file for Ripple's display widget category, re-exporting all eleven display primitives through a single import path. This indirection layer lets consumers and the widget registry import from `./display/index.js` without tracking individual component file paths.
- [Feed Widget — Timestamped Activity List with Type Indicators](./feed-widget-timestamped-activity-list-with-type-indicators.md) — A compact vertical feed component that renders a list of activity items — each with a colored dot, text, and optional timestamp — suitable for audit logs, notification streams, event histories, and agent activity panels. Dot colors are driven by semantic type values or custom color overrides.
- [Heading Widget — Semantic H1–H6 with Design System Typography](./heading-widget-semantic-h1h6-with-design-system-typography.md) — A typography widget that renders the correct semantic heading element (h1–h6) based on a `level` prop, with each level pre-mapped to a consistent Tailwind typography scale. Solves the problem of AI-generated specs needing to express hierarchy without embedding raw HTML tags.
- [Image Widget — Schema-Friendly img Element with Fit and Radius Controls](./image-widget-schema-friendly-img-element-with-fit-and-radius-controls.md) — A display widget that renders a native `<img>` element with declarative control over object-fit behavior, border radius, and dimensions via a JSON-serializable prop interface. Converts numeric dimensions to pixel strings and merges inline style objects from spec definitions.
- [Metric Widget — KPI Display with Trend Badge and Layout Variants](./metric-widget-kpi-display-with-trend-badge-and-layout-variants.md) — A focused display widget for key performance indicators, rendering a labeled numeric value alongside an optional trend badge that automatically applies semantic color (positive green, negative red, neutral gray) based on the trend string's leading sign character. Supports three layout variants for different dashboard density requirements.
- [Progress Bar Widget](./progress-bar-widget.md) — A thin wrapper around the shadcn/ui Progress component that adds height variants and runtime color overrides via CSS custom properties. Used by Ripple's generative UI runtime to render determinate progress indicators from a JSON spec.
- [Skeleton Loading Placeholder Widget](./skeleton-loading-placeholder-widget.md) — A lightweight shimmer placeholder shown by Ripple while a streaming spec has not yet produced a valid first parse. Provides four layout variants — card, dashboard, text, and none — so the UI can match the shape of the content it is waiting for.
- [Soul Status Display Widget](./soul-status-display-widget.md) — A compact or expanded display widget that surfaces an AI agent's soul state — including name, role, mood, energy level, memory count, and last action. Designed for dashboards that need to show multiple agents at a glance, with an inline energy bar and status dot in compact mode and a full detail panel in expanded mode.
- [Stat Metric Widget with Delta and Direction](./stat-metric-widget-with-delta-and-direction.md) — A data metric widget for dashboards that renders a labeled numeric value with optional change indicator (delta chip). Handles locale-aware number formatting across four modes — number, currency, percent, and compact — and supports a sophisticated direction/sentiment system that decouples visual arrow direction from color meaning.
- [Table Widget — Schema-Driven Data Grid with Row Interaction](./table-widget-schema-driven-data-grid-with-row-interaction.md) — A flexible data table widget that renders structured row/column data with automatic column detection, multiple visual variants, an optional status indicator column, and full Ripple event system integration for row-click actions. It normalizes several column definition formats to accommodate the range of shapes that AI-generated specs may produce.
- [Terminal Widget — Read-Only and Interactive Log Display](./terminal-widget-read-only-and-interactive-log-display.md) — A styled terminal emulator widget for Ripple that renders sequential log lines with per-type color coding (stdout, stderr, info, command) and optionally exposes a command input field. Auto-scrolls to the bottom when new lines arrive, making it suitable for streaming agent output, build logs, or shell session playback.
- [Test Suite for Stat Widget](./test-suite-for-stat-widget.md) — Comprehensive Vitest + Testing Library test suite for the Stat display widget covering number formatting, all direction/sentiment combinations, delta chip rendering, size variants, and Lucide icon selection. Uses data attributes as stable test hooks rather than class names.
- [Text Display Widget](./text-display-widget.md) — A foundational text rendering primitive that maps a flat set of typography props — size, weight, color, and inline mode — to semantic HTML elements. Renders as a `<p>` block element by default or an inline `<span>` when the `inline` prop is set.

### `event-system` — 4 article(s)

- [EventDispatcher — Multistep Flow Action Execution Engine](./eventdispatcher-multistep-flow-action-execution-engine.md) — The EventDispatcher is Ripple's core runtime for executing declarative event handlers against a StateManager. It supports 12 action types including composite multi-step flows, async API chaining, conditional branching, user confirmation dialogs, validation guards, and direct widget method invocation.
- [FollowUp: Inline Question Input for Conversational Research](./followup-inline-question-input-for-conversational-research.md) — FollowUp renders a pill-shaped text input with a send button that emits follow-up queries back into the Ripple event system — bridging widget-level user input with the runtime's event dispatcher. It implements empty-string guarding, Enter-key submission, and graceful degradation when the event system context is absent.
- [SourcesBar: Aggregated Sources Summary Bar with Copy and Share](./sourcesbar-aggregated-sources-summary-bar-with-copy-and-share.md) — SourcesBar renders a compact row showing stacked source favicons and a count label alongside copy and share action buttons — serving as a persistent summary of all sources cited in a research response. It integrates with the Ripple event system to dispatch toast notifications and supports the Web Share API with clipboard fallback.
- [Test Suite: EventDispatcher — Phase B Multistep Flow Actions](./test-suite-eventdispatcher-phase-b-multistep-flow-actions.md) — Behavior specs for the Phase B multistep event dispatcher, covering all action types including flow, branch, confirm, validate, delay, api, and invoke. Tests also verify backwards compatibility with the legacy flat-array dispatch pattern and enforce nesting depth limits.

### `events` — 2 article(s)

- [Event Handler Schema — Widget Interaction Action Types](./event-handler-schema-widget-interaction-action-types.md) — This module defines the complete Zod schema for every action a widget can fire in response to user interaction. It covers simple side-effect actions (set state, navigate, toast) through to async flow control (sequential flows, conditional branches, confirmation dialogs, and API calls with on_success/on_error chaining).
- [RippleEvent and RippleEventResult — Host Integration Types](./rippleevent-and-rippleeventresult-host-integration-types.md) — These two types define the contract between Ripple's internal event dispatcher and the host application. `RippleEvent` carries the payload for every host-delegated action; `RippleEventResult` carries the host's response back to enable async continuation chains (on_success / on_error) for API actions.

### `exports` — 2 article(s)

- [Research Widget Barrel Export](./research-widget-barrel-export.md) — Central re-export index for all research-domain UI widgets in the Ripple library. Groups thirteen Svelte components under a single import path so consumers never depend on internal file layout.
- [Workflow Widget Barrel Export](./workflow-widget-barrel-export.md) — The public API index for the workflow widget submodule, re-exporting the custom SvelteFlow node component, the auto-layout function, and the TypeScript types that consumers need to construct workflow specs.

### `expressions` — 1 article(s)

- [Expression Resolver — Template Binding and Condition Evaluation](./expression-resolver-template-binding-and-condition-evaluation.md) — The expression resolver evaluates `{state.x}` style binding expressions found in widget props, event handler values, and condition strings. It supports dot-notation paths, comparison operators, logical operators, ternary expressions, and template string interpolation without relying on `eval` or `Function`.

### `favicon` — 1 article(s)

- [favicon.ts: Source-to-Favicon URL Resolver](./faviconts-source-to-favicon-url-resolver.md) — The `favicon.ts` utility exports a single `faviconUrl()` function that maps a source name string to a Google Favicon Service URL. It maintains a hand-curated domain map for well-known publishers (particularly Indian financial and news sources) and falls back to heuristic domain inference for unknown sources.

### `feedback` — 2 article(s)

- [Alert Component Public API and Barrel Export](./alert-component-public-api-and-barrel-export.md) — The alert index file is the single public entry point for the entire alert component family, re-exporting all sub-components under both short-form and fully-qualified names. This dual-alias pattern gives consumers flexibility to use destructured imports or prefixed names without creating ambiguity.
- [Progress Bar — Animated Linear Progress Indicator](./progress-bar-animated-linear-progress-indicator.md) — A styled wrapper around bits-ui's `Progress.Root` that renders a horizontal progress bar with a CSS transform-based fill animation. It accepts `value` (current progress) and `max` (ceiling, default 100) props, renders an inner indicator div styled as the primary color, and uses `translateX` rather than `width` changes to animate smoothly.

### `finance` — 5 article(s)

- [AnalystBar: Analyst Rating Distribution Visualization](./analystbar-analyst-rating-distribution-visualization.md) — AnalystBar renders a proportional color-segmented bar showing the breakdown of analyst ratings (Buy, Hold, Sell) for a financial instrument, along with consensus label and target price. It uses Svelte 5 `$derived` reactivity to compute segment widths from raw counts, gracefully handling the zero-total edge case.
- [CompanyHeader: Company Identity and Live Price Block](./companyheader-company-identity-and-live-price-block.md) — CompanyHeader renders a full company identity card combining logo (with Clearbit auto-fetch and initial fallback), name, ticker, exchange badge, description, sector tags, live price, and price change — all from a single composable widget. It is the canonical top-of-widget header for stock and company research responses.
- [NewsCard: Scannable News Item Row with Sentiment Badge](./newscard-scannable-news-item-row-with-sentiment-badge.md) — NewsCard renders a news article as a list row with source favicon, publisher name, timestamp, optional sentiment badge (bullish/bearish/neutral), truncated headline, and thumbnail image. It is designed for dense news feed layouts where multiple items stack vertically, with separators between items and the last item having no bottom border.
- [RangeBar: Position Marker on a Min-Max Value Track](./rangebar-position-marker-on-a-min-max-value-track.md) — RangeBar renders a labeled track with a colored fill and a circular position marker showing where a current value falls within a min-max range — commonly used for 52-week price ranges, analyst target ranges, or any metric with natural bounds. It clamps the marker position defensively and falls back to the midpoint when min equals max.
- [Ticker: Stock Ticker Strip with Symbol, Price, and Change](./ticker-stock-ticker-strip-with-symbol-price-and-change.md) — Ticker renders one or more stock quotes in a horizontally-scrollable pill strip — each showing symbol, price, and color-coded change value with optional percentage. It uses string prefix detection to determine bullish/bearish coloring, matching how financial data APIs deliver pre-formatted change strings.

### `flow-actions` — 5 article(s)

- [ConfirmDialog Overlay Widget](./confirmdialog-overlay-widget.md) — An auto-mounted confirmation dialog that renders when the event dispatcher writes a `PendingConfirm` object to `state._ripple_confirm`. On user decision, it calls `resolveConfirm` to resume the suspended flow, with a fallback that directly clears state if the resolver is stale — preventing flows from hanging indefinitely.
- [EventDispatcher — Multistep Flow Action Execution Engine](./eventdispatcher-multistep-flow-action-execution-engine.md) — The EventDispatcher is Ripple's core runtime for executing declarative event handlers against a StateManager. It supports 12 action types including composite multi-step flows, async API chaining, conditional branching, user confirmation dialogs, validation guards, and direct widget method invocation.
- [Flow Actions Showcase — Interactive Form Submit Demo](./flow-actions-showcase-interactive-form-submit-demo.md) — A live showcase page that exercises Ripple's flow action system through a form-submit scenario, demonstrating validate, branch, confirm, api, toast, delay, invoke, and on_success/on_error chaining against a simulated host with a side-panel event log.
- [Test Suite: EventDispatcher — Phase B Multistep Flow Actions](./test-suite-eventdispatcher-phase-b-multistep-flow-actions.md) — Behavior specs for the Phase B multistep event dispatcher, covering all action types including flow, branch, confirm, validate, delay, api, and invoke. Tests also verify backwards compatibility with the legacy flat-array dispatch pattern and enforce nesting depth limits.
- [Test Suite for ConfirmDialog Overlay Widget](./test-suite-for-confirmdialog-overlay-widget.md) — Vitest + Testing Library test suite for `ConfirmDialog`, covering state-driven mount/unmount, confirm and cancel button resolution, and the fallback path where `resolveConfirm` returns false (stale resolver after HMR). The `seed()` helper wires a real `StateManager`, `EventDispatcher`, and `WidgetRegistry` through Svelte context.

### `form` — 22 article(s)

- [Button Component — Polymorphic Interactive Element with Full Variant and Size Matrix](./button-component-polymorphic-interactive-element-with-full-variant-and-size-matr.md) — Button is a fully polymorphic interactive component that renders as either a native `<button>` or an `<a>` element, with a comprehensive variant and size matrix defined via `tailwind-variants`. It includes accessibility-correct disabled state handling for link mode and a detailed icon sizing system for inline icon buttons.
- [Checkbox — Accessible Three-State Toggle with Indeterminate Support](./checkbox-accessible-three-state-toggle-with-indeterminate-support.md) — The Checkbox component wraps bits-ui's `Checkbox.Root` primitive to provide a fully accessible toggle with three visual states: unchecked, checked, and indeterminate. It uses lucide icons as indicators and applies extensive Tailwind classes for focus rings, validation states, and disabled group context.
- [Checkbox Component Index — Minimal Barrel with Dual-Name Export](./checkbox-component-index-minimal-barrel-with-dual-name-export.md) — The checkbox `index.ts` barrel re-exports the single `checkbox.svelte` component under both `Root` and `Checkbox` names. This follows the ripple dual-export convention, allowing both namespace-style and direct-import usage with minimal boilerplate.
- [Checkbox Input Widget](./checkbox-input-widget.md) — A controlled checkbox widget that wraps the shadcn/ui Checkbox primitive with optional label layout and a local state sync pattern that bridges Svelte 5 reactive props with the underlying component's internal checked state.
- [Input — Unified Text and File Input with Dual Rendering Paths](./input-unified-text-and-file-input-with-dual-rendering-paths.md) — The core Ripple input component handles both standard text-like inputs and file inputs through a conditional rendering split. File inputs require separate treatment because Svelte's `bind:files` binding is only valid on `type="file"` elements, and TypeScript's type system enforces this distinction at the prop level.
- [Radio Group Barrel — Dual-Export Entry Point for Root and Item](./radio-group-barrel-dual-export-entry-point-for-root-and-item.md) — The index module for the radio group component exports both the `Root` container and `Item` option under structural and semantic names, enabling consumers to import as `{ RadioGroup, RadioGroupItem }` or `{ Root, Item }`. It establishes the two-component composition model that every radio group must follow.
- [Radio Group Item — Accessible Option with Visual Check Indicator](./radio-group-item-accessible-option-with-visual-check-indicator.md) — A styled radio button option that wraps bits-ui's `RadioGroup.Item` primitive, rendering a circular border that fills with the primary color when selected, with a `CircleIcon` indicator that appears conditionally via Svelte 5 snippets. It handles checked, focused, disabled, and invalid states through data attributes and ARIA-driven Tailwind variants.
- [Radio Group Root — Selection Container with Bindable Value](./radio-group-root-selection-container-with-bindable-value.md) — The root container for a radio group, wrapping bits-ui's `RadioGroup.Root` to expose a two-way bindable `value` prop defaulting to an empty string. It renders as a full-width CSS grid with 8px gaps and provides the selection context that child `RadioGroupItem` components read from.
- [Select Component Barrel — Full Composition API with 11 Sub-Components](./select-component-barrel-full-composition-api-with-11-sub-components.md) — The index module for the select component family exports all 11 sub-components under both structural (`Root`, `Item`, etc.) and semantic (`Select`, `SelectItem`, etc.) names. It defines the complete public API for the most compositionally complex form control in the Ripple library.
- [Select Dropdown Widget](./select-dropdown-widget.md) — A single-select dropdown widget that wraps shadcn/ui's Select primitive and normalizes a flexible options format — accepting either plain strings or `{value, label}` objects — into a consistent internal structure. Renders an optional label and surfaces the selected label text in the trigger button.
- [Select Group Heading — Accessible Label for Option Groups](./select-group-heading-accessible-label-for-option-groups.md) — A styled wrapper around bits-ui's `SelectPrimitive.GroupHeading` that renders small, muted text as a non-interactive section header within grouped select options. It links to its parent `SelectGroup` via ARIA and is excluded from the keyboard navigation sequence.
- [Select Group — Semantic Container for Grouped Options](./select-group-semantic-container-for-grouped-options.md) — A minimal wrapper around bits-ui's `SelectPrimitive.Group` that applies consistent internal padding and scroll margins, acting as the ARIA grouping container that associates a heading label with its child options. It renders no visual chrome beyond spacing.
- [Select Item — Selectable Option with Checkmark Indicator](./select-item-selectable-option-with-checkmark-indicator.md) — A styled selectable option for the select dropdown that renders a checkmark when selected and accepts either a `label` string, a `value` fallback, or a custom children snippet with access to `selected` and `highlighted` state. It handles all interactive states — hover, keyboard highlight, selected, disabled — through Tailwind data-attribute variants.
- [Select Label — Standalone Descriptive Label for Select Content Panels](./select-label-standalone-descriptive-label-for-select-content-panels.md) — A plain `div`-based label component for use within select content panels, rendering small muted text without any bits-ui backing. It differs from `SelectGroupHeading` in that it is not associated with a group via ARIA and serves as a freestanding visual annotation rather than a semantic section header.
- [Select Root — Bindable State Entry Point for Dropdowns](./select-root-bindable-state-entry-point-for-dropdowns.md) — The top-level select component that owns `open` and `value` state as bindable props, enabling two-way data binding with parent components. It delegates all behavior to bits-ui's `Select.Root` while providing Svelte 5-idiomatic state management.
- [Select Trigger — The Dropdown Open/Close Button](./select-trigger-the-dropdown-openclose-button.md) — The visible button element users click to open or close a select dropdown. It combines value display, size variants, validation states, and a chevron icon into a single polished interactive control that integrates with bits-ui's accessibility layer.
- [Slider Component — Accessible Range Input with Orientation Support](./slider-component-accessible-range-input-with-orientation-support.md) — The Slider component wraps bits-ui's SliderPrimitive.Root to render an accessible, styled range input supporting horizontal and vertical orientations, multiple thumbs, and two-way value binding. It composes a track, a filled range bar, and dynamically generated thumb elements using Svelte 5 snippets.
- [Switch Component — Accessible Toggle with Size Variants](./switch-component-accessible-toggle-with-size-variants.md) — The Switch component wraps bits-ui's SwitchPrimitive to deliver an accessible, animated toggle control with `sm` and `default` size variants, two-way `checked` binding, validation error state, and RTL support. It uses CSS data attributes and Tailwind variants for all visual states, keeping zero state logic in the component itself.
- [Textarea Component Public API Index](./textarea-component-public-api-index.md) — The barrel file for the Textarea component, re-exporting the single `textarea.svelte` implementation under both `Root` and `Textarea` names. Provides the canonical import point for the component.
- [Textarea Input Component](./textarea-input-component.md) — A styled multi-line text input that auto-sizes to content, handles validation states visually, and supports dark mode. Exposes bindable `value` and `ref` props and forwards all native textarea attributes.
- [Textarea Input Widget](./textarea-input-widget.md) — A controlled multi-line text input widget that wraps the shadcn Textarea primitive with Ripple's prop and event conventions. It maintains local state synchronized with an incoming `value` prop and surfaces `onchange`, `onfocus`, and `onblur` callbacks for flow integration.
- [Toggle Switch Input Widget](./toggle-switch-input-widget.md) — A boolean toggle switch widget that wraps shadcn/ui's Switch primitive with an optional label layout and the same local state sync pattern used by the Checkbox widget. Provides a more visually distinctive alternative to Checkbox for on/off settings.

### `generative-ui` — 2 article(s)

- [Progress Bar Widget](./progress-bar-widget.md) — A thin wrapper around the shadcn/ui Progress component that adds height variants and runtime color overrides via CSS custom properties. Used by Ripple's generative UI runtime to render determinate progress indicators from a JSON spec.
- [Widget Registry — Central Component Map and Registration API](./widget-registry-central-component-map-and-registration-api.md) — The top-level widget registry for Ripple's generative UI runtime. It maintains a mutable map of string type names to Svelte components, provides a CRUD API for runtime registration, and serves as the single import point for all 40+ built-in widgets across layout, display, input, data, control, composite, overlay, research, workflow, and C4 categories.

### `graph-ui` — 1 article(s)

- [WorkflowNode — Custom SvelteFlow Node Component](./workflownode-custom-svelteflow-node-component.md) — A Svelte 5 component that renders a single node inside a SvelteFlow canvas, supporting six semantic node types with distinct colors and icons, five execution status states, and special two-handle branching for condition nodes.

### `input` — 14 article(s)

- [Button Input Widget](./button-input-widget.md) — A fully-featured button widget for Ripple's generative UI runtime, supporting six visual variants, four sizes, loading state with spinner, slot-based leading/trailing icons, and form integration. Designed to prevent double-submission and expose machine-readable state via data attributes.
- [Button Showcase — Visual QA for All Button Variants](./button-showcase-visual-qa-for-all-button-variants.md) — A dedicated visual QA page for the Ripple Button widget, rendering every combination of variant, size, icon slot, loading state, disabled state, and full-width layout to enable manual and automated visual regression testing.
- [Checkbox Input Widget](./checkbox-input-widget.md) — A controlled checkbox widget that wraps the shadcn/ui Checkbox primitive with optional label layout and a local state sync pattern that bridges Svelte 5 reactive props with the underlying component's internal checked state.
- [Input Component Index — Single-Component Barrel with Dual Export](./input-component-index-single-component-barrel-with-dual-export.md) — The public entry point for Ripple's input component. Exports `input.svelte` under both a short name (`Root`) and a semantic alias (`Input`), following the same dual-export convention used across all Ripple UI component families.
- [Input — Unified Text and File Input with Dual Rendering Paths](./input-unified-text-and-file-input-with-dual-rendering-paths.md) — The core Ripple input component handles both standard text-like inputs and file inputs through a conditional rendering split. File inputs require separate treatment because Svelte's `bind:files` binding is only valid on `type="file"` elements, and TypeScript's type system enforces this distinction at the prop level.
- [Input Widgets Barrel Export](./input-widgets-barrel-export.md) — The barrel export for Ripple's input widget family, re-exporting all six interactive input primitives from a single entry point. Consumers import from this index instead of individual file paths, keeping import sites stable as the internal file structure evolves.
- [Select Dropdown Widget](./select-dropdown-widget.md) — A single-select dropdown widget that wraps shadcn/ui's Select primitive and normalizes a flexible options format — accepting either plain strings or `{value, label}` objects — into a consistent internal structure. Renders an optional label and surfaces the selected label text in the trigger button.
- [Test Suite for Button Widget](./test-suite-for-button-widget.md) — Vitest + Testing Library test suite for the Button input widget covering rendering, variant and size data attributes, click event firing, disabled and loading state enforcement, spinner slot rendering, type attribute forwarding, and the data-state machine. Uses userEvent for realistic interaction simulation.
- [Test Suite for Input Widget](./test-suite-for-input-widget.md) — Comprehensive Vitest + Testing Library test suite for the Input text widget, covering rendering, label/id association, size and state data attributes, type forwarding, oninput/onchange event firing, error and helper message rendering, accessibility attributes (aria-invalid, aria-describedby), and disabled/required/readOnly attribute forwarding.
- [Text Input Widget with Widget Registry Integration](./text-input-widget-with-widget-registry-integration.md) — A fully-featured text input widget that supports labels, error/helper messages, prefix/suffix icon slots, and size variants. Uniquely, it opts into Ripple's WidgetRegistry when given an `id`, enabling the `invoke` flow action to call `focus()` on the input remotely from a spec action handler.
- [Textarea Component Public API Index](./textarea-component-public-api-index.md) — The barrel file for the Textarea component, re-exporting the single `textarea.svelte` implementation under both `Root` and `Textarea` names. Provides the canonical import point for the component.
- [Textarea Input Component](./textarea-input-component.md) — A styled multi-line text input that auto-sizes to content, handles validation states visually, and supports dark mode. Exposes bindable `value` and `ref` props and forwards all native textarea attributes.
- [Textarea Input Widget](./textarea-input-widget.md) — A controlled multi-line text input widget that wraps the shadcn Textarea primitive with Ripple's prop and event conventions. It maintains local state synchronized with an incoming `value` prop and surfaces `onchange`, `onfocus`, and `onblur` callbacks for flow integration.
- [Toggle Switch Input Widget](./toggle-switch-input-widget.md) — A boolean toggle switch widget that wraps shadcn/ui's Switch primitive with an optional label layout and the same local state sync pattern used by the Checkbox widget. Provides a more visually distinctive alternative to Checkbox for on/off settings.

### `integration` — 1 article(s)

- [RippleEvent and RippleEventResult — Host Integration Types](./rippleevent-and-rippleeventresult-host-integration-types.md) — These two types define the contract between Ripple's internal event dispatcher and the host application. `RippleEvent` carries the payload for every host-delegated action; `RippleEventResult` carries the host's response back to enable async continuation chains (on_success / on_error) for API actions.

### `intent-engine` — 6 article(s)

- [ChainExecutor — Client-Side Intent Chain Navigation with History](./chainexecutor-client-side-intent-chain-navigation-with-history.md) — ChainExecutor manages multi-step intent navigation entirely on the client, enabling instant transitions between chained specs without AI roundtrips. It maintains a browser-like history stack (with forward navigation), tracks accumulated context across steps, and supports both linear chains and selection-based `chain_map` routing.
- [Intent Module Barrel — lib/intent/index.ts](./intent-module-barrel-libintentindexts.md) — The `lib/intent/index.ts` barrel re-exports Ripple's intent-layer subsystems: the layout engine, pattern detector, chain executor, and dashboard manager. It provides a stable import boundary for the intent-resolution layer, which sits between raw spec parsing and concrete rendering.
- [Layout Engine — Intent-to-Layout Decision System](./layout-engine-intent-to-layout-decision-system.md) — The layout engine translates a UniversalSpec's intent, data shape, and display hints into a concrete LayoutType string that drives which Svelte component renders. It acts as the 'smart' middle layer so AI-generated specs don't need to hard-code visual layouts — instead they declare intent and the engine picks the best fit.
- [Pattern Detector — Quiz, Results, and Chart Data Transforms](./pattern-detector-quiz-results-and-chart-data-transforms.md) — The pattern detector identifies specialized data shapes — quizzes, results summaries, and chart data — from generic intent specs and normalizes them into typed structures that their respective renderer components expect. It was extracted from IntentRenderer to keep detection logic modular and independently testable.
- [Test Suite: Layout Engine — determineLayout and analyzeData](./test-suite-layout-engine-determinelayout-and-analyzedata.md) — Unit tests for the layout engine's two public functions: `determineLayout` (maps a UniversalSpec intent and data shape to a layout string) and `analyzeData` (extracts item count and field names from a spec's data object). Tests cover all intent types, display hint overrides, browse sub-layouts, and data analysis edge cases.
- [UniversalSpec Schema — Gen 2 Intent-Based UI Specification](./universalspec-schema-gen-2-intent-based-ui-specification.md) — UniversalSpec is Ripple's second-generation schema that replaces explicit component trees with high-level intent declarations. The layout engine and pattern detector consume a UniversalSpec to auto-select layouts and data renderers, while `normalizeSpec` provides a lossless migration path from Gen 1 UISpec documents.

### `interaction` — 7 article(s)

- [Dialog Trigger — Open Button Wired to Dialog State](./dialog-trigger-open-button-wired-to-dialog-state.md) — A thin wrapper around bits-ui's `DialogPrimitive.Trigger` that opens the parent dialog when activated. Defaults `type` to `"button"` to prevent accidental form submissions, and exposes a bindable ref for DOM access.
- [Popover Close — Dismiss Button Wired to Popover State](./popover-close-dismiss-button-wired-to-popover-state.md) — A minimal wrapper around bits-ui's `PopoverPrimitive.Close` that renders a button capable of dismissing the parent popover. Exposes a bindable `ref` and forwards all props, with no custom logic beyond the data-slot marker.
- [Popover Trigger — Accessible Activation Element for Popovers](./popover-trigger-accessible-activation-element-for-popovers.md) — Wraps the bits-ui `Popover.Trigger` primitive to give it a `data-slot` identifier and class-merge support, while binding the DOM reference back to the caller. It is the interactive element users click or activate to open a popover, with all accessibility behavior — ARIA attributes, keyboard handling, focus management — delegated to the headless primitive.
- [Reorderable — Drag-to-Reorder Svelte Action](./reorderable-drag-to-reorder-svelte-action.md) — The `reorderable` Svelte action attaches zero-dependency pointer-event drag-and-drop reordering to any container element, supporting grid, masonry, and flex layouts. It uses a ghost element for visual drag feedback, a configurable dead zone to prevent accidental drags, and nearest-edge drop detection with a maximum proximity threshold.
- [Select Item — Selectable Option with Checkmark Indicator](./select-item-selectable-option-with-checkmark-indicator.md) — A styled selectable option for the select dropdown that renders a checkmark when selected and accepts either a `label` string, a `value` fallback, or a custom children snippet with access to `selected` and `highlighted` state. It handles all interactive states — hover, keyboard highlight, selected, disabled — through Tailwind data-attribute variants.
- [Sheet Trigger — Open Control for Slide-in Panels](./sheet-trigger-open-control-for-slide-in-panels.md) — SheetTrigger wraps the bits-ui Dialog.Trigger primitive to provide a typed, ref-aware open control for sheet components. It delegates all open/close state management to the bits-ui context and exposes `data-slot` for consistent component targeting.
- [Slider Component — Accessible Range Input with Orientation Support](./slider-component-accessible-range-input-with-orientation-support.md) — The Slider component wraps bits-ui's SliderPrimitive.Root to render an accessible, styled range input supporting horizontal and vertical orientations, multiple thumbs, and two-way value binding. It composes a track, a filled range bar, and dynamically generated thumb elements using Svelte 5 snippets.

### `interactive` — 7 article(s)

- [Badge Component — Polymorphic Label with Variant System](./badge-component-polymorphic-label-with-variant-system.md) — Badge is a compact label component that renders as either a `<span>` or `<a>` depending on whether an `href` prop is provided. It uses `tailwind-variants` to expose six visual styles and supports inline icon placement with automatic icon sizing and padding adjustments.
- [Button Component — Polymorphic Interactive Element with Full Variant and Size Matrix](./button-component-polymorphic-interactive-element-with-full-variant-and-size-matr.md) — Button is a fully polymorphic interactive component that renders as either a native `<button>` or an `<a>` element, with a comprehensive variant and size matrix defined via `tailwind-variants`. It includes accessibility-correct disabled state handling for link mode and a detailed icon sizing system for inline icon buttons.
- [Button Component Public API Export](./button-component-public-api-export.md) — The button index barrel exports the Button component and all associated types and variant utilities. It follows the dual-alias pattern and additionally surfaces `Props` as a convenience alias for `ButtonProps`, enabling clean generic wrapper typings.
- [Citation: Inline Source Reference Chip with Favicon](./citation-inline-source-reference-chip-with-favicon.md) — Citation renders a compact inline chip showing a source name, its favicon, and an optional superscript citation number — behaving as a clickable link when a URL or click handler is provided. Favicon loading failures are handled gracefully with a colored dot fallback, keeping the component visually consistent even when icon fetching fails.
- [DiscoverCard: Media Card for Discoverable Content Links](./discovercard-media-card-for-discoverable-content-links.md) — DiscoverCard renders a compact card with a 16:10 thumbnail image, title, description, and source label — designed to be placed in horizontal scroll strips to surface recommended articles, topics, or related content. It activates as a button when a URL or click handler is provided.
- [FollowUp: Inline Question Input for Conversational Research](./followup-inline-question-input-for-conversational-research.md) — FollowUp renders a pill-shaped text input with a send button that emits follow-up queries back into the Ripple event system — bridging widget-level user input with the runtime's event dispatcher. It implements empty-string guarding, Enter-key submission, and graceful degradation when the event system context is absent.
- [SourceCard: Compact Source Attribution Card with Headline](./sourcecard-compact-source-attribution-card-with-headline.md) — SourceCard is a small card that pairs a favicon (with dot fallback) and source name with a headline — designed for horizontal citation strips where sources are surfaced as individual tappable cards. It follows the same favicon-error-fallback and conditional-interactivity patterns as Citation and NewsCard.

### `interactivity` — 1 article(s)

- [Flow Actions Showcase — Interactive Form Submit Demo](./flow-actions-showcase-interactive-form-submit-demo.md) — A live showcase page that exercises Ripple's flow action system through a form-submit scenario, demonstrating validate, branch, confirm, api, toast, delay, invoke, and on_success/on_error chaining against a simulated host with a side-panel event log.

### `label` — 2 article(s)

- [Badge Component — Polymorphic Label with Variant System](./badge-component-polymorphic-label-with-variant-system.md) — Badge is a compact label component that renders as either a `<span>` or `<a>` depending on whether an `href` prop is provided. It uses `tailwind-variants` to expose six visual styles and supports inline icon placement with automatic icon sizing and padding adjustments.
- [Badge Component Public API Export](./badge-component-public-api-export.md) — The badge index file is a minimal barrel that re-exports the Badge component and its variant utilities from the single implementation file. It provides the stable import path for the Badge family and makes the `badgeVariants` function and `BadgeVariant` type available for external use.

### `layout` — 72 article(s)

- [Accordion Component Barrel Export](./accordion-component-barrel-export.md) — The accordion `index.ts` barrel re-exports all four accordion sub-components — Root, Content, Item, and Trigger — under both short names and fully-qualified names, enabling two distinct import styles for the accordion compound component.
- [Accordion Content Panel](./accordion-content-panel.md) — AccordionContent wraps the bits-ui `Accordion.Content` primitive to provide the animated reveal/hide behavior for an accordion section's body. It adds a padded inner wrapper with opinionated link and paragraph styles, and exposes a `ref` binding for direct DOM access.
- [Accordion Item Container](./accordion-item-container.md) — AccordionItem is a thin wrapper around the bits-ui `Accordion.Item` primitive that adds a bottom border to all items except the last, maintains a `data-slot` marker for styling, and exposes a `ref` binding. It is the structural unit that groups a trigger and its content panel.
- [Accordion Root Container](./accordion-root-container.md) — The Accordion root component wraps bits-ui's `Accordion.Root` primitive to provide the stateful container for a set of collapsible sections. It exposes bindable `value` and `ref` props, and lays items out in a vertical flex column.
- [Accordion Trigger Button](./accordion-trigger-button.md) — AccordionTrigger wraps the bits-ui `Accordion.Header` and `Accordion.Trigger` primitives to render the clickable row that expands or collapses an accordion item. It renders a chevron icon that switches between down and up variants based on the item's open state using CSS group-aria selectors.
- [Alert Action Slot](./alert-action-slot.md) — AlertAction is a positioning wrapper that places action content (typically a dismiss button or a CTA link) in the top-right corner of an alert. It works in conjunction with the parent `Alert` component's `has-data-[slot=alert-action]:pr-18` rule to prevent the alert body text from overlapping the action.
- [Alert Description Text Block](./alert-description-text-block.md) — AlertDescription renders the body text of an alert with opinionated typography defaults including responsive text balancing, auto-spaced paragraphs, and normalized link styling. It is a thin presentational wrapper with no behavioral logic.
- [Alert Root Component with Variant System](./alert-root-component-with-variant-system.md) — The Alert root component provides a styled notification container with a `default` and `destructive` variant system built with `tailwind-variants`. It uses CSS grid with data-slot selectors to automatically accommodate icon+text layouts and action button placements without requiring callers to specify layout props.
- [Alert Title](./alert-title.md) — AlertTitle renders the heading line of an alert, with automatic column positioning when the alert contains an icon, and normalized link styling. It is a semantic label wrapper with no behavioral logic.
- [autoLayout — BFS Workflow Node Positioning](./autolayout-bfs-workflow-node-positioning.md) — A pure TypeScript function that computes x/y coordinates for workflow nodes using breadth-first search from root nodes, assigning depth-based horizontal positions and vertically centering each depth column. Includes cycle detection to prevent infinite BFS loops in graphs with back-edges.
- [Avatar Badge — Status Indicator Overlay](./avatar-badge-status-indicator-overlay.md) — AvatarBadge renders a small circular overlay anchored to the bottom-right corner of an avatar, used to convey status (online, offline, notifications). It reads the parent avatar's size via CSS group data attributes and scales itself proportionally, hiding its icon content on small avatars where it would otherwise be illegible.
- [Avatar Group Count — Overflow Indicator for Avatar Stacks](./avatar-group-count-overflow-indicator-for-avatar-stacks.md) — AvatarGroupCount renders the overflow indicator at the end of an avatar stack — the '+5' or similar count shown when a group has more members than the display limit. It mirrors the circular shape and sizing of sibling avatars while reading the group's size tier from a parent CSS group selector.
- [Avatar Group — Stacked Avatar Row Container](./avatar-group-stacked-avatar-row-container.md) — AvatarGroup is the layout container that stacks multiple Avatar components into the familiar overlapping-circles row. It establishes the CSS context that child avatars and the overflow count indicator query for size coordination, and applies negative spacing to create the overlapping effect.
- [C4 Diagram Widget — SvelteFlow + ELK Architecture Visualizer](./c4-diagram-widget-svelteflow-elk-architecture-visualizer.md) — The top-level C4 Model diagram widget that uses SvelteFlow for rendering and ELK.js for automatic layout. It supports all four C4 levels (Context, Container, Component, Code) with drill-down navigation, pan/zoom, and a minimap.
- [C4 Group Node — System Boundary Container for SvelteFlow Parent Nodes](./c4-group-node-system-boundary-container-for-svelteflow-parent-nodes.md) — A SvelteFlow parent/group node that renders a C4 system boundary as a dashed border container. It acts as the visual wrapper for nested container nodes, providing the "system boundary box" pattern from C4 architecture diagrams.
- [Callout: Contextual Highlight Block with Semantic Variants](./callout-contextual-highlight-block-with-semantic-variants.md) — Callout renders a left-bordered highlight block with an icon badge, supporting four semantic variants — info, success, warning, and insight — each with distinct color theming. It exists to draw reader attention to important conclusions, caveats, or findings within AI-generated research content.
- [Card Action — Grid-Positioned Action Slot in Card Header](./card-action-grid-positioned-action-slot-in-card-header.md) — CardAction is a specialised layout sub-component that places action controls (buttons, menus) in the top-right corner of a card header using CSS grid positioning. It uses explicit grid coordinates to float above card title and description content without affecting their flow.
- [Card Content — Main Body Region with Size-Responsive Padding](./card-content-main-body-region-with-size-responsive-padding.md) — CardContent is the main body slot of a card, providing horizontal padding that tightens automatically when the parent card is in its small size variant. It enforces consistent internal spacing without requiring consumers to manage padding manually.
- [Card Footer — Styled Bottom Section of a Card](./card-footer-styled-bottom-section-of-a-card.md) — CardFooter is the bottom-zone wrapper inside a card layout. It applies a muted background, rounded bottom corners, a top border, and responsive padding that automatically adjusts when the parent card is in `size=sm` mode.
- [Card Header — Adaptive Grid Layout for Card Titles and Actions](./card-header-adaptive-grid-layout-for-card-titles-and-actions.md) — CardHeader is the top section of a card component. It uses CSS Grid with container queries and group-data selectors to dynamically adapt its layout based on which child slots (title, description, action) are present.
- [Card Layout Widget](./card-layout-widget.md) — A versatile card container widget with five visual variants, two density modes, and optional interactive behavior. It dynamically renders as either a `<div>` or a `<button>` depending on whether an `onclick` handler is provided alongside `interactive: true`, ensuring semantic HTML and full keyboard accessibility.
- [Card — Root Container with Size Variants and Image-Aware Layout](./card-root-container-with-size-variants-and-image-aware-layout.md) — The root Card component is the orchestrating container for the card system. It exposes a `size` prop that propagates layout hints to all descendant sub-components via `data-size`, and it uses CSS selectors to handle image children, footer presence, and ring styling without any JavaScript.
- [Card Showcase — Visual QA for Card Variants and Composition](./card-showcase-visual-qa-for-card-variants-and-composition.md) — A dedicated visual QA page for the Ripple Card widget, covering all four visual variants, two density modes, interactive state, header/footer snippet slots, and a full composition example that embeds a Stat widget.
- [CompanyHeader: Company Identity and Live Price Block](./companyheader-company-identity-and-live-price-block.md) — CompanyHeader renders a full company identity card combining logo (with Clearbit auto-fetch and initial fallback), name, ticker, exchange badge, description, sector tags, live price, and price change — all from a single composable widget. It is the canonical top-of-widget header for stock and company research responses.
- [Container Layout Widget](./container-layout-widget.md) — A minimal transparent wrapper widget that renders a plain `<div>` with optional id, class, style, onclick, and children. It exists as the simplest possible Ripple layout primitive — a semantic grouping container with no visual opinions.
- [Dashboard Auto-Fill Grid Widget (Legacy)](./dashboard-auto-fill-grid-widget-legacy.md) — A legacy CSS grid layout widget that automatically fills columns based on a minimum width using `repeat(auto-fill, minmax(...))`. This component is the static, non-draggable predecessor to the Muuri-based `DashboardRenderer` and is retained for simpler use cases that do not need interactive reordering.
- [Dashboard Grid Slot Widget](./dashboard-grid-slot-widget.md) — A grid cell wrapper used inside the `Dashboard` layout widget that controls column span and prevents child overflow from breaking the grid. Each slot carries identity metadata (`slotId`, `itemId`) intended for future drag-and-drop or serialization use.
- [DashboardRenderer — Muuri-Powered Drag-and-Drop Dashboard Layout](./dashboardrenderer-muuri-powered-drag-and-drop-dashboard-layout.md) — DashboardRenderer renders `intent='dashboard'` specs as an interactive grid using Muuri for drag-and-drop, dynamic resizing, and auto-packing. It bridges the declarative `DashboardSpec` widget list with Muuri's DOM-level layout engine, handling widget normalization, multi-format data coercion, and per-widget controls.
- [Dialog Content — Animated Modal Panel with Portal and Optional Close Button](./dialog-content-animated-modal-panel-with-portal-and-optional-close-button.md) — DialogContent assembles the visible dialog panel. It wraps children in a portal (for z-index and stacking context isolation), renders the background overlay, and optionally includes an `X` close button in the top-right corner. Enter/exit animations are driven by bits-ui's `data-open`/`data-closed` attributes.
- [Dialog Footer — Action Bar with Optional Close Button](./dialog-footer-action-bar-with-optional-close-button.md) — Renders the bottom action area of a dialog, laying out child elements (typically buttons) in a responsive row. Optionally injects a Close button wired to the bits-ui dialog primitive for zero-configuration dismiss behavior.
- [Dialog Header — Title and Description Container](./dialog-header-title-and-description-container.md) — A minimal layout wrapper that vertically stacks title and description elements at the top of a dialog panel. It provides consistent vertical spacing and a `data-slot` marker for structural targeting.
- [Dialog Portal — DOM Teleportation Wrapper](./dialog-portal-dom-teleportation-wrapper.md) — A one-line pass-through component that delegates to bits-ui's `DialogPrimitive.Portal`, teleporting its children out of the current DOM tree and into the document body. This prevents z-index conflicts caused by stacking contexts in ancestor elements.
- [Each — Declarative List Iteration Control Widget](./each-declarative-list-iteration-control-widget.md) — A minimal control-flow wrapper that exposes Svelte's native `{#each}` iteration pattern as a named widget usable in Ripple's schema-driven UI. It accepts an `items` prop (used by the schema layer) and a `children` snippet that it unconditionally renders, delegating actual iteration to the caller.
- [ELK-Based Auto-Layout Engine for C4 Diagrams](./elk-based-auto-layout-engine-for-c4-diagrams.md) — Converts a `C4Diagram` data structure into ELK graph nodes and edges, runs the ELK layered layout algorithm, and returns a flat `Map<id, LayoutPosition>` of computed positions. Replaced an earlier grid-based layout to provide professional, collision-free hierarchical placement.
- [Flex Layout Widget](./flex-layout-widget.md) — A flexbox layout container widget that maps semantic prop names (`justify`, `align`, `direction`, `gap`, `wrap`) to CSS flexbox values via lookup tables, eliminating the need for callers to know CSS property names. It also supports `divided` and `compact` layout variants that apply separator borders or tight padding to direct children.
- [GlassCard Glassmorphism Container Widget](./glasscard-glassmorphism-container-widget.md) — A specialized card widget that renders a frosted-glass container using backdrop-filter, color-mix, and multi-layer box-shadow. Extracted from the paw-os-ui enterprise layout system, it exposes fine-grained glass effect controls (`opacity`, `blur`, `tint`, `borderGlow`) as props.
- [Grid Layout Widget](./grid-layout-widget.md) — A CSS grid layout widget that maps numeric column/row counts or raw CSS template strings to `grid-template-columns` and `grid-template-rows`, with optional gap normalization using the 4px grid system. It is the explicit-structure counterpart to `Dashboard`'s auto-fill approach.
- [If — Declarative Conditional Rendering Control Widget](./if-declarative-conditional-rendering-control-widget.md) — A minimal control-flow widget that wraps Svelte's `{#if}` block as a named, schema-addressable component, enabling conditional rendering in Ripple's JSON spec format. When `condition` is false (or a spec expression resolves falsy), the children snippet is suppressed entirely.
- [KvTable: Key-Value Data Grid with Single or Two-Column Layout](./kvtable-key-value-data-grid-with-single-or-two-column-layout.md) — KvTable renders an array of key-value pairs in a bordered, optionally-striped grid that can be displayed in one or two columns — with automatic midpoint splitting for the two-column case. It exists to present dense structured data (financial metrics, specs, metadata) in a compact tabular layout without requiring a full HTML table.
- [Layout Engine — Intent-to-Layout Decision System](./layout-engine-intent-to-layout-decision-system.md) — The layout engine translates a UniversalSpec's intent, data shape, and display hints into a concrete LayoutType string that drives which Svelte component renders. It acts as the 'smart' middle layer so AI-generated specs don't need to hard-code visual layouts — instead they declare intent and the engine picks the best fit.
- [Layout Widgets Barrel Export](./layout-widgets-barrel-export.md) — The barrel export for Ripple's layout widget family, re-exporting nine layout primitives from a single entry point. Phase 4 additions (`GlassCard`, `Modal`) are noted in the file comment, establishing a versioned changelog for the layout surface.
- [Modal Dialog Widget](./modal-dialog-widget.md) — A controlled modal dialog widget wrapping the shadcn Dialog primitive, with support for both prop-bound state and flow `invoke` actions via the `WidgetRegistry`. It synchronizes open state bidirectionally — accepting `value` from the outside and emitting `onchange(false)` when dismissed — so the UI state manager stays consistent regardless of how the modal is closed.
- [Popover Content — Animated Floating Panel with Portal Integration](./popover-content-animated-floating-panel-with-portal-integration.md) — Renders the floating panel of a popover inside a portal, with entrance/exit animations keyed to open/close state and configurable positioning. Automatically wraps content in `PopoverPortal` to escape ancestor stacking contexts.
- [Popover Header — Compact Title and Description Stack](./popover-header-compact-title-and-description-stack.md) — A lightweight layout wrapper that groups title and description elements inside a popover with tight vertical spacing. Mirrors the structure of `DialogHeader` but uses a smaller gap and text size appropriate for the popover's more compact panel format.
- [Popover Portal — DOM Escape Hatch for Overlay Rendering](./popover-portal-dom-escape-hatch-for-overlay-rendering.md) — A thin wrapper around the bits-ui PopoverPrimitive.Portal that teleports popover content outside its natural DOM position, preventing stacking context and overflow clipping issues. It passes all props through verbatim, keeping the surface area minimal while delegating all portal logic to the headless primitive.
- [Reorderable — Drag-to-Reorder Svelte Action](./reorderable-drag-to-reorder-svelte-action.md) — The `reorderable` Svelte action attaches zero-dependency pointer-event drag-and-drop reordering to any container element, supporting grid, masonry, and flex layouts. It uses a ghost element for visual drag feedback, a configurable dead zone to prevent accidental drags, and nearest-edge drop detection with a maximum proximity threshold.
- [Root Layout — Shell, Navigation, and Theme Toggle](./root-layout-shell-navigation-and-theme-toggle.md) — The SvelteKit root layout component that wraps every page in a persistent navigation shell, injects global styles and the SVG favicon, and wires up a reactive dark-mode toggle that reads the OS preference on first load.
- [Select Group — Semantic Container for Grouped Options](./select-group-semantic-container-for-grouped-options.md) — A minimal wrapper around bits-ui's `SelectPrimitive.Group` that applies consistent internal padding and scroll margins, acting as the ARIA grouping container that associates a heading label with its child options. It renders no visual chrome beyond spacing.
- [Select Portal — DOM Escape Hatch for Dropdown Menus](./select-portal-dom-escape-hatch-for-dropdown-menus.md) — A thin wrapper around bits-ui's `Select.Portal` that teleports the select dropdown out of its DOM parent and into the document body. This prevents clipping caused by `overflow: hidden` or `z-index` stacking contexts on ancestor elements.
- [Select Separator — Visual Divider Between Option Groups](./select-separator-visual-divider-between-option-groups.md) — A horizontal rule that visually segments groups of options inside a select dropdown. It reuses the shared Separator primitive with select-specific margins so dividers align flush with the popover edges.
- [Separator Component — Accessible Horizontal and Vertical Divider](./separator-component-accessible-horizontal-and-vertical-divider.md) — A styled wrapper around bits-ui's `Separator.Root` that renders a 1px divider line in either horizontal or vertical orientation. It applies design-token colors and includes a deliberate deviation from the upstream shadcn/ui pattern to fix a self-stretch layout bug.
- [Separator Component Index — Dual Export for Flexible Imports](./separator-component-index-dual-export-for-flexible-imports.md) — The barrel export file for the Separator UI component that exposes the root implementation under both a plain `Root` alias and a semantic `Separator` alias. This dual-export pattern lets consumers choose between namespace-style and direct-import ergonomics.
- [Sheet Component Index — Barrel Export for Slide-Out Panel System](./sheet-component-index-barrel-export-for-slide-out-panel-system.md) — The barrel export for Ripple's Sheet (slide-out panel) component family. It exports all ten sub-components under both short primitive names and longer semantic `Sheet*` names, giving consumers the flexibility to import either style while maintaining a single source of truth.
- [Sheet Footer — Action Button Region at Panel Bottom](./sheet-footer-action-button-region-at-panel-bottom.md) — A plain div-based layout region that anchors to the bottom of the sheet panel with `mt-auto`, providing a consistent placement for action buttons like Submit, Cancel, or Save. It is a pure layout primitive with no bits-ui dependency.
- [Sheet Header — Title and Description Container Region](./sheet-header-title-and-description-container-region.md) — A simple `<div>` layout region that groups the sheet's title and description into a consistently padded top section. Like `SheetFooter`, it is a pure layout primitive with no bits-ui dependency, relying on semantic composition rather than ARIA wiring.
- [Sheet Portal — DOM Teleportation Wrapper](./sheet-portal-dom-teleportation-wrapper.md) — SheetPortal is a thin Svelte 5 wrapper around the bits-ui Dialog.Portal primitive that teleports sheet overlay content outside the normal DOM tree. By delegating all rendering to the underlying primitive, it ensures sheet content escapes stacking contexts and z-index traps without any additional logic.
- [Svelte Actions Barrel Export](./svelte-actions-barrel-export.md) — The `actions/index.ts` file is a barrel module that re-exports all Svelte actions from the actions directory as a single import surface. Currently it exports the `reorderable` action and its associated `ReorderableOptions` type.
- [Table Body — Data Row Container with Last-Row Border Suppression](./table-body-data-row-container-with-last-row-border-suppression.md) — TableBody renders a styled `<tbody>` element that wraps the data rows of a table, applying a targeted CSS rule to remove the bottom border from the last row. This prevents a double-border artifact at the boundary between `<tbody>` and `<tfoot>` (or the table edge).
- [Table Cell — Data Cell with Checkbox Padding Adjustment](./table-cell-data-cell-with-checkbox-padding-adjustment.md) — TableCell renders a styled `<td>` element with consistent padding, vertical alignment, and a defensive CSS rule that removes right padding when the cell contains a checkbox. This prevents double-spacing in selection column cells without requiring conditional props or wrapper divs.
- [Table Footer — Summary Row Container with Subtle Background](./table-footer-summary-row-container-with-subtle-background.md) — TableFooter renders a styled `<tfoot>` element for housing summary or aggregate rows at the bottom of a table, applying a muted semi-transparent background and a top border while removing the bottom border from its last row to prevent double borders.
- [Table Header — Column Group Container with Row Border Rules](./table-header-column-group-container-with-row-border-rules.md) — TableHeader renders a styled `<thead>` element that groups all column header rows, applying a CSS rule that adds a bottom border to every `<tr>` it contains. This ensures header rows are always visually separated from the data body without requiring individual row styling.
- [Table Root Component](./table-root-component.md) — The root container for data tables, wrapping a native `<table>` in an overflow-safe scroll container. Applies consistent sizing and caption positioning while exposing a bindable DOM reference.
- [Table Row Component](./table-row-component.md) — A styled `<tr>` wrapper that provides hover, selection, and transition behaviors for table rows. Exposes a bindable ref and forwards all native HTML table row attributes.
- [Tabs Layout Widget](./tabs-layout-widget.md) — A tabbed panel widget wrapping shadcn Tabs that accepts tabs as either plain strings or `{value, label}` objects, normalizes them into a consistent shape, and manages active tab state with support for both internal control and external value binding. Each tab panel only renders its content when active, preventing hidden panels from running their child effects.
- [Tabs List Container with Variant Support](./tabs-list-container-with-variant-support.md) — The container that holds tab trigger buttons, supporting `default` (pill/background) and `line` (underline) visual variants. Uses `tailwind-variants` for variant composition and adapts layout for both horizontal and vertical orientations.
- [Test Suite for C4 ELK Layout Utilities](./test-suite-for-c4-elk-layout-utilities.md) — Vitest unit tests for `elk-layout.ts`, covering the `getNodeType`, `isGroupNode`, and `computeElkLayout` functions. Tests verify correct C4 element classification and that ELK produces valid node positions for realistic diagram fixtures.
- [Test Suite for Card Layout Widget](./test-suite-for-card-layout-widget.md) — Vitest + Testing Library test suite covering the Card widget's rendering, structural conditionals, variant/density data attributes, interactive button behavior, and ARIA semantics. The suite validates both the happy path and defensive edge cases like non-interactive cards with onclick handlers.
- [Test Suite for NodeRenderer Named Slot Routing](./test-suite-for-noderenderer-named-slot-routing.md) — This test suite verifies that `NodeRenderer` correctly routes child nodes to named slots (`header`, `footer`) or the default body slot based on each child's optional `slot` field. It covers default routing, named-slot placement, multi-child same-slot stacking, unknown slot silencing, and regression-guards for specs written before slots were introduced.
- [Test Suite: Layout Engine — determineLayout and analyzeData](./test-suite-layout-engine-determinelayout-and-analyzedata.md) — Unit tests for the layout engine's two public functions: `determineLayout` (maps a UniversalSpec intent and data shape to a layout string) and `analyzeData` (extracts item count and field names from a spec's data object). Tests cover all intent types, display hint overrides, browse sub-layouts, and data analysis edge cases.
- [Timeline: Chronological Event List with Color-Coded Dots](./timeline-chronological-event-list-with-color-coded-dots.md) — Timeline renders a vertical list of dated events connected by a continuous rail line — each event has a colored dot, date label, title, and optional detail text. It supports five semantic dot types plus custom color overrides, truncation via maxItems, and a trailing overflow indicator.
- [Tooltip Portal Wrapper Component](./tooltip-portal-wrapper-component.md) — A minimal pass-through wrapper around `bits-ui`'s `Tooltip.Portal`, enabling tooltip content to be rendered outside its DOM ancestor tree. Prevents clipping by `overflow: hidden` ancestors.
- [Workflow Widget — Interactive Node-Based Diagram](./workflow-widget-interactive-node-based-diagram.md) — A Svelte 5 widget that renders interactive workflow diagrams using SvelteFlow (`@xyflow/svelte`). It accepts a simplified `WorkflowNodeData[]` and `WorkflowEdgeData[]` spec and handles auto-layout, type mapping, and interactivity controls transparently.

### `loading-state` — 1 article(s)

- [Skeleton Loading Placeholder Widget](./skeleton-loading-placeholder-widget.md) — A lightweight shimmer placeholder shown by Ripple while a streaming spec has not yet produced a valid first parse. Provides four layout variants — card, dashboard, text, and none — so the UI can match the shape of the content it is waiting for.

### `media` — 1 article(s)

- [Avatar Image — Profile Picture Renderer with Load Tracking](./avatar-image-profile-picture-renderer-with-load-tracking.md) — AvatarImage renders the actual profile picture inside an avatar, delegating load-state tracking to bits-ui's headless primitive. It enforces circular cropping and aspect-ratio preservation so arbitrarily-sized source images always fit the circular frame correctly.

### `messaging` — 1 article(s)

- [C4 Queue Node — Parallelogram-Shaped Node for Message Brokers](./c4-queue-node-parallelogram-shaped-node-for-message-brokers.md) — A SvelteFlow custom node that renders C4 message queue and broker elements using an amber CSS parallelogram shape. Uses `clip-path` for the skewed visual, includes URL-sanitized KB links, and fires click handlers for element selection.

### `module` — 7 article(s)

- [C4 Node Components Barrel — Exports All SvelteFlow Custom Node Types](./c4-node-components-barrel-exports-all-svelteflow-custom-node-types.md) — The barrel file for the C4 node component directory, re-exporting all seven custom SvelteFlow node types under a single import path. This keeps `C4Diagram.svelte`'s node type registration clean and allows individual node components to be imported independently by consumers.
- [C4 Widget Public API Barrel — Exports for Component, Types, and Layout](./c4-widget-public-api-barrel-exports-for-component-types-and-layout.md) — The barrel export file for the C4 diagram widget subsystem. It re-exports the `C4Diagram` Svelte component, layout utilities, and all TypeScript type definitions from a single import path, establishing the public API surface of the C4 module.
- [Composite Widgets Barrel Export](./composite-widgets-barrel-export.md) — A single-line barrel file that re-exports all composite widgets from the `composite/` directory under a unified import path. Currently exports `Terminal`; the barrel pattern means consumers need only update their import path once when new composite widgets are added.
- [Control Widgets Barrel Export](./control-widgets-barrel-export.md) — Barrel file that aggregates Ripple's structural control-flow widgets — `If` and `Each` — under a single import path. These two widgets form the core of Ripple's declarative conditionals and list rendering in schema-driven UIs.
- [Data Widgets Barrel Export](./data-widgets-barrel-export.md) — Barrel file that re-exports Ripple's data-display widgets — `Table` and `Chart` — from a single import path. These two widgets cover the primary structured-data visualization needs in Ripple-generated UIs.
- [Input Widgets Barrel Export](./input-widgets-barrel-export.md) — The barrel export for Ripple's input widget family, re-exporting all six interactive input primitives from a single entry point. Consumers import from this index instead of individual file paths, keeping import sites stable as the internal file structure evolves.
- [Layout Widgets Barrel Export](./layout-widgets-barrel-export.md) — The barrel export for Ripple's layout widget family, re-exporting nine layout primitives from a single entry point. Phase 4 additions (`GlassCard`, `Modal`) are noted in the file comment, establishing a versioned changelog for the layout surface.

### `module-organization` — 10 article(s)

- [Display Widget Barrel Export](./display-widget-barrel-export.md) — The barrel export file for Ripple's display widget category, re-exporting all eleven display primitives through a single import path. This indirection layer lets consumers and the widget registry import from `./display/index.js` without tracking individual component file paths.
- [Progress Component Barrel — Public API Entry Point](./progress-component-barrel-public-api-entry-point.md) — The index module for the progress component exports the `Root` component under two names — `Root` and `Progress` — giving consumers the flexibility to use either a structural import style or a semantic one. It is the single entry point that downstream code should import from rather than reaching into the component directory directly.
- [Radio Group Barrel — Dual-Export Entry Point for Root and Item](./radio-group-barrel-dual-export-entry-point-for-root-and-item.md) — The index module for the radio group component exports both the `Root` container and `Item` option under structural and semantic names, enabling consumers to import as `{ RadioGroup, RadioGroupItem }` or `{ Root, Item }`. It establishes the two-component composition model that every radio group must follow.
- [Schema Package Barrel — Public Schema Exports](./schema-package-barrel-public-schema-exports.md) — The `lib/schema/index.ts` barrel re-exports every public type and validator from the four schema modules as a single import surface. Consumers import from `@ripple-ui/svelte/schema` (or equivalent) without needing to know which sub-file holds a given type.
- [Select Component Barrel — Full Composition API with 11 Sub-Components](./select-component-barrel-full-composition-api-with-11-sub-components.md) — The index module for the select component family exports all 11 sub-components under both structural (`Root`, `Item`, etc.) and semantic (`Select`, `SelectItem`, etc.) names. It defines the complete public API for the most compositionally complex form control in the Ripple library.
- [Separator Component Index — Dual Export for Flexible Imports](./separator-component-index-dual-export-for-flexible-imports.md) — The barrel export file for the Separator UI component that exposes the root implementation under both a plain `Root` alias and a semantic `Separator` alias. This dual-export pattern lets consumers choose between namespace-style and direct-import ergonomics.
- [Slider Index — Public API Entry Point](./slider-index-public-api-entry-point.md) — The slider index module re-exports the Slider component under both its canonical `Root` name and its human-readable `Slider` alias, following ripple's standard barrel file pattern. It acts as the single import surface for all downstream consumers of the slider widget.
- [Streaming Module Barrel — Isolated Entry Point for LLM Streaming](./streaming-module-barrel-isolated-entry-point-for-llm-streaming.md) — The streaming barrel exposes `streamSpec` and the public streaming types as a deliberately isolated sub-entry point (`@ripple-ui/svelte/streaming`). The isolation exists because this module pulls in `partial-json`, a dependency that should not be bundled into the base Ripple package for consumers who don't need streaming.
- [Switch Index — Public API Entry Point](./switch-index-public-api-entry-point.md) — The switch index module re-exports the Switch component under both its canonical `Root` name and its human-readable `Switch` alias, following ripple's standard barrel file pattern for all UI widget modules.
- [Table Index — Full Component Family Public API](./table-index-full-component-family-public-api.md) — The table index module assembles all eight table sub-components — Root, Body, Caption, Cell, Footer, Head, Header, Row — into a single importable surface with both short canonical names and full descriptive aliases. This barrel file is the only import consumers need for complete table functionality.

### `module-structure` — 1 article(s)

- [Overlay Widgets Barrel Export](./overlay-widgets-barrel-export.md) — The overlay barrel module centralizes all overlay-layer widgets — currently ConfirmDialog — behind a single import path. This pattern keeps widget consumers decoupled from internal file structure and makes tree-shaking predictable.

### `module-system` — 9 article(s)

- [Card Component Index — Dual-Name Export Barrel](./card-component-index-dual-name-export-barrel.md) — The card `index.ts` barrel file re-exports all seven card sub-components under both short semantic names (`Root`, `Header`, `Content`) and fully-qualified PascalCase names (`Card`, `CardHeader`, `CardContent`). This dual-export pattern supports both namespace-style and destructured imports.
- [Chart Component Index — Public API Barrel for Chart System](./chart-component-index-public-api-barrel-for-chart-system.md) — The chart `index.ts` barrel exports `ChartContainer` and `ChartTooltip` as the public surface of the chart system, alongside `ChartConfig` type and `getPayloadConfigFromPayload` utility for consumer-level payload handling.
- [Checkbox Component Index — Minimal Barrel with Dual-Name Export](./checkbox-component-index-minimal-barrel-with-dual-name-export.md) — The checkbox `index.ts` barrel re-exports the single `checkbox.svelte` component under both `Root` and `Checkbox` names. This follows the ripple dual-export convention, allowing both namespace-style and direct-import usage with minimal boilerplate.
- [Core Module Barrel — lib/core/index.ts](./core-module-barrel-libcoreindexts.md) — The `lib/core/index.ts` barrel re-exports all public symbols from Ripple's four core subsystems: StateManager, EventDispatcher, expression resolver utilities, and the spec normalizer. It provides a single stable import path for the core engine without exposing internal module structure.
- [Dialog Component Index — Dual-Name Export Surface](./dialog-component-index-dual-name-export-surface.md) — The public barrel file for the Ripple dialog system. It exports all ten sub-components under two naming conventions — short names (`Root`, `Title`, etc.) for destructured imports and prefixed names (`Dialog`, `DialogTitle`, etc.) for namespace imports — giving consumers flexibility in how they reference components.
- [Input Component Index — Single-Component Barrel with Dual Export](./input-component-index-single-component-barrel-with-dual-export.md) — The public entry point for Ripple's input component. Exports `input.svelte` under both a short name (`Root`) and a semantic alias (`Input`), following the same dual-export convention used across all Ripple UI component families.
- [Intent Module Barrel — lib/intent/index.ts](./intent-module-barrel-libintentindexts.md) — The `lib/intent/index.ts` barrel re-exports Ripple's intent-layer subsystems: the layout engine, pattern detector, chain executor, and dashboard manager. It provides a stable import boundary for the intent-resolution layer, which sits between raw spec parsing and concrete rendering.
- [Popover Component Index — Eight-Part Barrel with Dual-Name Exports](./popover-component-index-eight-part-barrel-with-dual-name-exports.md) — The public entry point for Ripple's popover system. Exports all eight sub-components under both short and prefixed naming conventions, mirroring the structure of the dialog index and enabling flexible, collision-free imports.
- [Ripple Package Entry Point — lib/index.ts](./ripple-package-entry-point-libindexts.md) — The top-level `lib/index.ts` is the public API surface of the Ripple package. It re-exports the main `Ripple` Svelte component, the full core engine (StateManager, EventDispatcher, expression resolver, normalizer), widget registry utilities, Zod-backed schemas, the `reorderable` action, and all public TypeScript types.

### `navigation` — 9 article(s)

- [C4 Container Node — SvelteFlow Custom Node with Drill-Down Support](./c4-container-node-svelteflow-custom-node-with-drill-down-support.md) — A SvelteFlow custom node that renders a C4 Container element (API servers, databases, queues, etc.) as a blue-700 box with technology label. Supports drill-down navigation from Container to Component level, and includes URL-sanitized knowledge base links.
- [C4 System Node — Top-Level Architecture Block with Drill-Down](./c4-system-node-top-level-architecture-block-with-drill-down.md) — A SvelteFlow custom node that renders a C4 System element as a rounded rectangle, with blue styling for internal systems and gray dashed borders for external ones. Supports multi-level drill-down from Context through Component, and carries reactive style bindings for all color properties.
- [ChainExecutor — Client-Side Intent Chain Navigation with History](./chainexecutor-client-side-intent-chain-navigation-with-history.md) — ChainExecutor manages multi-step intent navigation entirely on the client, enabling instant transitions between chained specs without AI roundtrips. It maintains a browser-like history stack (with forward navigation), tracks accumulated context across steps, and supports both linear chains and selection-based `chain_map` routing.
- [Tabs Component Public API Index](./tabs-component-public-api-index.md) — The barrel file for the Tabs component family, re-exporting all four sub-components plus variant utilities under both short and verbose names. Establishes the single import point consumers use to access the entire tabs system.
- [Tabs Content Panel Component](./tabs-content-panel-component.md) — The content panel for an individual tab, delegating visibility control to the `bits-ui` Tabs primitive. Adds consistent typography sizing, flex growth, and outline removal while forwarding all primitive props.
- [Tabs Layout Widget](./tabs-layout-widget.md) — A tabbed panel widget wrapping shadcn Tabs that accepts tabs as either plain strings or `{value, label}` objects, normalizes them into a consistent shape, and manages active tab state with support for both internal control and external value binding. Each tab panel only renders its content when active, preventing hidden panels from running their child effects.
- [Tabs List Container with Variant Support](./tabs-list-container-with-variant-support.md) — The container that holds tab trigger buttons, supporting `default` (pill/background) and `line` (underline) visual variants. Uses `tailwind-variants` for variant composition and adapts layout for both horizontal and vertical orientations.
- [Tabs Root Component](./tabs-root-component.md) — The root orchestrator for the tabs system, wrapping `bits-ui`'s `Tabs.Root` primitive with a bindable `value` prop and orientation-aware flex layout. Serves as the context provider for all child tab components.
- [Tabs Trigger Button Component](./tabs-trigger-button-component.md) — The clickable tab button that activates a content panel, with an extensive Tailwind class composition covering active state, focus visibility, dark mode, orientation adaptation, and an animated line-variant underline indicator.

### `news` — 1 article(s)

- [NewsCard: Scannable News Item Row with Sentiment Badge](./newscard-scannable-news-item-row-with-sentiment-badge.md) — NewsCard renders a news article as a list row with source favicon, publisher name, timestamp, optional sentiment badge (bullish/bearish/neutral), truncated headline, and thumbnail image. It is designed for dense news feed layouts where multiple items stack vertically, with separators between items and the last item having no bottom border.

### `normalization` — 1 article(s)

- [Spec Normalizer — UniversalSpec Input Coercion](./spec-normalizer-universalspec-input-coercion.md) — The normalizer transforms any raw spec input into a valid `UniversalSpec` object before rendering. It handles three input shapes: fully-formed `UniversalSpec` (pass-through), legacy `UISpec` format (has `ui` but no `intent`), and completely invalid input (returns a safe empty-container fallback).

### `overlay` — 22 article(s)

- [ConfirmDialog Overlay Widget](./confirmdialog-overlay-widget.md) — An auto-mounted confirmation dialog that renders when the event dispatcher writes a `PendingConfirm` object to `state._ripple_confirm`. On user decision, it calls `resolveConfirm` to resume the suspended flow, with a fallback that directly clears state if the resolver is stale — preventing flows from hanging indefinitely.
- [Modal Dialog Widget](./modal-dialog-widget.md) — A controlled modal dialog widget wrapping the shadcn Dialog primitive, with support for both prop-bound state and flow `invoke` actions via the `WidgetRegistry`. It synchronizes open state bidirectionally — accepting `value` from the outside and emitting `onchange(false)` when dismissed — so the UI state manager stays consistent regardless of how the modal is closed.
- [Overlay Widgets Barrel Export](./overlay-widgets-barrel-export.md) — The overlay barrel module centralizes all overlay-layer widgets — currently ConfirmDialog — behind a single import path. This pattern keeps widget consumers decoupled from internal file structure and makes tree-shaking predictable.
- [Popover Portal — DOM Escape Hatch for Overlay Rendering](./popover-portal-dom-escape-hatch-for-overlay-rendering.md) — A thin wrapper around the bits-ui PopoverPrimitive.Portal that teleports popover content outside its natural DOM position, preventing stacking context and overflow clipping issues. It passes all props through verbatim, keeping the surface area minimal while delegating all portal logic to the headless primitive.
- [Popover Root — State Container and Context Provider for Popovers](./popover-root-state-container-and-context-provider-for-popovers.md) — The root component of the popover composition, wrapping bits-ui's `Popover.Root` to expose a bindable `open` state that defaults to `false`. It acts as the invisible state container and context provider that coordinates all child components — trigger, content, portal — without rendering any DOM of its own.
- [Popover Title — Semantic Heading Slot for Popover Panels](./popover-title-semantic-heading-slot-for-popover-panels.md) — A styled `div` that renders as the title section inside a popover panel, applying medium font weight and exposing a bindable element reference for programmatic access. It uses the standard `cn` utility for class merging and the `WithElementRef` pattern to support imperative DOM operations.
- [Popover Trigger — Accessible Activation Element for Popovers](./popover-trigger-accessible-activation-element-for-popovers.md) — Wraps the bits-ui `Popover.Trigger` primitive to give it a `data-slot` identifier and class-merge support, while binding the DOM reference back to the caller. It is the interactive element users click or activate to open a popover, with all accessibility behavior — ARIA attributes, keyboard handling, focus management — delegated to the headless primitive.
- [Select Content — Animated Floating Panel with Scroll Controls](./select-content-animated-floating-panel-with-scroll-controls.md) — The floating content panel for the select dropdown, composing a portal, scroll buttons, a viewport, and animated entry/exit transitions. It defaults to preventing page scroll when open (`preventScroll = true`) and uses a 4px side offset from the trigger, with directional slide animations driven by `data-[side]` attributes.
- [Select Portal — DOM Escape Hatch for Dropdown Menus](./select-portal-dom-escape-hatch-for-dropdown-menus.md) — A thin wrapper around bits-ui's `Select.Portal` that teleports the select dropdown out of its DOM parent and into the document body. This prevents clipping caused by `overflow: hidden` or `z-index` stacking contexts on ancestor elements.
- [Sheet Close — Accessible Panel Dismiss Button](./sheet-close-accessible-panel-dismiss-button.md) — A minimal passthrough to bits-ui's `Dialog.Close` primitive that renders a dismiss button inside a sheet panel. It wires up the accessibility contract for closing the sheet via keyboard or click without requiring application-level event handling.
- [Sheet Component Index — Barrel Export for Slide-Out Panel System](./sheet-component-index-barrel-export-for-slide-out-panel-system.md) — The barrel export for Ripple's Sheet (slide-out panel) component family. It exports all ten sub-components under both short primitive names and longer semantic `Sheet*` names, giving consumers the flexibility to import either style while maintaining a single source of truth.
- [Sheet Content — Animated Side Panel with Side-Aware Layout](./sheet-content-animated-side-panel-with-side-aware-layout.md) — The core panel component of the Sheet system that renders a fixed-position drawer sliding in from any of the four viewport edges. It composes the portal, overlay, and close button into a single cohesive unit and applies side-aware entrance/exit animations via data attributes.
- [Sheet Overlay — Backdrop Scrim with Blur Effect](./sheet-overlay-backdrop-scrim-with-blur-effect.md) — Renders a semi-transparent backdrop behind an open sheet panel that blocks interaction with underlying page content and provides a subtle blur effect when the browser supports it. It is always rendered inside `SheetContent` and delegates close-on-click behavior to bits-ui's `Dialog.Overlay`.
- [Sheet Portal — DOM Teleportation Wrapper](./sheet-portal-dom-teleportation-wrapper.md) — SheetPortal is a thin Svelte 5 wrapper around the bits-ui Dialog.Portal primitive that teleports sheet overlay content outside the normal DOM tree. By delegating all rendering to the underlying primitive, it ensures sheet content escapes stacking contexts and z-index traps without any additional logic.
- [Sheet Root — Controlled Slide-in Panel State Manager](./sheet-root-controlled-slide-in-panel-state-manager.md) — Sheet is the root context provider for the sheet component family, delegating to bits-ui's Dialog.Root while exposing `open` as a bindable Svelte 5 prop for two-way state control. It is the entry point that all other Sheet sub-components depend on for shared dialog state.
- [Test Suite for ConfirmDialog Overlay Widget](./test-suite-for-confirmdialog-overlay-widget.md) — Vitest + Testing Library test suite for `ConfirmDialog`, covering state-driven mount/unmount, confirm and cancel button resolution, and the fallback path where `resolveConfirm` returns false (stale resolver after HMR). The `seed()` helper wires a real `StateManager`, `EventDispatcher`, and `WidgetRegistry` through Svelte context.
- [Tooltip Component Public API Index](./tooltip-component-public-api-index.md) — The barrel file for the Tooltip component family, re-exporting five sub-components — Root, Trigger, Content, Provider, and Portal — under both short and verbose names. Forms the single import surface for all tooltip functionality.
- [Tooltip Content Bubble with Arrow and Animations](./tooltip-content-bubble-with-arrow-and-animations.md) — The visible tooltip bubble component, composing portal rendering, directional entry/exit animations, keyboard shortcut badge support, and a custom arrow indicator. Delegates positioning and accessibility to `bits-ui`'s Tooltip.Content primitive.
- [Tooltip Portal Wrapper Component](./tooltip-portal-wrapper-component.md) — A minimal pass-through wrapper around `bits-ui`'s `Tooltip.Portal`, enabling tooltip content to be rendered outside its DOM ancestor tree. Prevents clipping by `overflow: hidden` ancestors.
- [Tooltip Provider Component](./tooltip-provider-component.md) — A shared configuration provider for tooltip groups, setting `delayDuration` to zero by default to deliver immediate tooltip responses. Wraps `bits-ui`'s Tooltip.Provider to override the library's built-in delay.
- [Tooltip Root Component](./tooltip-root-component.md) — The top-level orchestrator for a single tooltip instance, composing `TooltipProvider` and `bits-ui`'s `Tooltip.Root` with a bindable `open` state. Ensures every tooltip has provider context without requiring consumers to add it manually.
- [Tooltip Trigger Component](./tooltip-trigger-component.md) — A minimal wrapper around `bits-ui`'s `Tooltip.Trigger`, adding a `data-slot` identifier and exposing a bindable `ref`. Acts as the hover/focus target that activates the tooltip.

### `parsing` — 3 article(s)

- [Partial JSON Parser — Truncated Enum Guard for Progressive Streaming](./partial-json-parser-truncated-enum-guard-for-progressive-streaming.md) — This module wraps the `partial-json` library to add a critical safety layer: enum-typed fields (`type`, `intent`, `version`, `action`, `variant`) are stripped from parsed output unless their string value is provably closed in the raw buffer. Without this guard, progressive renders would briefly flash "Unknown widget type" errors as LLM output streams in character-by-character.
- [Test Suite for Stream Spec Simulation — LLM-Realistic Streaming Scenarios](./test-suite-for-stream-spec-simulation-llm-realistic-streaming-scenarios.md) — This test suite validates `streamSpec` and `parsePartialSpec` under realistic LLM-like streaming conditions using a deterministic pseudo-random chunk generator and a set of representative fixture documents. It enforces four invariants across all fixtures: final output correctness, monotonically non-decreasing node counts, no truncated enum values leaking through, and all widget types resolving to registered components.
- [Test Suite for streamSpec — Unit Tests for Lifecycle, Chunking, Options, and Safety](./test-suite-for-streamspec-unit-tests-for-lifecycle-chunking-options-and-safety.md) — This unit test suite provides systematic coverage of `streamSpec` across lifecycle edge cases, variable chunk sizes, configuration options, safety boundaries, and cross-environment compatibility. It uses controlled synthetic generators rather than real LLM output, giving precise control over timing and content to assert exact behavior.

### `pocket-spec` — 1 article(s)

- [Home Page — Pocket Gallery with Detail View](./home-page-pocket-gallery-with-detail-view.md) — The Ripple app's main landing page, which renders a gallery of interactive pocket demos (Research, Market Watch, Weather, and others) in an accordion layout and expands any selected pocket into a full detail view powered by the Ripple component.

### `popover` — 5 article(s)

- [Popover Close — Dismiss Button Wired to Popover State](./popover-close-dismiss-button-wired-to-popover-state.md) — A minimal wrapper around bits-ui's `PopoverPrimitive.Close` that renders a button capable of dismissing the parent popover. Exposes a bindable `ref` and forwards all props, with no custom logic beyond the data-slot marker.
- [Popover Component Index — Eight-Part Barrel with Dual-Name Exports](./popover-component-index-eight-part-barrel-with-dual-name-exports.md) — The public entry point for Ripple's popover system. Exports all eight sub-components under both short and prefixed naming conventions, mirroring the structure of the dialog index and enabling flexible, collision-free imports.
- [Popover Content — Animated Floating Panel with Portal Integration](./popover-content-animated-floating-panel-with-portal-integration.md) — Renders the floating panel of a popover inside a portal, with entrance/exit animations keyed to open/close state and configurable positioning. Automatically wraps content in `PopoverPortal` to escape ancestor stacking contexts.
- [Popover Description — Muted Supporting Text Block](./popover-description-muted-supporting-text-block.md) — A minimal layout component that renders supporting text inside a popover with muted color styling. It is a plain `<div>` wrapper — unlike its dialog counterpart, it does not use the bits-ui `Description` primitive and therefore does not set `aria-describedby` automatically.
- [Popover Header — Compact Title and Description Stack](./popover-header-compact-title-and-description-stack.md) — A lightweight layout wrapper that groups title and description elements inside a popover with tight vertical spacing. Mirrors the structure of `DialogHeader` but uses a smaller gap and text size appropriate for the popover's more compact panel format.

### `public-api` — 1 article(s)

- [Ripple Package Entry Point — lib/index.ts](./ripple-package-entry-point-libindexts.md) — The top-level `lib/index.ts` is the public API surface of the Ripple package. It re-exports the main `Ripple` Svelte component, the full core engine (StateManager, EventDispatcher, expression resolver, normalizer), widget registry utilities, Zod-backed schemas, the `reorderable` action, and all public TypeScript types.

### `reactivity` — 1 article(s)

- [StateManager — Reactive Widget State with Svelte 5 Runes](./statemanager-reactive-widget-state-with-svelte-5-runes.md) — StateManager is the reactive state container for a Ripple widget instance. It wraps a Svelte 5 `$state` object and exposes dot-notation path-based get, set, update, delete, and reset operations. Every mutation triggers Svelte's fine-grained reactivity, causing only the affected widget subtrees to re-render.

### `registry` — 2 article(s)

- [Test Suite: WidgetRegistry — Register, Invoke, and Unregister Round Trips](./test-suite-widgetregistry-register-invoke-and-unregister-round-trips.md) — Unit tests for the WidgetRegistry class, verifying method registration, argument forwarding, idempotent unregistration, replacement safety, and edge case handling. These tests were written alongside Phase B flow actions to ensure the `invoke` action has a well-behaved backing registry.
- [WidgetRegistry — Runtime Widget Method Registry for Invoke Actions](./widgetregistry-runtime-widget-method-registry-for-invoke-actions.md) — WidgetRegistry is a per-Ripple-instance registry that allows widget components to expose named methods so declarative `invoke` flow actions can call them programmatically. It uses a nested `Map<string, Map<string, WidgetMethod>>` structure keyed by widget ID and method name, with safe unregister semantics designed for Svelte 5 `$effect` cleanup.

### `rendering` — 7 article(s)

- [DashboardRenderer — Muuri-Powered Drag-and-Drop Dashboard Layout](./dashboardrenderer-muuri-powered-drag-and-drop-dashboard-layout.md) — DashboardRenderer renders `intent='dashboard'` specs as an interactive grid using Muuri for drag-and-drop, dynamic resizing, and auto-packing. It bridges the declarative `DashboardSpec` widget list with Muuri's DOM-level layout engine, handling widget normalization, multi-format data coercion, and per-widget controls.
- [Layout Engine — Intent-to-Layout Decision System](./layout-engine-intent-to-layout-decision-system.md) — The layout engine translates a UniversalSpec's intent, data shape, and display hints into a concrete LayoutType string that drives which Svelte component renders. It acts as the 'smart' middle layer so AI-generated specs don't need to hard-code visual layouts — instead they declare intent and the engine picks the best fit.
- [Pattern Detector — Quiz, Results, and Chart Data Transforms](./pattern-detector-quiz-results-and-chart-data-transforms.md) — The pattern detector identifies specialized data shapes — quizzes, results summaries, and chart data — from generic intent specs and normalizes them into typed structures that their respective renderer components expect. It was extracted from IntentRenderer to keep detection logic modular and independently testable.
- [streamSpec — Svelte 5 Reactive LLM Streaming Core](./streamspec-svelte-5-reactive-llm-streaming-core.md) — The `streamSpec` function is the runtime core of Ripple's streaming layer: it consumes a ReadableStream or AsyncIterable of string/binary chunks, progressively parses them as a partial JSON spec, and exposes the result as a Svelte 5 `$state`-backed reactive store. Components reading `store.current` automatically re-render as each new partial parse succeeds.
- [UISpec Schema — Gen 1 JSON UI Specification](./uispec-schema-gen-1-json-ui-specification.md) — UISpec is Ripple's original (Gen 1) schema for a complete JSON-described UI. It defines the recursive UINode tree, optional data fetchers and state, theme overrides, and the parse/safeParse entry points that validate LLM-generated output before it reaches the renderer.
- [UniversalSpec Schema — Gen 2 Intent-Based UI Specification](./universalspec-schema-gen-2-intent-based-ui-specification.md) — UniversalSpec is Ripple's second-generation schema that replaces explicit component trees with high-level intent declarations. The layout engine and pattern detector consume a UniversalSpec to auto-select layouts and data renderers, while `normalizeSpec` provides a lossless migration path from Gen 1 UISpec documents.
- [Widget Type Registry — Enum and Category Map](./widget-type-registry-enum-and-category-map.md) — This module defines the `WidgetType` Zod enum (the complete set of string values the `type` field of a UINode may hold) and the `WIDGET_CATEGORIES` constant that organizes those types into named groups used by documentation, tooling, and the layout engine.

### `research` — 15 article(s)

- [AnalystBar: Analyst Rating Distribution Visualization](./analystbar-analyst-rating-distribution-visualization.md) — AnalystBar renders a proportional color-segmented bar showing the breakdown of analyst ratings (Buy, Hold, Sell) for a financial instrument, along with consensus label and target price. It uses Svelte 5 `$derived` reactivity to compute segment widths from raw counts, gracefully handling the zero-total edge case.
- [Callout: Contextual Highlight Block with Semantic Variants](./callout-contextual-highlight-block-with-semantic-variants.md) — Callout renders a left-bordered highlight block with an icon badge, supporting four semantic variants — info, success, warning, and insight — each with distinct color theming. It exists to draw reader attention to important conclusions, caveats, or findings within AI-generated research content.
- [Citation: Inline Source Reference Chip with Favicon](./citation-inline-source-reference-chip-with-favicon.md) — Citation renders a compact inline chip showing a source name, its favicon, and an optional superscript citation number — behaving as a clickable link when a URL or click handler is provided. Favicon loading failures are handled gracefully with a colored dot fallback, keeping the component visually consistent even when icon fetching fails.
- [CompanyHeader: Company Identity and Live Price Block](./companyheader-company-identity-and-live-price-block.md) — CompanyHeader renders a full company identity card combining logo (with Clearbit auto-fetch and initial fallback), name, ticker, exchange badge, description, sector tags, live price, and price change — all from a single composable widget. It is the canonical top-of-widget header for stock and company research responses.
- [DiscoverCard: Media Card for Discoverable Content Links](./discovercard-media-card-for-discoverable-content-links.md) — DiscoverCard renders a compact card with a 16:10 thumbnail image, title, description, and source label — designed to be placed in horizontal scroll strips to surface recommended articles, topics, or related content. It activates as a button when a URL or click handler is provided.
- [favicon.ts: Source-to-Favicon URL Resolver](./faviconts-source-to-favicon-url-resolver.md) — The `favicon.ts` utility exports a single `faviconUrl()` function that maps a source name string to a Google Favicon Service URL. It maintains a hand-curated domain map for well-known publishers (particularly Indian financial and news sources) and falls back to heuristic domain inference for unknown sources.
- [FollowUp: Inline Question Input for Conversational Research](./followup-inline-question-input-for-conversational-research.md) — FollowUp renders a pill-shaped text input with a send button that emits follow-up queries back into the Ripple event system — bridging widget-level user input with the runtime's event dispatcher. It implements empty-string guarding, Enter-key submission, and graceful degradation when the event system context is absent.
- [KvTable: Key-Value Data Grid with Single or Two-Column Layout](./kvtable-key-value-data-grid-with-single-or-two-column-layout.md) — KvTable renders an array of key-value pairs in a bordered, optionally-striped grid that can be displayed in one or two columns — with automatic midpoint splitting for the two-column case. It exists to present dense structured data (financial metrics, specs, metadata) in a compact tabular layout without requiring a full HTML table.
- [NewsCard: Scannable News Item Row with Sentiment Badge](./newscard-scannable-news-item-row-with-sentiment-badge.md) — NewsCard renders a news article as a list row with source favicon, publisher name, timestamp, optional sentiment badge (bullish/bearish/neutral), truncated headline, and thumbnail image. It is designed for dense news feed layouts where multiple items stack vertically, with separators between items and the last item having no bottom border.
- [RangeBar: Position Marker on a Min-Max Value Track](./rangebar-position-marker-on-a-min-max-value-track.md) — RangeBar renders a labeled track with a colored fill and a circular position marker showing where a current value falls within a min-max range — commonly used for 52-week price ranges, analyst target ranges, or any metric with natural bounds. It clamps the marker position defensively and falls back to the midpoint when min equals max.
- [Research Widget Barrel Export](./research-widget-barrel-export.md) — Central re-export index for all research-domain UI widgets in the Ripple library. Groups thirteen Svelte components under a single import path so consumers never depend on internal file layout.
- [SourceCard: Compact Source Attribution Card with Headline](./sourcecard-compact-source-attribution-card-with-headline.md) — SourceCard is a small card that pairs a favicon (with dot fallback) and source name with a headline — designed for horizontal citation strips where sources are surfaced as individual tappable cards. It follows the same favicon-error-fallback and conditional-interactivity patterns as Citation and NewsCard.
- [SourcesBar: Aggregated Sources Summary Bar with Copy and Share](./sourcesbar-aggregated-sources-summary-bar-with-copy-and-share.md) — SourcesBar renders a compact row showing stacked source favicons and a count label alongside copy and share action buttons — serving as a persistent summary of all sources cited in a research response. It integrates with the Ripple event system to dispatch toast notifications and supports the Web Share API with clipboard fallback.
- [Ticker: Stock Ticker Strip with Symbol, Price, and Change](./ticker-stock-ticker-strip-with-symbol-price-and-change.md) — Ticker renders one or more stock quotes in a horizontally-scrollable pill strip — each showing symbol, price, and color-coded change value with optional percentage. It uses string prefix detection to determine bullish/bearish coloring, matching how financial data APIs deliver pre-formatted change strings.
- [Timeline: Chronological Event List with Color-Coded Dots](./timeline-chronological-event-list-with-color-coded-dots.md) — Timeline renders a vertical list of dated events connected by a continuous rail line — each event has a colored dot, date label, title, and optional detail text. It supports five semantic dot types plus custom color overrides, truncation via maxItems, and a trailing overflow indicator.

### `routing` — 2 article(s)

- [Home Page — Pocket Gallery with Detail View](./home-page-pocket-gallery-with-detail-view.md) — The Ripple app's main landing page, which renders a gallery of interactive pocket demos (Research, Market Watch, Weather, and others) in an accordion layout and expands any selected pocket into a full detail view powered by the Ripple component.
- [Root Layout — Shell, Navigation, and Theme Toggle](./root-layout-shell-navigation-and-theme-toggle.md) — The SvelteKit root layout component that wraps every page in a persistent navigation shell, injects global styles and the SVG favicon, and wires up a reactive dark-mode toggle that reads the OS preference on first load.

### `runtime` — 2 article(s)

- [NodeRenderer — Recursive UI Tree Renderer](./noderenderer-recursive-ui-tree-renderer.md) — NodeRenderer.svelte is the recursive engine at the heart of Ripple's render pipeline, responsible for taking a single `UINode` and producing the correct widget component with resolved props, live bindings, event handlers, loop context, and named-slot children. It handles all control-flow node types (`if`, `each`) and falls back to a visible error for unknown widget types.
- [Ripple — Main Entry Point and Rendering Orchestrator](./ripple-main-entry-point-and-rendering-orchestrator.md) — Ripple.svelte is the top-level component that accepts a UI spec or a streaming spec store, bootstraps all core services (state manager, event dispatcher, widget registry), and routes rendering to the correct sub-renderer based on spec intent and stream state.

### `safety` — 3 article(s)

- [Partial JSON Parser — Truncated Enum Guard for Progressive Streaming](./partial-json-parser-truncated-enum-guard-for-progressive-streaming.md) — This module wraps the `partial-json` library to add a critical safety layer: enum-typed fields (`type`, `intent`, `version`, `action`, `variant`) are stripped from parsed output unless their string value is provably closed in the raw buffer. Without this guard, progressive renders would briefly flash "Unknown widget type" errors as LLM output streams in character-by-character.
- [Test Suite for Stream Spec Simulation — LLM-Realistic Streaming Scenarios](./test-suite-for-stream-spec-simulation-llm-realistic-streaming-scenarios.md) — This test suite validates `streamSpec` and `parsePartialSpec` under realistic LLM-like streaming conditions using a deterministic pseudo-random chunk generator and a set of representative fixture documents. It enforces four invariants across all fixtures: final output correctness, monotonically non-decreasing node counts, no truncated enum values leaking through, and all widget types resolving to registered components.
- [Test Suite for streamSpec — Unit Tests for Lifecycle, Chunking, Options, and Safety](./test-suite-for-streamspec-unit-tests-for-lifecycle-chunking-options-and-safety.md) — This unit test suite provides systematic coverage of `streamSpec` across lifecycle edge cases, variable chunk sizes, configuration options, safety boundaries, and cross-environment compatibility. It uses controlled synthetic generators rather than real LLM output, giving precise control over timing and content to assert exact behavior.

### `schema` — 9 article(s)

- [Event Handler Schema — Widget Interaction Action Types](./event-handler-schema-widget-interaction-action-types.md) — This module defines the complete Zod schema for every action a widget can fire in response to user interaction. It covers simple side-effect actions (set state, navigate, toast) through to async flow control (sequential flows, conditional branches, confirmation dialogs, and API calls with on_success/on_error chaining).
- [Layout Engine — Intent-to-Layout Decision System](./layout-engine-intent-to-layout-decision-system.md) — The layout engine translates a UniversalSpec's intent, data shape, and display hints into a concrete LayoutType string that drives which Svelte component renders. It acts as the 'smart' middle layer so AI-generated specs don't need to hard-code visual layouts — instead they declare intent and the engine picks the best fit.
- [RippleEvent and RippleEventResult — Host Integration Types](./rippleevent-and-rippleeventresult-host-integration-types.md) — These two types define the contract between Ripple's internal event dispatcher and the host application. `RippleEvent` carries the payload for every host-delegated action; `RippleEventResult` carries the host's response back to enable async continuation chains (on_success / on_error) for API actions.
- [Schema Package Barrel — Public Schema Exports](./schema-package-barrel-public-schema-exports.md) — The `lib/schema/index.ts` barrel re-exports every public type and validator from the four schema modules as a single import surface. Consumers import from `@ripple-ui/svelte/schema` (or equivalent) without needing to know which sub-file holds a given type.
- [Spec Normalizer — UniversalSpec Input Coercion](./spec-normalizer-universalspec-input-coercion.md) — The normalizer transforms any raw spec input into a valid `UniversalSpec` object before rendering. It handles three input shapes: fully-formed `UniversalSpec` (pass-through), legacy `UISpec` format (has `ui` but no `intent`), and completely invalid input (returns a safe empty-container fallback).
- [Streaming Types — StreamSpecStore, StreamParseError, and Options](./streaming-types-streamspecstore-streamparseerror-and-options.md) — This module defines the public TypeScript contract for the streaming layer: the reactive store interface, a structured error class with kind discrimination and last-valid-spec preservation, and the options bag that controls throttling, buffer limits, cancellation, and update callbacks.
- [UISpec Schema — Gen 1 JSON UI Specification](./uispec-schema-gen-1-json-ui-specification.md) — UISpec is Ripple's original (Gen 1) schema for a complete JSON-described UI. It defines the recursive UINode tree, optional data fetchers and state, theme overrides, and the parse/safeParse entry points that validate LLM-generated output before it reaches the renderer.
- [UniversalSpec Schema — Gen 2 Intent-Based UI Specification](./universalspec-schema-gen-2-intent-based-ui-specification.md) — UniversalSpec is Ripple's second-generation schema that replaces explicit component trees with high-level intent declarations. The layout engine and pattern detector consume a UniversalSpec to auto-select layouts and data renderers, while `normalizeSpec` provides a lossless migration path from Gen 1 UISpec documents.
- [Widget Type Registry — Enum and Category Map](./widget-type-registry-enum-and-category-map.md) — This module defines the `WidgetType` Zod enum (the complete set of string values the `type` field of a UINode may hold) and the `WIDGET_CATEGORIES` constant that organizes those types into named groups used by documentation, tooling, and the layout engine.

### `scroll` — 2 article(s)

- [Select Scroll Down Button — Overflow Navigation Indicator](./select-scroll-down-button-overflow-navigation-indicator.md) — Renders a chevron-down affordance anchored to the bottom of a select listbox when the list overflows its container. It signals to the user that more options exist below the visible area and provides a scroll target managed by bits-ui.
- [Select Scroll Up Button — Top Overflow Navigation Indicator](./select-scroll-up-button-top-overflow-navigation-indicator.md) — Renders a chevron-up affordance pinned to the top of a select listbox when hidden options exist above the visible scroll area. It is the symmetric counterpart to `SelectScrollDownButton`, completing the bidirectional scroll navigation pattern.

### `security` — 2 article(s)

- [C4 Component Node — SvelteFlow Custom Node for Service Components](./c4-component-node-svelteflow-custom-node-for-service-components.md) — A SvelteFlow custom node that renders a C4 Component element as a compact blue box with a four-square icon, subtype label, technology badge, and optional knowledge base link. URL sanitization prevents XSS via `javascript:` or `data:` scheme injection in the KB link.
- [URL Sanitizer — XSS-Safe Href Utility](./url-sanitizer-xss-safe-href-utility.md) — A focused security utility that strips dangerous URL schemes before they reach the DOM, preventing XSS attacks through javascript:, data:, and file: hrefs. Added as a blocker fix in PR #14 when externally-supplied Pocket spec URLs were found to flow directly into href attributes.

### `select` — 6 article(s)

- [Select Portal — DOM Escape Hatch for Dropdown Menus](./select-portal-dom-escape-hatch-for-dropdown-menus.md) — A thin wrapper around bits-ui's `Select.Portal` that teleports the select dropdown out of its DOM parent and into the document body. This prevents clipping caused by `overflow: hidden` or `z-index` stacking contexts on ancestor elements.
- [Select Root — Bindable State Entry Point for Dropdowns](./select-root-bindable-state-entry-point-for-dropdowns.md) — The top-level select component that owns `open` and `value` state as bindable props, enabling two-way data binding with parent components. It delegates all behavior to bits-ui's `Select.Root` while providing Svelte 5-idiomatic state management.
- [Select Scroll Down Button — Overflow Navigation Indicator](./select-scroll-down-button-overflow-navigation-indicator.md) — Renders a chevron-down affordance anchored to the bottom of a select listbox when the list overflows its container. It signals to the user that more options exist below the visible area and provides a scroll target managed by bits-ui.
- [Select Scroll Up Button — Top Overflow Navigation Indicator](./select-scroll-up-button-top-overflow-navigation-indicator.md) — Renders a chevron-up affordance pinned to the top of a select listbox when hidden options exist above the visible scroll area. It is the symmetric counterpart to `SelectScrollDownButton`, completing the bidirectional scroll navigation pattern.
- [Select Separator — Visual Divider Between Option Groups](./select-separator-visual-divider-between-option-groups.md) — A horizontal rule that visually segments groups of options inside a select dropdown. It reuses the shared Separator primitive with select-specific margins so dividers align flush with the popover edges.
- [Select Trigger — The Dropdown Open/Close Button](./select-trigger-the-dropdown-openclose-button.md) — The visible button element users click to open or close a select dropdown. It combines value display, size variants, validation states, and a chevron icon into a single polished interactive control that integrates with bits-ui's accessibility layer.

### `sheet` — 10 article(s)

- [Sheet Close — Accessible Panel Dismiss Button](./sheet-close-accessible-panel-dismiss-button.md) — A minimal passthrough to bits-ui's `Dialog.Close` primitive that renders a dismiss button inside a sheet panel. It wires up the accessibility contract for closing the sheet via keyboard or click without requiring application-level event handling.
- [Sheet Content — Animated Side Panel with Side-Aware Layout](./sheet-content-animated-side-panel-with-side-aware-layout.md) — The core panel component of the Sheet system that renders a fixed-position drawer sliding in from any of the four viewport edges. It composes the portal, overlay, and close button into a single cohesive unit and applies side-aware entrance/exit animations via data attributes.
- [Sheet Description — Accessible Panel Summary Text](./sheet-description-accessible-panel-summary-text.md) — Renders the descriptive subtitle text inside a sheet panel with muted styling. It maps to bits-ui's `Dialog.Description`, which wires `aria-describedby` between this element and the sheet content so screen readers can announce the panel's purpose.
- [Sheet Footer — Action Button Region at Panel Bottom](./sheet-footer-action-button-region-at-panel-bottom.md) — A plain div-based layout region that anchors to the bottom of the sheet panel with `mt-auto`, providing a consistent placement for action buttons like Submit, Cancel, or Save. It is a pure layout primitive with no bits-ui dependency.
- [Sheet Header — Title and Description Container Region](./sheet-header-title-and-description-container-region.md) — A simple `<div>` layout region that groups the sheet's title and description into a consistently padded top section. Like `SheetFooter`, it is a pure layout primitive with no bits-ui dependency, relying on semantic composition rather than ARIA wiring.
- [Sheet Overlay — Backdrop Scrim with Blur Effect](./sheet-overlay-backdrop-scrim-with-blur-effect.md) — Renders a semi-transparent backdrop behind an open sheet panel that blocks interaction with underlying page content and provides a subtle blur effect when the browser supports it. It is always rendered inside `SheetContent` and delegates close-on-click behavior to bits-ui's `Dialog.Overlay`.
- [Sheet Portal — DOM Teleportation Wrapper](./sheet-portal-dom-teleportation-wrapper.md) — SheetPortal is a thin Svelte 5 wrapper around the bits-ui Dialog.Portal primitive that teleports sheet overlay content outside the normal DOM tree. By delegating all rendering to the underlying primitive, it ensures sheet content escapes stacking contexts and z-index traps without any additional logic.
- [Sheet Root — Controlled Slide-in Panel State Manager](./sheet-root-controlled-slide-in-panel-state-manager.md) — Sheet is the root context provider for the sheet component family, delegating to bits-ui's Dialog.Root while exposing `open` as a bindable Svelte 5 prop for two-way state control. It is the entry point that all other Sheet sub-components depend on for shared dialog state.
- [Sheet Title — Accessible Heading for Slide-in Panels](./sheet-title-accessible-heading-for-slide-in-panels.md) — SheetTitle wraps the bits-ui Dialog.Title primitive to provide a consistently styled, accessible heading element for sheet panels. It merges design-system typography tokens with an optional `className` override and exposes a bindable `ref` for imperative DOM access.
- [Sheet Trigger — Open Control for Slide-in Panels](./sheet-trigger-open-control-for-slide-in-panels.md) — SheetTrigger wraps the bits-ui Dialog.Trigger primitive to provide a typed, ref-aware open control for sheet components. It delegates all open/close state management to the bits-ui context and exposes `data-slot` for consistent component targeting.

### `showcase` — 6 article(s)

- [Button Showcase — Visual QA for All Button Variants](./button-showcase-visual-qa-for-all-button-variants.md) — A dedicated visual QA page for the Ripple Button widget, rendering every combination of variant, size, icon slot, loading state, disabled state, and full-width layout to enable manual and automated visual regression testing.
- [Card Showcase — Visual QA for Card Variants and Composition](./card-showcase-visual-qa-for-card-variants-and-composition.md) — A dedicated visual QA page for the Ripple Card widget, covering all four visual variants, two density modes, interactive state, header/footer snippet slots, and a full composition example that embeds a Stat widget.
- [Flow Actions Showcase — Interactive Form Submit Demo](./flow-actions-showcase-interactive-form-submit-demo.md) — A live showcase page that exercises Ripple's flow action system through a form-submit scenario, demonstrating validate, branch, confirm, api, toast, delay, invoke, and on_success/on_error chaining against a simulated host with a side-panel event log.
- [Showcase Index — Full Widget Catalog](./showcase-index-full-widget-catalog.md) — The primary showcase page for the Ripple widget library, rendering every core widget type through inline pocket specs that are fed to the Ripple component, serving as both a visual regression reference and a live usage guide.
- [Spec Showcase — Card + Chart + Stat Composition Demo](./spec-showcase-card-chart-stat-composition-demo.md) — A showcase page that renders four analytics dashboard cards (revenue, latency, signups, error share) using the Ripple spec format, demonstrating how Chart, Stat, and Card compose together with slot injection to build production-style metric panels.
- [Stat Showcase — Visual QA for Metric Display Widget](./stat-showcase-visual-qa-for-metric-display-widget.md) — A comprehensive visual QA page for the Ripple Stat widget, covering all three sizes, five number formats, the full direction-semantics matrix, label/alignment options, and Card composition.

### `slider` — 2 article(s)

- [Slider Component — Accessible Range Input with Orientation Support](./slider-component-accessible-range-input-with-orientation-support.md) — The Slider component wraps bits-ui's SliderPrimitive.Root to render an accessible, styled range input supporting horizontal and vertical orientations, multiple thumbs, and two-way value binding. It composes a track, a filled range bar, and dynamically generated thumb elements using Svelte 5 snippets.
- [Slider Index — Public API Entry Point](./slider-index-public-api-entry-point.md) — The slider index module re-exports the Slider component under both its canonical `Root` name and its human-readable `Slider` alias, following ripple's standard barrel file pattern. It acts as the single import surface for all downstream consumers of the slider widget.

### `soul-protocol` — 1 article(s)

- [Soul Status Display Widget](./soul-status-display-widget.md) — A compact or expanded display widget that surfaces an AI agent's soul state — including name, role, mood, energy level, memory count, and last action. Designed for dashboards that need to show multiple agents at a glance, with an inline energy bar and status dot in compact mode and a full detail panel in expanded mode.

### `state-management` — 27 article(s)

- [Accordion Root Container](./accordion-root-container.md) — The Accordion root component wraps bits-ui's `Accordion.Root` primitive to provide the stateful container for a set of collapsible sections. It exposes bindable `value` and `ref` props, and lays items out in a vertical flex column.
- [Alert Root Component with Variant System](./alert-root-component-with-variant-system.md) — The Alert root component provides a styled notification container with a `default` and `destructive` variant system built with `tailwind-variants`. It uses CSS grid with data-slot selectors to automatically accommodate icon+text layouts and action button placements without requiring callers to specify layout props.
- [Avatar Fallback — Image Load Failure Display](./avatar-fallback-image-load-failure-display.md) — AvatarFallback is the graceful-degradation layer rendered when an avatar image fails to load or is unavailable. It wraps bits-ui's headless `AvatarPrimitive.Fallback` to inject Ripple's design tokens and size-responsive typography.
- [Avatar Root — Core Container with Size and Load State](./avatar-root-core-container-with-size-and-load-state.md) — Avatar is the root component in the avatar family, providing the circular container, size tier selection, load status binding, and the pseudo-element border overlay. It coordinates sizing context for child components (image, fallback, badge) and exposes loadingStatus as a bindable prop for parent consumption.
- [ChainExecutor — Client-Side Intent Chain Navigation with History](./chainexecutor-client-side-intent-chain-navigation-with-history.md) — ChainExecutor manages multi-step intent navigation entirely on the client, enabling instant transitions between chained specs without AI roundtrips. It maintains a browser-like history stack (with forward navigation), tracks accumulated context across steps, and supports both linear chains and selection-based `chain_map` routing.
- [Chart Utils — Config Types, Context Management, and Payload Resolution](./chart-utils-config-types-context-management-and-payload-resolution.md) — This utility module defines the `ChartConfig` type and the Svelte context pair (`setChartContext` / `useChart`) that share chart configuration across the component tree. It also exports `getPayloadConfigFromPayload`, a defensive resolver that extracts per-series config from LayerChart's polymorphic tooltip payload.
- [DashboardManager — Reactive Widget List State for Persistent Dashboards](./dashboardmanager-reactive-widget-list-state-for-persistent-dashboards.md) — DashboardManager is the reactive state container for a mutable dashboard widget list. It exposes a CRUD API (add, remove, update, move, reorder) plus an `onChange` subscription for notifying parent components when the spec changes, supporting persistence in platforms that save dashboard layouts across sessions.
- [Dialog Root — Open State Provider and Context Root](./dialog-root-open-state-provider-and-context-root.md) — The root component of the Ripple dialog system. It wraps bits-ui's `DialogPrimitive.Root` and provides a bindable `open` prop that allows parent components to externally control dialog visibility. All other dialog components must be rendered as descendants of this root.
- [Event Handler Schema — Widget Interaction Action Types](./event-handler-schema-widget-interaction-action-types.md) — This module defines the complete Zod schema for every action a widget can fire in response to user interaction. It covers simple side-effect actions (set state, navigate, toast) through to async flow control (sequential flows, conditional branches, confirmation dialogs, and API calls with on_success/on_error chaining).
- [EventDispatcher — Multistep Flow Action Execution Engine](./eventdispatcher-multistep-flow-action-execution-engine.md) — The EventDispatcher is Ripple's core runtime for executing declarative event handlers against a StateManager. It supports 12 action types including composite multi-step flows, async API chaining, conditional branching, user confirmation dialogs, validation guards, and direct widget method invocation.
- [NodeRenderer — Recursive UI Tree Renderer](./noderenderer-recursive-ui-tree-renderer.md) — NodeRenderer.svelte is the recursive engine at the heart of Ripple's render pipeline, responsible for taking a single `UINode` and producing the correct widget component with resolved props, live bindings, event handlers, loop context, and named-slot children. It handles all control-flow node types (`if`, `each`) and falls back to a visible error for unknown widget types.
- [Popover Root — State Container and Context Provider for Popovers](./popover-root-state-container-and-context-provider-for-popovers.md) — The root component of the popover composition, wrapping bits-ui's `Popover.Root` to expose a bindable `open` state that defaults to `false`. It acts as the invisible state container and context provider that coordinates all child components — trigger, content, portal — without rendering any DOM of its own.
- [Radio Group Root — Selection Container with Bindable Value](./radio-group-root-selection-container-with-bindable-value.md) — The root container for a radio group, wrapping bits-ui's `RadioGroup.Root` to expose a two-way bindable `value` prop defaulting to an empty string. It renders as a full-width CSS grid with 8px gaps and provides the selection context that child `RadioGroupItem` components read from.
- [Ripple — Main Entry Point and Rendering Orchestrator](./ripple-main-entry-point-and-rendering-orchestrator.md) — Ripple.svelte is the top-level component that accepts a UI spec or a streaming spec store, bootstraps all core services (state manager, event dispatcher, widget registry), and routes rendering to the correct sub-renderer based on spec intent and stream state.
- [Select Root — Bindable State Entry Point for Dropdowns](./select-root-bindable-state-entry-point-for-dropdowns.md) — The top-level select component that owns `open` and `value` state as bindable props, enabling two-way data binding with parent components. It delegates all behavior to bits-ui's `Select.Root` while providing Svelte 5-idiomatic state management.
- [Sheet Root — Controlled Slide-in Panel State Manager](./sheet-root-controlled-slide-in-panel-state-manager.md) — Sheet is the root context provider for the sheet component family, delegating to bits-ui's Dialog.Root while exposing `open` as a bindable Svelte 5 prop for two-way state control. It is the entry point that all other Sheet sub-components depend on for shared dialog state.
- [StateManager — Reactive Widget State with Svelte 5 Runes](./statemanager-reactive-widget-state-with-svelte-5-runes.md) — StateManager is the reactive state container for a Ripple widget instance. It wraps a Svelte 5 `$state` object and exposes dot-notation path-based get, set, update, delete, and reset operations. Every mutation triggers Svelte's fine-grained reactivity, causing only the affected widget subtrees to re-render.
- [Streaming Types — StreamSpecStore, StreamParseError, and Options](./streaming-types-streamspecstore-streamparseerror-and-options.md) — This module defines the public TypeScript contract for the streaming layer: the reactive store interface, a structured error class with kind discrimination and last-valid-spec preservation, and the options bag that controls throttling, buffer limits, cancellation, and update callbacks.
- [streamSpec — Svelte 5 Reactive LLM Streaming Core](./streamspec-svelte-5-reactive-llm-streaming-core.md) — The `streamSpec` function is the runtime core of Ripple's streaming layer: it consumes a ReadableStream or AsyncIterable of string/binary chunks, progressively parses them as a partial JSON spec, and exposes the result as a Svelte 5 `$state`-backed reactive store. Components reading `store.current` automatically re-render as each new partial parse succeeds.
- [Tabs Component Public API Index](./tabs-component-public-api-index.md) — The barrel file for the Tabs component family, re-exporting all four sub-components plus variant utilities under both short and verbose names. Establishes the single import point consumers use to access the entire tabs system.
- [Tabs Root Component](./tabs-root-component.md) — The root orchestrator for the tabs system, wrapping `bits-ui`'s `Tabs.Root` primitive with a bindable `value` prop and orientation-aware flex layout. Serves as the context provider for all child tab components.
- [Test Suite: DashboardManager — Widget CRUD, Reorder, and Change Events](./test-suite-dashboardmanager-widget-crud-reorder-and-change-events.md) — Comprehensive unit tests for DashboardManager covering widget add, remove, update, move, reorder, bulk load, ID generation, and the onChange subscription system. Tests verify that mutations produce correct state and reliably fire change callbacks, while load() deliberately does not trigger callbacks.
- [Tooltip Provider Component](./tooltip-provider-component.md) — A shared configuration provider for tooltip groups, setting `delayDuration` to zero by default to deliver immediate tooltip responses. Wraps `bits-ui`'s Tooltip.Provider to override the library's built-in delay.
- [Tooltip Root Component](./tooltip-root-component.md) — The top-level orchestrator for a single tooltip instance, composing `TooltipProvider` and `bits-ui`'s `Tooltip.Root` with a bindable `open` state. Ensures every tooltip has provider context without requiring consumers to add it manually.
- [UISpec Schema — Gen 1 JSON UI Specification](./uispec-schema-gen-1-json-ui-specification.md) — UISpec is Ripple's original (Gen 1) schema for a complete JSON-described UI. It defines the recursive UINode tree, optional data fetchers and state, theme overrides, and the parse/safeParse entry points that validate LLM-generated output before it reaches the renderer.
- [UniversalSpec Schema — Gen 2 Intent-Based UI Specification](./universalspec-schema-gen-2-intent-based-ui-specification.md) — UniversalSpec is Ripple's second-generation schema that replaces explicit component trees with high-level intent declarations. The layout engine and pattern detector consume a UniversalSpec to auto-select layouts and data renderers, while `normalizeSpec` provides a lossless migration path from Gen 1 UISpec documents.
- [Widget Registry — Central Component Map and Registration API](./widget-registry-central-component-map-and-registration-api.md) — The top-level widget registry for Ripple's generative UI runtime. It maintains a mutable map of string type names to Svelte components, provides a CRUD API for runtime registration, and serves as the single import point for all 40+ built-in widgets across layout, display, input, data, control, composite, overlay, research, workflow, and C4 categories.

### `storage` — 1 article(s)

- [C4 Database Node — Cylinder-Shaped SvelteFlow Node for Storage Elements](./c4-database-node-cylinder-shaped-svelteflow-node-for-storage-elements.md) — A SvelteFlow custom node that renders a C4 database container using a CSS-composed cylinder shape (SVG ellipse cap + rectangular body). Visually distinct from other node types with a purple color scheme, and includes URL-sanitized KB links.

### `streaming` — 8 article(s)

- [Partial JSON Parser — Truncated Enum Guard for Progressive Streaming](./partial-json-parser-truncated-enum-guard-for-progressive-streaming.md) — This module wraps the `partial-json` library to add a critical safety layer: enum-typed fields (`type`, `intent`, `version`, `action`, `variant`) are stripped from parsed output unless their string value is provably closed in the raw buffer. Without this guard, progressive renders would briefly flash "Unknown widget type" errors as LLM output streams in character-by-character.
- [Skeleton Loading Placeholder Widget](./skeleton-loading-placeholder-widget.md) — A lightweight shimmer placeholder shown by Ripple while a streaming spec has not yet produced a valid first parse. Provides four layout variants — card, dashboard, text, and none — so the UI can match the shape of the content it is waiting for.
- [Streaming Module Barrel — Isolated Entry Point for LLM Streaming](./streaming-module-barrel-isolated-entry-point-for-llm-streaming.md) — The streaming barrel exposes `streamSpec` and the public streaming types as a deliberately isolated sub-entry point (`@ripple-ui/svelte/streaming`). The isolation exists because this module pulls in `partial-json`, a dependency that should not be bundled into the base Ripple package for consumers who don't need streaming.
- [Streaming Types — StreamSpecStore, StreamParseError, and Options](./streaming-types-streamspecstore-streamparseerror-and-options.md) — This module defines the public TypeScript contract for the streaming layer: the reactive store interface, a structured error class with kind discrimination and last-valid-spec preservation, and the options bag that controls throttling, buffer limits, cancellation, and update callbacks.
- [streamSpec — Svelte 5 Reactive LLM Streaming Core](./streamspec-svelte-5-reactive-llm-streaming-core.md) — The `streamSpec` function is the runtime core of Ripple's streaming layer: it consumes a ReadableStream or AsyncIterable of string/binary chunks, progressively parses them as a partial JSON spec, and exposes the result as a Svelte 5 `$state`-backed reactive store. Components reading `store.current` automatically re-render as each new partial parse succeeds.
- [Test Suite for Stream Spec Simulation — LLM-Realistic Streaming Scenarios](./test-suite-for-stream-spec-simulation-llm-realistic-streaming-scenarios.md) — This test suite validates `streamSpec` and `parsePartialSpec` under realistic LLM-like streaming conditions using a deterministic pseudo-random chunk generator and a set of representative fixture documents. It enforces four invariants across all fixtures: final output correctness, monotonically non-decreasing node counts, no truncated enum values leaking through, and all widget types resolving to registered components.
- [Test Suite for Stream Spec Smoke Tests — Module Integrity and Basic Lifecycle](./test-suite-for-stream-spec-smoke-tests-module-integrity-and-basic-lifecycle.md) — The smoke tests are the first gate in the streaming test pipeline: they verify that the module is importable, exported types exist, the widget registry is intact, and the `streamSpec` store handles trivial lifecycle events correctly. These tests run fast and should catch packaging regressions before the heavier simulation and unit suites execute.
- [Test Suite for streamSpec — Unit Tests for Lifecycle, Chunking, Options, and Safety](./test-suite-for-streamspec-unit-tests-for-lifecycle-chunking-options-and-safety.md) — This unit test suite provides systematic coverage of `streamSpec` across lifecycle edge cases, variable chunk sizes, configuration options, safety boundaries, and cross-environment compatibility. It uses controlled synthetic generators rather than real LLM output, giving precise control over timing and content to assert exact behavior.

### `styling` — 1 article(s)

- [Class Name Utility (cn)](./class-name-utility-cn.md) — A thin wrapper around the `clsx` library that merges conditional CSS class strings into a single space-separated string. Used throughout Ripple components to compose Tailwind and custom class names cleanly.

### `switch` — 2 article(s)

- [Switch Component — Accessible Toggle with Size Variants](./switch-component-accessible-toggle-with-size-variants.md) — The Switch component wraps bits-ui's SwitchPrimitive to deliver an accessible, animated toggle control with `sm` and `default` size variants, two-way `checked` binding, validation error state, and RTL support. It uses CSS data attributes and Tailwind variants for all visual states, keeping zero state logic in the component itself.
- [Switch Index — Public API Entry Point](./switch-index-public-api-entry-point.md) — The switch index module re-exports the Switch component under both its canonical `Root` name and its human-readable `Switch` alias, following ripple's standard barrel file pattern for all UI widget modules.

### `table` — 9 article(s)

- [Table Body — Data Row Container with Last-Row Border Suppression](./table-body-data-row-container-with-last-row-border-suppression.md) — TableBody renders a styled `<tbody>` element that wraps the data rows of a table, applying a targeted CSS rule to remove the bottom border from the last row. This prevents a double-border artifact at the boundary between `<tbody>` and `<tfoot>` (or the table edge).
- [Table Caption — Accessible Table Description Element](./table-caption-accessible-table-description-element.md) — TableCaption renders a styled HTML `<caption>` element that provides an accessible, programmatically associated description for a table. Positioned below the table by default via `margin-top`, it uses muted foreground and small text to visually distinguish itself from table data.
- [Table Cell — Data Cell with Checkbox Padding Adjustment](./table-cell-data-cell-with-checkbox-padding-adjustment.md) — TableCell renders a styled `<td>` element with consistent padding, vertical alignment, and a defensive CSS rule that removes right padding when the cell contains a checkbox. This prevents double-spacing in selection column cells without requiring conditional props or wrapper divs.
- [Table Footer — Summary Row Container with Subtle Background](./table-footer-summary-row-container-with-subtle-background.md) — TableFooter renders a styled `<tfoot>` element for housing summary or aggregate rows at the bottom of a table, applying a muted semi-transparent background and a top border while removing the bottom border from its last row to prevent double borders.
- [Table Head — Column Header Cell with Checkbox Spacing](./table-head-column-header-cell-with-checkbox-spacing.md) — TableHead renders a styled `<th>` element for column header cells, applying consistent height, horizontal padding, left-aligned text, and the same checkbox-column padding fix used in TableCell. It ensures header cells align with their body counterparts in selection columns.
- [Table Header — Column Group Container with Row Border Rules](./table-header-column-group-container-with-row-border-rules.md) — TableHeader renders a styled `<thead>` element that groups all column header rows, applying a CSS rule that adds a bottom border to every `<tr>` it contains. This ensures header rows are always visually separated from the data body without requiring individual row styling.
- [Table Index — Full Component Family Public API](./table-index-full-component-family-public-api.md) — The table index module assembles all eight table sub-components — Root, Body, Caption, Cell, Footer, Head, Header, Row — into a single importable surface with both short canonical names and full descriptive aliases. This barrel file is the only import consumers need for complete table functionality.
- [Table Root Component](./table-root-component.md) — The root container for data tables, wrapping a native `<table>` in an overflow-safe scroll container. Applies consistent sizing and caption positioning while exposing a bindable DOM reference.
- [Table Row Component](./table-row-component.md) — A styled `<tr>` wrapper that provides hover, selection, and transition behaviors for table rows. Exposes a bindable ref and forwards all native HTML table row attributes.

### `tabs` — 5 article(s)

- [Tabs Component Public API Index](./tabs-component-public-api-index.md) — The barrel file for the Tabs component family, re-exporting all four sub-components plus variant utilities under both short and verbose names. Establishes the single import point consumers use to access the entire tabs system.
- [Tabs Content Panel Component](./tabs-content-panel-component.md) — The content panel for an individual tab, delegating visibility control to the `bits-ui` Tabs primitive. Adds consistent typography sizing, flex growth, and outline removal while forwarding all primitive props.
- [Tabs List Container with Variant Support](./tabs-list-container-with-variant-support.md) — The container that holds tab trigger buttons, supporting `default` (pill/background) and `line` (underline) visual variants. Uses `tailwind-variants` for variant composition and adapts layout for both horizontal and vertical orientations.
- [Tabs Root Component](./tabs-root-component.md) — The root orchestrator for the tabs system, wrapping `bits-ui`'s `Tabs.Root` primitive with a bindable `value` prop and orientation-aware flex layout. Serves as the context provider for all child tab components.
- [Tabs Trigger Button Component](./tabs-trigger-button-component.md) — The clickable tab button that activates a content panel, with an extensive Tailwind class composition covering active state, focus visibility, dark mode, orientation adaptation, and an animated line-variant underline indicator.

### `test` — 14 article(s)

- [Test Suite: DashboardManager — Widget CRUD, Reorder, and Change Events](./test-suite-dashboardmanager-widget-crud-reorder-and-change-events.md) — Comprehensive unit tests for DashboardManager covering widget add, remove, update, move, reorder, bulk load, ID generation, and the onChange subscription system. Tests verify that mutations produce correct state and reliably fire change callbacks, while load() deliberately does not trigger callbacks.
- [Test Suite: EventDispatcher — Phase B Multistep Flow Actions](./test-suite-eventdispatcher-phase-b-multistep-flow-actions.md) — Behavior specs for the Phase B multistep event dispatcher, covering all action types including flow, branch, confirm, validate, delay, api, and invoke. Tests also verify backwards compatibility with the legacy flat-array dispatch pattern and enforce nesting depth limits.
- [Test Suite for Button Widget](./test-suite-for-button-widget.md) — Vitest + Testing Library test suite for the Button input widget covering rendering, variant and size data attributes, click event firing, disabled and loading state enforcement, spinner slot rendering, type attribute forwarding, and the data-state machine. Uses userEvent for realistic interaction simulation.
- [Test Suite for C4 ELK Layout Utilities](./test-suite-for-c4-elk-layout-utilities.md) — Vitest unit tests for `elk-layout.ts`, covering the `getNodeType`, `isGroupNode`, and `computeElkLayout` functions. Tests verify correct C4 element classification and that ELK produces valid node positions for realistic diagram fixtures.
- [Test Suite for Card Layout Widget](./test-suite-for-card-layout-widget.md) — Vitest + Testing Library test suite covering the Card widget's rendering, structural conditionals, variant/density data attributes, interactive button behavior, and ARIA semantics. The suite validates both the happy path and defensive edge cases like non-interactive cards with onclick handlers.
- [Test Suite for ConfirmDialog Overlay Widget](./test-suite-for-confirmdialog-overlay-widget.md) — Vitest + Testing Library test suite for `ConfirmDialog`, covering state-driven mount/unmount, confirm and cancel button resolution, and the fallback path where `resolveConfirm` returns false (stale resolver after HMR). The `seed()` helper wires a real `StateManager`, `EventDispatcher`, and `WidgetRegistry` through Svelte context.
- [Test Suite for Input Widget](./test-suite-for-input-widget.md) — Comprehensive Vitest + Testing Library test suite for the Input text widget, covering rendering, label/id association, size and state data attributes, type forwarding, oninput/onchange event firing, error and helper message rendering, accessibility attributes (aria-invalid, aria-describedby), and disabled/required/readOnly attribute forwarding.
- [Test Suite for NodeRenderer Named Slot Routing](./test-suite-for-noderenderer-named-slot-routing.md) — This test suite verifies that `NodeRenderer` correctly routes child nodes to named slots (`header`, `footer`) or the default body slot based on each child's optional `slot` field. It covers default routing, named-slot placement, multi-child same-slot stacking, unknown slot silencing, and regression-guards for specs written before slots were introduced.
- [Test Suite for Stat Widget](./test-suite-for-stat-widget.md) — Comprehensive Vitest + Testing Library test suite for the Stat display widget covering number formatting, all direction/sentiment combinations, delta chip rendering, size variants, and Lucide icon selection. Uses data attributes as stable test hooks rather than class names.
- [Test Suite for Stream Spec Simulation — LLM-Realistic Streaming Scenarios](./test-suite-for-stream-spec-simulation-llm-realistic-streaming-scenarios.md) — This test suite validates `streamSpec` and `parsePartialSpec` under realistic LLM-like streaming conditions using a deterministic pseudo-random chunk generator and a set of representative fixture documents. It enforces four invariants across all fixtures: final output correctness, monotonically non-decreasing node counts, no truncated enum values leaking through, and all widget types resolving to registered components.
- [Test Suite for Stream Spec Smoke Tests — Module Integrity and Basic Lifecycle](./test-suite-for-stream-spec-smoke-tests-module-integrity-and-basic-lifecycle.md) — The smoke tests are the first gate in the streaming test pipeline: they verify that the module is importable, exported types exist, the widget registry is intact, and the `streamSpec` store handles trivial lifecycle events correctly. These tests run fast and should catch packaging regressions before the heavier simulation and unit suites execute.
- [Test Suite for streamSpec — Unit Tests for Lifecycle, Chunking, Options, and Safety](./test-suite-for-streamspec-unit-tests-for-lifecycle-chunking-options-and-safety.md) — This unit test suite provides systematic coverage of `streamSpec` across lifecycle edge cases, variable chunk sizes, configuration options, safety boundaries, and cross-environment compatibility. It uses controlled synthetic generators rather than real LLM output, giving precise control over timing and content to assert exact behavior.
- [Test Suite: Layout Engine — determineLayout and analyzeData](./test-suite-layout-engine-determinelayout-and-analyzedata.md) — Unit tests for the layout engine's two public functions: `determineLayout` (maps a UniversalSpec intent and data shape to a layout string) and `analyzeData` (extracts item count and field names from a spec's data object). Tests cover all intent types, display hint overrides, browse sub-layouts, and data analysis edge cases.
- [Test Suite: WidgetRegistry — Register, Invoke, and Unregister Round Trips](./test-suite-widgetregistry-register-invoke-and-unregister-round-trips.md) — Unit tests for the WidgetRegistry class, verifying method registration, argument forwarding, idempotent unregistration, replacement safety, and edge case handling. These tests were written alongside Phase B flow actions to ensure the `invoke` action has a well-behaved backing registry.

### `testing` — 14 article(s)

- [Test Suite: DashboardManager — Widget CRUD, Reorder, and Change Events](./test-suite-dashboardmanager-widget-crud-reorder-and-change-events.md) — Comprehensive unit tests for DashboardManager covering widget add, remove, update, move, reorder, bulk load, ID generation, and the onChange subscription system. Tests verify that mutations produce correct state and reliably fire change callbacks, while load() deliberately does not trigger callbacks.
- [Test Suite: EventDispatcher — Phase B Multistep Flow Actions](./test-suite-eventdispatcher-phase-b-multistep-flow-actions.md) — Behavior specs for the Phase B multistep event dispatcher, covering all action types including flow, branch, confirm, validate, delay, api, and invoke. Tests also verify backwards compatibility with the legacy flat-array dispatch pattern and enforce nesting depth limits.
- [Test Suite for Button Widget](./test-suite-for-button-widget.md) — Vitest + Testing Library test suite for the Button input widget covering rendering, variant and size data attributes, click event firing, disabled and loading state enforcement, spinner slot rendering, type attribute forwarding, and the data-state machine. Uses userEvent for realistic interaction simulation.
- [Test Suite for Card Layout Widget](./test-suite-for-card-layout-widget.md) — Vitest + Testing Library test suite covering the Card widget's rendering, structural conditionals, variant/density data attributes, interactive button behavior, and ARIA semantics. The suite validates both the happy path and defensive edge cases like non-interactive cards with onclick handlers.
- [Test Suite for ConfirmDialog Overlay Widget](./test-suite-for-confirmdialog-overlay-widget.md) — Vitest + Testing Library test suite for `ConfirmDialog`, covering state-driven mount/unmount, confirm and cancel button resolution, and the fallback path where `resolveConfirm` returns false (stale resolver after HMR). The `seed()` helper wires a real `StateManager`, `EventDispatcher`, and `WidgetRegistry` through Svelte context.
- [Test Suite for Input Widget](./test-suite-for-input-widget.md) — Comprehensive Vitest + Testing Library test suite for the Input text widget, covering rendering, label/id association, size and state data attributes, type forwarding, oninput/onchange event firing, error and helper message rendering, accessibility attributes (aria-invalid, aria-describedby), and disabled/required/readOnly attribute forwarding.
- [Test Suite for NodeRenderer Named Slot Routing](./test-suite-for-noderenderer-named-slot-routing.md) — This test suite verifies that `NodeRenderer` correctly routes child nodes to named slots (`header`, `footer`) or the default body slot based on each child's optional `slot` field. It covers default routing, named-slot placement, multi-child same-slot stacking, unknown slot silencing, and regression-guards for specs written before slots were introduced.
- [Test Suite for Stat Widget](./test-suite-for-stat-widget.md) — Comprehensive Vitest + Testing Library test suite for the Stat display widget covering number formatting, all direction/sentiment combinations, delta chip rendering, size variants, and Lucide icon selection. Uses data attributes as stable test hooks rather than class names.
- [Test Suite for Stream Spec Simulation — LLM-Realistic Streaming Scenarios](./test-suite-for-stream-spec-simulation-llm-realistic-streaming-scenarios.md) — This test suite validates `streamSpec` and `parsePartialSpec` under realistic LLM-like streaming conditions using a deterministic pseudo-random chunk generator and a set of representative fixture documents. It enforces four invariants across all fixtures: final output correctness, monotonically non-decreasing node counts, no truncated enum values leaking through, and all widget types resolving to registered components.
- [Test Suite for Stream Spec Smoke Tests — Module Integrity and Basic Lifecycle](./test-suite-for-stream-spec-smoke-tests-module-integrity-and-basic-lifecycle.md) — The smoke tests are the first gate in the streaming test pipeline: they verify that the module is importable, exported types exist, the widget registry is intact, and the `streamSpec` store handles trivial lifecycle events correctly. These tests run fast and should catch packaging regressions before the heavier simulation and unit suites execute.
- [Test Suite for streamSpec — Unit Tests for Lifecycle, Chunking, Options, and Safety](./test-suite-for-streamspec-unit-tests-for-lifecycle-chunking-options-and-safety.md) — This unit test suite provides systematic coverage of `streamSpec` across lifecycle edge cases, variable chunk sizes, configuration options, safety boundaries, and cross-environment compatibility. It uses controlled synthetic generators rather than real LLM output, giving precise control over timing and content to assert exact behavior.
- [Test Suite: Layout Engine — determineLayout and analyzeData](./test-suite-layout-engine-determinelayout-and-analyzedata.md) — Unit tests for the layout engine's two public functions: `determineLayout` (maps a UniversalSpec intent and data shape to a layout string) and `analyzeData` (extracts item count and field names from a spec's data object). Tests cover all intent types, display hint overrides, browse sub-layouts, and data analysis edge cases.
- [Test Suite: WidgetRegistry — Register, Invoke, and Unregister Round Trips](./test-suite-widgetregistry-register-invoke-and-unregister-round-trips.md) — Unit tests for the WidgetRegistry class, verifying method registration, argument forwarding, idempotent unregistration, replacement safety, and edge case handling. These tests were written alongside Phase B flow actions to ensure the `invoke` action has a well-behaved backing registry.
- [Vitest Test Setup — jest-dom Matchers](./vitest-test-setup-jest-dom-matchers.md) — A single-line Vitest global setup file that imports the @testing-library/jest-dom/vitest extension, injecting DOM-aware assertion matchers like toBeInTheDocument, toHaveClass, and toBeVisible into every test in the suite.

### `theming` — 2 article(s)

- [Chart Style — Dynamic CSS Variable Injection for Theme-Aware Chart Colors](./chart-style-dynamic-css-variable-injection-for-theme-aware-chart-colors.md) — ChartStyle generates and injects a `<style>` block at runtime that maps chart series identifiers to CSS custom properties, supporting both light and dark theme variants. It is a headless utility component — it produces no visible DOM, only scoped CSS.
- [Root Layout — Shell, Navigation, and Theme Toggle](./root-layout-shell-navigation-and-theme-toggle.md) — The SvelteKit root layout component that wraps every page in a persistent navigation shell, injects global styles and the SVG favicon, and wires up a reactive dark-mode toggle that reads the OS preference on first load.

### `tooltip` — 6 article(s)

- [Tooltip Component Public API Index](./tooltip-component-public-api-index.md) — The barrel file for the Tooltip component family, re-exporting five sub-components — Root, Trigger, Content, Provider, and Portal — under both short and verbose names. Forms the single import surface for all tooltip functionality.
- [Tooltip Content Bubble with Arrow and Animations](./tooltip-content-bubble-with-arrow-and-animations.md) — The visible tooltip bubble component, composing portal rendering, directional entry/exit animations, keyboard shortcut badge support, and a custom arrow indicator. Delegates positioning and accessibility to `bits-ui`'s Tooltip.Content primitive.
- [Tooltip Portal Wrapper Component](./tooltip-portal-wrapper-component.md) — A minimal pass-through wrapper around `bits-ui`'s `Tooltip.Portal`, enabling tooltip content to be rendered outside its DOM ancestor tree. Prevents clipping by `overflow: hidden` ancestors.
- [Tooltip Provider Component](./tooltip-provider-component.md) — A shared configuration provider for tooltip groups, setting `delayDuration` to zero by default to deliver immediate tooltip responses. Wraps `bits-ui`'s Tooltip.Provider to override the library's built-in delay.
- [Tooltip Root Component](./tooltip-root-component.md) — The top-level orchestrator for a single tooltip instance, composing `TooltipProvider` and `bits-ui`'s `Tooltip.Root` with a bindable `open` state. Ensures every tooltip has provider context without requiring consumers to add it manually.
- [Tooltip Trigger Component](./tooltip-trigger-component.md) — A minimal wrapper around `bits-ui`'s `Tooltip.Trigger`, adding a `data-slot` identifier and exposing a bindable `ref`. Acts as the hover/focus target that activates the tooltip.

### `type-system` — 1 article(s)

- [SvelteKit App Type Declarations](./sveltekit-app-type-declarations.md) — The `app.d.ts` file is the SvelteKit-generated ambient type declaration file that extends the global `App` namespace. It provides a central place to type platform-specific interfaces like error shapes, locals, page data, page state, and platform context.

### `types` — 2 article(s)

- [C4 Widget Type Definitions — Data Model for All Four C4 Levels](./c4-widget-type-definitions-data-model-for-all-four-c4-levels.md) — The TypeScript type definitions for the entire C4 widget subsystem, covering all four C4 hierarchy levels (Person, System, Container, Component), relationships, diagram spec, and the internal SvelteFlow node data payload. These types are the contract between diagram data authors and the rendering engine.
- [Workflow Widget Type Definitions](./workflow-widget-type-definitions.md) — TypeScript interfaces and union types that define the data contract for the workflow widget's nodes and edges, enabling type-safe pocket spec authoring and enforcing the six valid node types and five execution status states.

### `typography` — 8 article(s)

- [Card Description — Muted Supporting Text for Card Headers](./card-description-muted-supporting-text-for-card-headers.md) — CardDescription renders secondary descriptive text beneath a card title using muted foreground color and small type. It uses a semantic `<p>` element to preserve document structure and applies design token classes that adapt to light and dark themes automatically.
- [Card Title — Size-Aware Heading Text for Card Components](./card-title-size-aware-heading-text-for-card-components.md) — CardTitle renders the primary heading within a card header. It is a thin styled wrapper that responds to the parent card's size variant, reducing text size from `text-base` to `text-sm` when the card is in compact mode.
- [Heading Widget — Semantic H1–H6 with Design System Typography](./heading-widget-semantic-h1h6-with-design-system-typography.md) — A typography widget that renders the correct semantic heading element (h1–h6) based on a `level` prop, with each level pre-mapped to a consistent Tailwind typography scale. Solves the problem of AI-generated specs needing to express hierarchy without embedding raw HTML tags.
- [Popover Title — Semantic Heading Slot for Popover Panels](./popover-title-semantic-heading-slot-for-popover-panels.md) — A styled `div` that renders as the title section inside a popover panel, applying medium font weight and exposing a bindable element reference for programmatic access. It uses the standard `cn` utility for class merging and the `WithElementRef` pattern to support imperative DOM operations.
- [Select Label — Standalone Descriptive Label for Select Content Panels](./select-label-standalone-descriptive-label-for-select-content-panels.md) — A plain `div`-based label component for use within select content panels, rendering small muted text without any bits-ui backing. It differs from `SelectGroupHeading` in that it is not associated with a group via ARIA and serves as a freestanding visual annotation rather than a semantic section header.
- [Sheet Description — Accessible Panel Summary Text](./sheet-description-accessible-panel-summary-text.md) — Renders the descriptive subtitle text inside a sheet panel with muted styling. It maps to bits-ui's `Dialog.Description`, which wires `aria-describedby` between this element and the sheet content so screen readers can announce the panel's purpose.
- [Sheet Title — Accessible Heading for Slide-in Panels](./sheet-title-accessible-heading-for-slide-in-panels.md) — SheetTitle wraps the bits-ui Dialog.Title primitive to provide a consistently styled, accessible heading element for sheet panels. It merges design-system typography tokens with an optional `className` override and exposes a bindable `ref` for imperative DOM access.
- [Text Display Widget](./text-display-widget.md) — A foundational text rendering primitive that maps a flat set of typography props — size, weight, color, and inline mode — to semantic HTML elements. Renders as a `<p>` block element by default or an inline `<span>` when the `inline` prop is set.

### `ui` — 15 article(s)

- [Sheet Portal — DOM Teleportation Wrapper](./sheet-portal-dom-teleportation-wrapper.md) — SheetPortal is a thin Svelte 5 wrapper around the bits-ui Dialog.Portal primitive that teleports sheet overlay content outside the normal DOM tree. By delegating all rendering to the underlying primitive, it ensures sheet content escapes stacking contexts and z-index traps without any additional logic.
- [Sheet Root — Controlled Slide-in Panel State Manager](./sheet-root-controlled-slide-in-panel-state-manager.md) — Sheet is the root context provider for the sheet component family, delegating to bits-ui's Dialog.Root while exposing `open` as a bindable Svelte 5 prop for two-way state control. It is the entry point that all other Sheet sub-components depend on for shared dialog state.
- [Sheet Title — Accessible Heading for Slide-in Panels](./sheet-title-accessible-heading-for-slide-in-panels.md) — SheetTitle wraps the bits-ui Dialog.Title primitive to provide a consistently styled, accessible heading element for sheet panels. It merges design-system typography tokens with an optional `className` override and exposes a bindable `ref` for imperative DOM access.
- [Sheet Trigger — Open Control for Slide-in Panels](./sheet-trigger-open-control-for-slide-in-panels.md) — SheetTrigger wraps the bits-ui Dialog.Trigger primitive to provide a typed, ref-aware open control for sheet components. It delegates all open/close state management to the bits-ui context and exposes `data-slot` for consistent component targeting.
- [Slider Component — Accessible Range Input with Orientation Support](./slider-component-accessible-range-input-with-orientation-support.md) — The Slider component wraps bits-ui's SliderPrimitive.Root to render an accessible, styled range input supporting horizontal and vertical orientations, multiple thumbs, and two-way value binding. It composes a track, a filled range bar, and dynamically generated thumb elements using Svelte 5 snippets.
- [Slider Index — Public API Entry Point](./slider-index-public-api-entry-point.md) — The slider index module re-exports the Slider component under both its canonical `Root` name and its human-readable `Slider` alias, following ripple's standard barrel file pattern. It acts as the single import surface for all downstream consumers of the slider widget.
- [Switch Component — Accessible Toggle with Size Variants](./switch-component-accessible-toggle-with-size-variants.md) — The Switch component wraps bits-ui's SwitchPrimitive to deliver an accessible, animated toggle control with `sm` and `default` size variants, two-way `checked` binding, validation error state, and RTL support. It uses CSS data attributes and Tailwind variants for all visual states, keeping zero state logic in the component itself.
- [Switch Index — Public API Entry Point](./switch-index-public-api-entry-point.md) — The switch index module re-exports the Switch component under both its canonical `Root` name and its human-readable `Switch` alias, following ripple's standard barrel file pattern for all UI widget modules.
- [Table Body — Data Row Container with Last-Row Border Suppression](./table-body-data-row-container-with-last-row-border-suppression.md) — TableBody renders a styled `<tbody>` element that wraps the data rows of a table, applying a targeted CSS rule to remove the bottom border from the last row. This prevents a double-border artifact at the boundary between `<tbody>` and `<tfoot>` (or the table edge).
- [Table Caption — Accessible Table Description Element](./table-caption-accessible-table-description-element.md) — TableCaption renders a styled HTML `<caption>` element that provides an accessible, programmatically associated description for a table. Positioned below the table by default via `margin-top`, it uses muted foreground and small text to visually distinguish itself from table data.
- [Table Cell — Data Cell with Checkbox Padding Adjustment](./table-cell-data-cell-with-checkbox-padding-adjustment.md) — TableCell renders a styled `<td>` element with consistent padding, vertical alignment, and a defensive CSS rule that removes right padding when the cell contains a checkbox. This prevents double-spacing in selection column cells without requiring conditional props or wrapper divs.
- [Table Footer — Summary Row Container with Subtle Background](./table-footer-summary-row-container-with-subtle-background.md) — TableFooter renders a styled `<tfoot>` element for housing summary or aggregate rows at the bottom of a table, applying a muted semi-transparent background and a top border while removing the bottom border from its last row to prevent double borders.
- [Table Head — Column Header Cell with Checkbox Spacing](./table-head-column-header-cell-with-checkbox-spacing.md) — TableHead renders a styled `<th>` element for column header cells, applying consistent height, horizontal padding, left-aligned text, and the same checkbox-column padding fix used in TableCell. It ensures header cells align with their body counterparts in selection columns.
- [Table Header — Column Group Container with Row Border Rules](./table-header-column-group-container-with-row-border-rules.md) — TableHeader renders a styled `<thead>` element that groups all column header rows, applying a CSS rule that adds a bottom border to every `<tr>` it contains. This ensures header rows are always visually separated from the data body without requiring individual row styling.
- [Table Index — Full Component Family Public API](./table-index-full-component-family-public-api.md) — The table index module assembles all eight table sub-components — Root, Body, Caption, Cell, Footer, Head, Header, Row — into a single importable surface with both short canonical names and full descriptive aliases. This barrel file is the only import consumers need for complete table functionality.

### `ui-component` — 15 article(s)

- [Dialog Component Index — Dual-Name Export Surface](./dialog-component-index-dual-name-export-surface.md) — The public barrel file for the Ripple dialog system. It exports all ten sub-components under two naming conventions — short names (`Root`, `Title`, etc.) for destructured imports and prefixed names (`Dialog`, `DialogTitle`, etc.) for namespace imports — giving consumers flexibility in how they reference components.
- [Dialog Footer — Action Bar with Optional Close Button](./dialog-footer-action-bar-with-optional-close-button.md) — Renders the bottom action area of a dialog, laying out child elements (typically buttons) in a responsive row. Optionally injects a Close button wired to the bits-ui dialog primitive for zero-configuration dismiss behavior.
- [Dialog Header — Title and Description Container](./dialog-header-title-and-description-container.md) — A minimal layout wrapper that vertically stacks title and description elements at the top of a dialog panel. It provides consistent vertical spacing and a `data-slot` marker for structural targeting.
- [Dialog Overlay — Animated Backdrop with Backdrop Filter](./dialog-overlay-animated-backdrop-with-backdrop-filter.md) — Wraps the bits-ui `DialogPrimitive.Overlay` to produce a full-viewport dimmed backdrop behind an open dialog. Applies entrance/exit animations and a conditional blur effect, with progressive enhancement for browsers that support the backdrop-filter CSS property.
- [Dialog Portal — DOM Teleportation Wrapper](./dialog-portal-dom-teleportation-wrapper.md) — A one-line pass-through component that delegates to bits-ui's `DialogPrimitive.Portal`, teleporting its children out of the current DOM tree and into the document body. This prevents z-index conflicts caused by stacking contexts in ancestor elements.
- [Dialog Root — Open State Provider and Context Root](./dialog-root-open-state-provider-and-context-root.md) — The root component of the Ripple dialog system. It wraps bits-ui's `DialogPrimitive.Root` and provides a bindable `open` prop that allows parent components to externally control dialog visibility. All other dialog components must be rendered as descendants of this root.
- [Dialog Title — Accessible Heading with Typography Defaults](./dialog-title-accessible-heading-with-typography-defaults.md) — Wraps bits-ui's `DialogPrimitive.Title` to render the dialog's primary heading with consistent typographic styling. The primitive handles ARIA role assignment automatically, making the title accessible to screen readers without manual `role` or `aria-labelledby` wiring.
- [Dialog Trigger — Open Button Wired to Dialog State](./dialog-trigger-open-button-wired-to-dialog-state.md) — A thin wrapper around bits-ui's `DialogPrimitive.Trigger` that opens the parent dialog when activated. Defaults `type` to `"button"` to prevent accidental form submissions, and exposes a bindable ref for DOM access.
- [Input Component Index — Single-Component Barrel with Dual Export](./input-component-index-single-component-barrel-with-dual-export.md) — The public entry point for Ripple's input component. Exports `input.svelte` under both a short name (`Root`) and a semantic alias (`Input`), following the same dual-export convention used across all Ripple UI component families.
- [Input — Unified Text and File Input with Dual Rendering Paths](./input-unified-text-and-file-input-with-dual-rendering-paths.md) — The core Ripple input component handles both standard text-like inputs and file inputs through a conditional rendering split. File inputs require separate treatment because Svelte's `bind:files` binding is only valid on `type="file"` elements, and TypeScript's type system enforces this distinction at the prop level.
- [Popover Close — Dismiss Button Wired to Popover State](./popover-close-dismiss-button-wired-to-popover-state.md) — A minimal wrapper around bits-ui's `PopoverPrimitive.Close` that renders a button capable of dismissing the parent popover. Exposes a bindable `ref` and forwards all props, with no custom logic beyond the data-slot marker.
- [Popover Component Index — Eight-Part Barrel with Dual-Name Exports](./popover-component-index-eight-part-barrel-with-dual-name-exports.md) — The public entry point for Ripple's popover system. Exports all eight sub-components under both short and prefixed naming conventions, mirroring the structure of the dialog index and enabling flexible, collision-free imports.
- [Popover Content — Animated Floating Panel with Portal Integration](./popover-content-animated-floating-panel-with-portal-integration.md) — Renders the floating panel of a popover inside a portal, with entrance/exit animations keyed to open/close state and configurable positioning. Automatically wraps content in `PopoverPortal` to escape ancestor stacking contexts.
- [Popover Description — Muted Supporting Text Block](./popover-description-muted-supporting-text-block.md) — A minimal layout component that renders supporting text inside a popover with muted color styling. It is a plain `<div>` wrapper — unlike its dialog counterpart, it does not use the bits-ui `Description` primitive and therefore does not set `aria-describedby` automatically.
- [Popover Header — Compact Title and Description Stack](./popover-header-compact-title-and-description-stack.md) — A lightweight layout wrapper that groups title and description elements inside a popover with tight vertical spacing. Mirrors the structure of `DialogHeader` but uses a smaller gap and text size appropriate for the popover's more compact panel format.

### `utilities` — 1 article(s)

- [Chart Utils — Config Types, Context Management, and Payload Resolution](./chart-utils-config-types-context-management-and-payload-resolution.md) — This utility module defines the `ChartConfig` type and the Svelte context pair (`setChartContext` / `useChart`) that share chart configuration across the component tree. It also exports `getPayloadConfigFromPayload`, a defensive resolver that extracts per-series config from LayerChart's polymorphic tooltip payload.

### `utility` — 4 article(s)

- [Class Name Utility (cn)](./class-name-utility-cn.md) — A thin wrapper around the `clsx` library that merges conditional CSS class strings into a single space-separated string. Used throughout Ripple components to compose Tailwind and custom class names cleanly.
- [ELK-Based Auto-Layout Engine for C4 Diagrams](./elk-based-auto-layout-engine-for-c4-diagrams.md) — Converts a `C4Diagram` data structure into ELK graph nodes and edges, runs the ELK layered layout algorithm, and returns a flat `Map<id, LayoutPosition>` of computed positions. Replaced an earlier grid-based layout to provide professional, collision-free hierarchical placement.
- [favicon.ts: Source-to-Favicon URL Resolver](./faviconts-source-to-favicon-url-resolver.md) — The `favicon.ts` utility exports a single `faviconUrl()` function that maps a source name string to a Google Favicon Service URL. It maintains a hand-curated domain map for well-known publishers (particularly Indian financial and news sources) and falls back to heuristic domain inference for unknown sources.
- [URL Sanitizer — XSS-Safe Href Utility](./url-sanitizer-xss-safe-href-utility.md) — A focused security utility that strips dangerous URL schemes before they reach the DOM, preventing XSS attacks through javascript:, data:, and file: hrefs. Added as a blocker fix in PR #14 when externally-supplied Pocket spec URLs were found to flow directly into href attributes.

### `visual-effects` — 1 article(s)

- [GlassCard Glassmorphism Container Widget](./glasscard-glassmorphism-container-widget.md) — A specialized card widget that renders a frosted-glass container using backdrop-filter, color-mix, and multi-layer box-shadow. Extracted from the paw-os-ui enterprise layout system, it exposes fine-grained glass effect controls (`opacity`, `blur`, `tint`, `borderGlow`) as props.

### `visualization` — 1 article(s)

- [Chart Widget — Multi-Type Data Visualization with ECharts](./chart-widget-multi-type-data-visualization-with-echarts.md) — A single-component wrapper around Apache ECharts that supports ten chart types — bar, line, area, pie, donut, candlestick, sparkline, heatmap, gauge, and radar — all within a consistent theme-aware, resize-reactive API. The component lazy-loads ECharts on mount, adapts colors to the active CSS theme, and accepts deep-merge overrides for advanced customization.

### `vitest` — 1 article(s)

- [Vitest Test Setup — jest-dom Matchers](./vitest-test-setup-jest-dom-matchers.md) — A single-line Vitest global setup file that imports the @testing-library/jest-dom/vitest extension, injecting DOM-aware assertion matchers like toBeInTheDocument, toHaveClass, and toBeVisible into every test in the suite.

### `widget` — 147 article(s)

- [Accordion Component Barrel Export](./accordion-component-barrel-export.md) — The accordion `index.ts` barrel re-exports all four accordion sub-components — Root, Content, Item, and Trigger — under both short names and fully-qualified names, enabling two distinct import styles for the accordion compound component.
- [Accordion Content Panel](./accordion-content-panel.md) — AccordionContent wraps the bits-ui `Accordion.Content` primitive to provide the animated reveal/hide behavior for an accordion section's body. It adds a padded inner wrapper with opinionated link and paragraph styles, and exposes a `ref` binding for direct DOM access.
- [Accordion Item Container](./accordion-item-container.md) — AccordionItem is a thin wrapper around the bits-ui `Accordion.Item` primitive that adds a bottom border to all items except the last, maintains a `data-slot` marker for styling, and exposes a `ref` binding. It is the structural unit that groups a trigger and its content panel.
- [Accordion Root Container](./accordion-root-container.md) — The Accordion root component wraps bits-ui's `Accordion.Root` primitive to provide the stateful container for a set of collapsible sections. It exposes bindable `value` and `ref` props, and lays items out in a vertical flex column.
- [Accordion Trigger Button](./accordion-trigger-button.md) — AccordionTrigger wraps the bits-ui `Accordion.Header` and `Accordion.Trigger` primitives to render the clickable row that expands or collapses an accordion item. It renders a chevron icon that switches between down and up variants based on the item's open state using CSS group-aria selectors.
- [Alert Action Slot](./alert-action-slot.md) — AlertAction is a positioning wrapper that places action content (typically a dismiss button or a CTA link) in the top-right corner of an alert. It works in conjunction with the parent `Alert` component's `has-data-[slot=alert-action]:pr-18` rule to prevent the alert body text from overlapping the action.
- [Alert Component Public API and Barrel Export](./alert-component-public-api-and-barrel-export.md) — The alert index file is the single public entry point for the entire alert component family, re-exporting all sub-components under both short-form and fully-qualified names. This dual-alias pattern gives consumers flexibility to use destructured imports or prefixed names without creating ambiguity.
- [Alert Description Text Block](./alert-description-text-block.md) — AlertDescription renders the body text of an alert with opinionated typography defaults including responsive text balancing, auto-spaced paragraphs, and normalized link styling. It is a thin presentational wrapper with no behavioral logic.
- [Alert Root Component with Variant System](./alert-root-component-with-variant-system.md) — The Alert root component provides a styled notification container with a `default` and `destructive` variant system built with `tailwind-variants`. It uses CSS grid with data-slot selectors to automatically accommodate icon+text layouts and action button placements without requiring callers to specify layout props.
- [Alert Title](./alert-title.md) — AlertTitle renders the heading line of an alert, with automatic column positioning when the alert contains an icon, and normalized link styling. It is a semantic label wrapper with no behavioral logic.
- [AnalystBar: Analyst Rating Distribution Visualization](./analystbar-analyst-rating-distribution-visualization.md) — AnalystBar renders a proportional color-segmented bar showing the breakdown of analyst ratings (Buy, Hold, Sell) for a financial instrument, along with consensus label and target price. It uses Svelte 5 `$derived` reactivity to compute segment widths from raw counts, gracefully handling the zero-total edge case.
- [Avatar Badge — Status Indicator Overlay](./avatar-badge-status-indicator-overlay.md) — AvatarBadge renders a small circular overlay anchored to the bottom-right corner of an avatar, used to convey status (online, offline, notifications). It reads the parent avatar's size via CSS group data attributes and scales itself proportionally, hiding its icon content on small avatars where it would otherwise be illegible.
- [Avatar Component Public API and Barrel Export](./avatar-component-public-api-and-barrel-export.md) — The avatar index barrel file is the single import point for the entire Avatar component family, exporting all six sub-components under both short primitive names and fully-qualified prefixed aliases. This enables consistent, import-friendly consumption across Ripple's generative UI runtime.
- [Avatar Fallback — Image Load Failure Display](./avatar-fallback-image-load-failure-display.md) — AvatarFallback is the graceful-degradation layer rendered when an avatar image fails to load or is unavailable. It wraps bits-ui's headless `AvatarPrimitive.Fallback` to inject Ripple's design tokens and size-responsive typography.
- [Avatar Group Count — Overflow Indicator for Avatar Stacks](./avatar-group-count-overflow-indicator-for-avatar-stacks.md) — AvatarGroupCount renders the overflow indicator at the end of an avatar stack — the '+5' or similar count shown when a group has more members than the display limit. It mirrors the circular shape and sizing of sibling avatars while reading the group's size tier from a parent CSS group selector.
- [Avatar Group — Stacked Avatar Row Container](./avatar-group-stacked-avatar-row-container.md) — AvatarGroup is the layout container that stacks multiple Avatar components into the familiar overlapping-circles row. It establishes the CSS context that child avatars and the overflow count indicator query for size coordination, and applies negative spacing to create the overlapping effect.
- [Avatar Image — Profile Picture Renderer with Load Tracking](./avatar-image-profile-picture-renderer-with-load-tracking.md) — AvatarImage renders the actual profile picture inside an avatar, delegating load-state tracking to bits-ui's headless primitive. It enforces circular cropping and aspect-ratio preservation so arbitrarily-sized source images always fit the circular frame correctly.
- [Avatar Root — Core Container with Size and Load State](./avatar-root-core-container-with-size-and-load-state.md) — Avatar is the root component in the avatar family, providing the circular container, size tier selection, load status binding, and the pseudo-element border overlay. It coordinates sizing context for child components (image, fallback, badge) and exposes loadingStatus as a bindable prop for parent consumption.
- [Avatar Widget — User Image with Fallback Initials](./avatar-widget-user-image-with-fallback-initials.md) — A display widget that renders a user or entity avatar using shadcn's Avatar component, automatically falling back to a placeholder character when the image is absent or fails to load. Supports inline style objects passed as records, which it serializes to a CSS style string.
- [Badge Component — Polymorphic Label with Variant System](./badge-component-polymorphic-label-with-variant-system.md) — Badge is a compact label component that renders as either a `<span>` or `<a>` depending on whether an `href` prop is provided. It uses `tailwind-variants` to expose six visual styles and supports inline icon placement with automatic icon sizing and padding adjustments.
- [Badge Component Public API Export](./badge-component-public-api-export.md) — The badge index file is a minimal barrel that re-exports the Badge component and its variant utilities from the single implementation file. It provides the stable import path for the Badge family and makes the `badgeVariants` function and `BadgeVariant` type available for external use.
- [Badge Widget — Status and Category Label with Variant Extension](./badge-widget-status-and-category-label-with-variant-extension.md) — A display widget that renders inline badges for status labels, categories, and tags, extending shadcn's Badge with two semantic variants (`success` and `warning`) that shadcn doesn't natively support. The extension is implemented through CSS class injection rather than shadcn variant registration, keeping the component self-contained.
- [Button Component — Polymorphic Interactive Element with Full Variant and Size Matrix](./button-component-polymorphic-interactive-element-with-full-variant-and-size-matr.md) — Button is a fully polymorphic interactive component that renders as either a native `<button>` or an `<a>` element, with a comprehensive variant and size matrix defined via `tailwind-variants`. It includes accessibility-correct disabled state handling for link mode and a detailed icon sizing system for inline icon buttons.
- [Button Component Public API Export](./button-component-public-api-export.md) — The button index barrel exports the Button component and all associated types and variant utilities. It follows the dual-alias pattern and additionally surfaces `Props` as a convenience alias for `ButtonProps`, enabling clean generic wrapper typings.
- [Button Input Widget](./button-input-widget.md) — A fully-featured button widget for Ripple's generative UI runtime, supporting six visual variants, four sizes, loading state with spinner, slot-based leading/trailing icons, and form integration. Designed to prevent double-submission and expose machine-readable state via data attributes.
- [Button Showcase — Visual QA for All Button Variants](./button-showcase-visual-qa-for-all-button-variants.md) — A dedicated visual QA page for the Ripple Button widget, rendering every combination of variant, size, icon slot, loading state, disabled state, and full-width layout to enable manual and automated visual regression testing.
- [C4 Component Node — SvelteFlow Custom Node for Service Components](./c4-component-node-svelteflow-custom-node-for-service-components.md) — A SvelteFlow custom node that renders a C4 Component element as a compact blue box with a four-square icon, subtype label, technology badge, and optional knowledge base link. URL sanitization prevents XSS via `javascript:` or `data:` scheme injection in the KB link.
- [C4 Container Node — SvelteFlow Custom Node with Drill-Down Support](./c4-container-node-svelteflow-custom-node-with-drill-down-support.md) — A SvelteFlow custom node that renders a C4 Container element (API servers, databases, queues, etc.) as a blue-700 box with technology label. Supports drill-down navigation from Container to Component level, and includes URL-sanitized knowledge base links.
- [C4 Database Node — Cylinder-Shaped SvelteFlow Node for Storage Elements](./c4-database-node-cylinder-shaped-svelteflow-node-for-storage-elements.md) — A SvelteFlow custom node that renders a C4 database container using a CSS-composed cylinder shape (SVG ellipse cap + rectangular body). Visually distinct from other node types with a purple color scheme, and includes URL-sanitized KB links.
- [C4 Diagram Widget — SvelteFlow + ELK Architecture Visualizer](./c4-diagram-widget-svelteflow-elk-architecture-visualizer.md) — The top-level C4 Model diagram widget that uses SvelteFlow for rendering and ELK.js for automatic layout. It supports all four C4 levels (Context, Container, Component, Code) with drill-down navigation, pan/zoom, and a minimap.
- [C4 Group Node — System Boundary Container for SvelteFlow Parent Nodes](./c4-group-node-system-boundary-container-for-svelteflow-parent-nodes.md) — A SvelteFlow parent/group node that renders a C4 system boundary as a dashed border container. It acts as the visual wrapper for nested container nodes, providing the "system boundary box" pattern from C4 architecture diagrams.
- [C4 Node Components Barrel — Exports All SvelteFlow Custom Node Types](./c4-node-components-barrel-exports-all-svelteflow-custom-node-types.md) — The barrel file for the C4 node component directory, re-exporting all seven custom SvelteFlow node types under a single import path. This keeps `C4Diagram.svelte`'s node type registration clean and allows individual node components to be imported independently by consumers.
- [C4 Person Node — Human Actor Node for C4 Context Diagrams](./c4-person-node-human-actor-node-for-c4-context-diagrams.md) — A SvelteFlow custom node that renders a C4 Person element with a circular avatar icon and name/description labels. Distinguishes internal users (blue) from external actors (gray) and supports optional drill-down navigation.
- [C4 Queue Node — Parallelogram-Shaped Node for Message Brokers](./c4-queue-node-parallelogram-shaped-node-for-message-brokers.md) — A SvelteFlow custom node that renders C4 message queue and broker elements using an amber CSS parallelogram shape. Uses `clip-path` for the skewed visual, includes URL-sanitized KB links, and fires click handlers for element selection.
- [C4 System Node — Top-Level Architecture Block with Drill-Down](./c4-system-node-top-level-architecture-block-with-drill-down.md) — A SvelteFlow custom node that renders a C4 System element as a rounded rectangle, with blue styling for internal systems and gray dashed borders for external ones. Supports multi-level drill-down from Context through Component, and carries reactive style bindings for all color properties.
- [C4 Widget Public API Barrel — Exports for Component, Types, and Layout](./c4-widget-public-api-barrel-exports-for-component-types-and-layout.md) — The barrel export file for the C4 diagram widget subsystem. It re-exports the `C4Diagram` Svelte component, layout utilities, and all TypeScript type definitions from a single import path, establishing the public API surface of the C4 module.
- [Callout: Contextual Highlight Block with Semantic Variants](./callout-contextual-highlight-block-with-semantic-variants.md) — Callout renders a left-bordered highlight block with an icon badge, supporting four semantic variants — info, success, warning, and insight — each with distinct color theming. It exists to draw reader attention to important conclusions, caveats, or findings within AI-generated research content.
- [Card Action — Grid-Positioned Action Slot in Card Header](./card-action-grid-positioned-action-slot-in-card-header.md) — CardAction is a specialised layout sub-component that places action controls (buttons, menus) in the top-right corner of a card header using CSS grid positioning. It uses explicit grid coordinates to float above card title and description content without affecting their flow.
- [Card Component Index — Dual-Name Export Barrel](./card-component-index-dual-name-export-barrel.md) — The card `index.ts` barrel file re-exports all seven card sub-components under both short semantic names (`Root`, `Header`, `Content`) and fully-qualified PascalCase names (`Card`, `CardHeader`, `CardContent`). This dual-export pattern supports both namespace-style and destructured imports.
- [Card Content — Main Body Region with Size-Responsive Padding](./card-content-main-body-region-with-size-responsive-padding.md) — CardContent is the main body slot of a card, providing horizontal padding that tightens automatically when the parent card is in its small size variant. It enforces consistent internal spacing without requiring consumers to manage padding manually.
- [Card Description — Muted Supporting Text for Card Headers](./card-description-muted-supporting-text-for-card-headers.md) — CardDescription renders secondary descriptive text beneath a card title using muted foreground color and small type. It uses a semantic `<p>` element to preserve document structure and applies design token classes that adapt to light and dark themes automatically.
- [Card Footer — Styled Bottom Section of a Card](./card-footer-styled-bottom-section-of-a-card.md) — CardFooter is the bottom-zone wrapper inside a card layout. It applies a muted background, rounded bottom corners, a top border, and responsive padding that automatically adjusts when the parent card is in `size=sm` mode.
- [Card Header — Adaptive Grid Layout for Card Titles and Actions](./card-header-adaptive-grid-layout-for-card-titles-and-actions.md) — CardHeader is the top section of a card component. It uses CSS Grid with container queries and group-data selectors to dynamically adapt its layout based on which child slots (title, description, action) are present.
- [Card Layout Widget](./card-layout-widget.md) — A versatile card container widget with five visual variants, two density modes, and optional interactive behavior. It dynamically renders as either a `<div>` or a `<button>` depending on whether an `onclick` handler is provided alongside `interactive: true`, ensuring semantic HTML and full keyboard accessibility.
- [Card — Root Container with Size Variants and Image-Aware Layout](./card-root-container-with-size-variants-and-image-aware-layout.md) — The root Card component is the orchestrating container for the card system. It exposes a `size` prop that propagates layout hints to all descendant sub-components via `data-size`, and it uses CSS selectors to handle image children, footer presence, and ring styling without any JavaScript.
- [Card Showcase — Visual QA for Card Variants and Composition](./card-showcase-visual-qa-for-card-variants-and-composition.md) — A dedicated visual QA page for the Ripple Card widget, covering all four visual variants, two density modes, interactive state, header/footer snippet slots, and a full composition example that embeds a Stat widget.
- [Card Title — Size-Aware Heading Text for Card Components](./card-title-size-aware-heading-text-for-card-components.md) — CardTitle renders the primary heading within a card header. It is a thin styled wrapper that responds to the parent card's size variant, reducing text size from `text-base` to `text-sm` when the card is in compact mode.
- [Chart Container — LayerChart Integration Host with CSS Override Layer](./chart-container-layerchart-integration-host-with-css-override-layer.md) — ChartContainer is the root wrapper for all chart widgets. It establishes chart context, generates a unique scoped chart ID, injects theme-based CSS variables, and applies a comprehensive set of Tailwind overrides that normalize LayerChart's default visual behavior.
- [Chart Tooltip — Rich Data Series Overlay with Configurable Indicators](./chart-tooltip-rich-data-series-overlay-with-configurable-indicators.md) — ChartTooltip is a styled hover overlay for chart widgets that reads active data series from LayerChart's tooltip context, resolves each series label and color from `ChartConfig`, and renders rows with configurable indicator shapes (dot, line, or dashed). It supports custom formatters via Svelte snippets.
- [Chart Widget — Multi-Type Data Visualization with ECharts](./chart-widget-multi-type-data-visualization-with-echarts.md) — A single-component wrapper around Apache ECharts that supports ten chart types — bar, line, area, pie, donut, candlestick, sparkline, heatmap, gauge, and radar — all within a consistent theme-aware, resize-reactive API. The component lazy-loads ECharts on mount, adapts colors to the active CSS theme, and accepts deep-merge overrides for advanced customization.
- [Checkbox — Accessible Three-State Toggle with Indeterminate Support](./checkbox-accessible-three-state-toggle-with-indeterminate-support.md) — The Checkbox component wraps bits-ui's `Checkbox.Root` primitive to provide a fully accessible toggle with three visual states: unchecked, checked, and indeterminate. It uses lucide icons as indicators and applies extensive Tailwind classes for focus rings, validation states, and disabled group context.
- [Checkbox Component Index — Minimal Barrel with Dual-Name Export](./checkbox-component-index-minimal-barrel-with-dual-name-export.md) — The checkbox `index.ts` barrel re-exports the single `checkbox.svelte` component under both `Root` and `Checkbox` names. This follows the ripple dual-export convention, allowing both namespace-style and direct-import usage with minimal boilerplate.
- [Checkbox Input Widget](./checkbox-input-widget.md) — A controlled checkbox widget that wraps the shadcn/ui Checkbox primitive with optional label layout and a local state sync pattern that bridges Svelte 5 reactive props with the underlying component's internal checked state.
- [Citation: Inline Source Reference Chip with Favicon](./citation-inline-source-reference-chip-with-favicon.md) — Citation renders a compact inline chip showing a source name, its favicon, and an optional superscript citation number — behaving as a clickable link when a URL or click handler is provided. Favicon loading failures are handled gracefully with a colored dot fallback, keeping the component visually consistent even when icon fetching fails.
- [CompanyHeader: Company Identity and Live Price Block](./companyheader-company-identity-and-live-price-block.md) — CompanyHeader renders a full company identity card combining logo (with Clearbit auto-fetch and initial fallback), name, ticker, exchange badge, description, sector tags, live price, and price change — all from a single composable widget. It is the canonical top-of-widget header for stock and company research responses.
- [ConfirmDialog Overlay Widget](./confirmdialog-overlay-widget.md) — An auto-mounted confirmation dialog that renders when the event dispatcher writes a `PendingConfirm` object to `state._ripple_confirm`. On user decision, it calls `resolveConfirm` to resume the suspended flow, with a fallback that directly clears state if the resolver is stale — preventing flows from hanging indefinitely.
- [Container Layout Widget](./container-layout-widget.md) — A minimal transparent wrapper widget that renders a plain `<div>` with optional id, class, style, onclick, and children. It exists as the simplest possible Ripple layout primitive — a semantic grouping container with no visual opinions.
- [Dashboard Auto-Fill Grid Widget (Legacy)](./dashboard-auto-fill-grid-widget-legacy.md) — A legacy CSS grid layout widget that automatically fills columns based on a minimum width using `repeat(auto-fill, minmax(...))`. This component is the static, non-draggable predecessor to the Muuri-based `DashboardRenderer` and is retained for simpler use cases that do not need interactive reordering.
- [Dashboard Grid Slot Widget](./dashboard-grid-slot-widget.md) — A grid cell wrapper used inside the `Dashboard` layout widget that controls column span and prevents child overflow from breaking the grid. Each slot carries identity metadata (`slotId`, `itemId`) intended for future drag-and-drop or serialization use.
- [DashboardRenderer — Muuri-Powered Drag-and-Drop Dashboard Layout](./dashboardrenderer-muuri-powered-drag-and-drop-dashboard-layout.md) — DashboardRenderer renders `intent='dashboard'` specs as an interactive grid using Muuri for drag-and-drop, dynamic resizing, and auto-packing. It bridges the declarative `DashboardSpec` widget list with Muuri's DOM-level layout engine, handling widget normalization, multi-format data coercion, and per-widget controls.
- [Dialog Close — Type-Safe Close Trigger with Default Button Semantics](./dialog-close-type-safe-close-trigger-with-default-button-semantics.md) — DialogClose is a minimal wrapper around bits-ui's `Dialog.Close` primitive. Its sole substantive addition is defaulting `type` to `"button"` — a defensive fix that prevents form submission side effects when the close trigger is rendered inside a `<form>` element.
- [Dialog Content — Animated Modal Panel with Portal and Optional Close Button](./dialog-content-animated-modal-panel-with-portal-and-optional-close-button.md) — DialogContent assembles the visible dialog panel. It wraps children in a portal (for z-index and stacking context isolation), renders the background overlay, and optionally includes an `X` close button in the top-right corner. Enter/exit animations are driven by bits-ui's `data-open`/`data-closed` attributes.
- [Dialog Description — Accessible Subtitle with Auto-Styled Link Formatting](./dialog-description-accessible-subtitle-with-auto-styled-link-formatting.md) — DialogDescription wraps bits-ui's `Dialog.Description` primitive to render the dialog's accessible subtitle text. It automatically styles inline anchor links with underlines and hover effects, and exposes its content to ARIA via the primitive's `aria-describedby` wiring.
- [DiscoverCard: Media Card for Discoverable Content Links](./discovercard-media-card-for-discoverable-content-links.md) — DiscoverCard renders a compact card with a 16:10 thumbnail image, title, description, and source label — designed to be placed in horizontal scroll strips to surface recommended articles, topics, or related content. It activates as a button when a URL or click handler is provided.
- [Each — Declarative List Iteration Control Widget](./each-declarative-list-iteration-control-widget.md) — A minimal control-flow wrapper that exposes Svelte's native `{#each}` iteration pattern as a named widget usable in Ripple's schema-driven UI. It accepts an `items` prop (used by the schema layer) and a `children` snippet that it unconditionally renders, delegating actual iteration to the caller.
- [Event Handler Schema — Widget Interaction Action Types](./event-handler-schema-widget-interaction-action-types.md) — This module defines the complete Zod schema for every action a widget can fire in response to user interaction. It covers simple side-effect actions (set state, navigate, toast) through to async flow control (sequential flows, conditional branches, confirmation dialogs, and API calls with on_success/on_error chaining).
- [Feed Widget — Timestamped Activity List with Type Indicators](./feed-widget-timestamped-activity-list-with-type-indicators.md) — A compact vertical feed component that renders a list of activity items — each with a colored dot, text, and optional timestamp — suitable for audit logs, notification streams, event histories, and agent activity panels. Dot colors are driven by semantic type values or custom color overrides.
- [Flex Layout Widget](./flex-layout-widget.md) — A flexbox layout container widget that maps semantic prop names (`justify`, `align`, `direction`, `gap`, `wrap`) to CSS flexbox values via lookup tables, eliminating the need for callers to know CSS property names. It also supports `divided` and `compact` layout variants that apply separator borders or tight padding to direct children.
- [FollowUp: Inline Question Input for Conversational Research](./followup-inline-question-input-for-conversational-research.md) — FollowUp renders a pill-shaped text input with a send button that emits follow-up queries back into the Ripple event system — bridging widget-level user input with the runtime's event dispatcher. It implements empty-string guarding, Enter-key submission, and graceful degradation when the event system context is absent.
- [GlassCard Glassmorphism Container Widget](./glasscard-glassmorphism-container-widget.md) — A specialized card widget that renders a frosted-glass container using backdrop-filter, color-mix, and multi-layer box-shadow. Extracted from the paw-os-ui enterprise layout system, it exposes fine-grained glass effect controls (`opacity`, `blur`, `tint`, `borderGlow`) as props.
- [Grid Layout Widget](./grid-layout-widget.md) — A CSS grid layout widget that maps numeric column/row counts or raw CSS template strings to `grid-template-columns` and `grid-template-rows`, with optional gap normalization using the 4px grid system. It is the explicit-structure counterpart to `Dashboard`'s auto-fill approach.
- [Heading Widget — Semantic H1–H6 with Design System Typography](./heading-widget-semantic-h1h6-with-design-system-typography.md) — A typography widget that renders the correct semantic heading element (h1–h6) based on a `level` prop, with each level pre-mapped to a consistent Tailwind typography scale. Solves the problem of AI-generated specs needing to express hierarchy without embedding raw HTML tags.
- [If — Declarative Conditional Rendering Control Widget](./if-declarative-conditional-rendering-control-widget.md) — A minimal control-flow widget that wraps Svelte's `{#if}` block as a named, schema-addressable component, enabling conditional rendering in Ripple's JSON spec format. When `condition` is false (or a spec expression resolves falsy), the children snippet is suppressed entirely.
- [Image Widget — Schema-Friendly img Element with Fit and Radius Controls](./image-widget-schema-friendly-img-element-with-fit-and-radius-controls.md) — A display widget that renders a native `<img>` element with declarative control over object-fit behavior, border radius, and dimensions via a JSON-serializable prop interface. Converts numeric dimensions to pixel strings and merges inline style objects from spec definitions.
- [Input Widgets Barrel Export](./input-widgets-barrel-export.md) — The barrel export for Ripple's input widget family, re-exporting all six interactive input primitives from a single entry point. Consumers import from this index instead of individual file paths, keeping import sites stable as the internal file structure evolves.
- [KvTable: Key-Value Data Grid with Single or Two-Column Layout](./kvtable-key-value-data-grid-with-single-or-two-column-layout.md) — KvTable renders an array of key-value pairs in a bordered, optionally-striped grid that can be displayed in one or two columns — with automatic midpoint splitting for the two-column case. It exists to present dense structured data (financial metrics, specs, metadata) in a compact tabular layout without requiring a full HTML table.
- [Layout Widgets Barrel Export](./layout-widgets-barrel-export.md) — The barrel export for Ripple's layout widget family, re-exporting nine layout primitives from a single entry point. Phase 4 additions (`GlassCard`, `Modal`) are noted in the file comment, establishing a versioned changelog for the layout surface.
- [Metric Widget — KPI Display with Trend Badge and Layout Variants](./metric-widget-kpi-display-with-trend-badge-and-layout-variants.md) — A focused display widget for key performance indicators, rendering a labeled numeric value alongside an optional trend badge that automatically applies semantic color (positive green, negative red, neutral gray) based on the trend string's leading sign character. Supports three layout variants for different dashboard density requirements.
- [Modal Dialog Widget](./modal-dialog-widget.md) — A controlled modal dialog widget wrapping the shadcn Dialog primitive, with support for both prop-bound state and flow `invoke` actions via the `WidgetRegistry`. It synchronizes open state bidirectionally — accepting `value` from the outside and emitting `onchange(false)` when dismissed — so the UI state manager stays consistent regardless of how the modal is closed.
- [NewsCard: Scannable News Item Row with Sentiment Badge](./newscard-scannable-news-item-row-with-sentiment-badge.md) — NewsCard renders a news article as a list row with source favicon, publisher name, timestamp, optional sentiment badge (bullish/bearish/neutral), truncated headline, and thumbnail image. It is designed for dense news feed layouts where multiple items stack vertically, with separators between items and the last item having no bottom border.
- [NodeRenderer — Recursive UI Tree Renderer](./noderenderer-recursive-ui-tree-renderer.md) — NodeRenderer.svelte is the recursive engine at the heart of Ripple's render pipeline, responsible for taking a single `UINode` and producing the correct widget component with resolved props, live bindings, event handlers, loop context, and named-slot children. It handles all control-flow node types (`if`, `each`) and falls back to a visible error for unknown widget types.
- [Overlay Widgets Barrel Export](./overlay-widgets-barrel-export.md) — The overlay barrel module centralizes all overlay-layer widgets — currently ConfirmDialog — behind a single import path. This pattern keeps widget consumers decoupled from internal file structure and makes tree-shaking predictable.
- [Pattern Detector — Quiz, Results, and Chart Data Transforms](./pattern-detector-quiz-results-and-chart-data-transforms.md) — The pattern detector identifies specialized data shapes — quizzes, results summaries, and chart data — from generic intent specs and normalizes them into typed structures that their respective renderer components expect. It was extracted from IntentRenderer to keep detection logic modular and independently testable.
- [Popover Portal — DOM Escape Hatch for Overlay Rendering](./popover-portal-dom-escape-hatch-for-overlay-rendering.md) — A thin wrapper around the bits-ui PopoverPrimitive.Portal that teleports popover content outside its natural DOM position, preventing stacking context and overflow clipping issues. It passes all props through verbatim, keeping the surface area minimal while delegating all portal logic to the headless primitive.
- [Popover Root — State Container and Context Provider for Popovers](./popover-root-state-container-and-context-provider-for-popovers.md) — The root component of the popover composition, wrapping bits-ui's `Popover.Root` to expose a bindable `open` state that defaults to `false`. It acts as the invisible state container and context provider that coordinates all child components — trigger, content, portal — without rendering any DOM of its own.
- [Popover Title — Semantic Heading Slot for Popover Panels](./popover-title-semantic-heading-slot-for-popover-panels.md) — A styled `div` that renders as the title section inside a popover panel, applying medium font weight and exposing a bindable element reference for programmatic access. It uses the standard `cn` utility for class merging and the `WithElementRef` pattern to support imperative DOM operations.
- [Popover Trigger — Accessible Activation Element for Popovers](./popover-trigger-accessible-activation-element-for-popovers.md) — Wraps the bits-ui `Popover.Trigger` primitive to give it a `data-slot` identifier and class-merge support, while binding the DOM reference back to the caller. It is the interactive element users click or activate to open a popover, with all accessibility behavior — ARIA attributes, keyboard handling, focus management — delegated to the headless primitive.
- [Progress Bar — Animated Linear Progress Indicator](./progress-bar-animated-linear-progress-indicator.md) — A styled wrapper around bits-ui's `Progress.Root` that renders a horizontal progress bar with a CSS transform-based fill animation. It accepts `value` (current progress) and `max` (ceiling, default 100) props, renders an inner indicator div styled as the primary color, and uses `translateX` rather than `width` changes to animate smoothly.
- [Progress Bar Widget](./progress-bar-widget.md) — A thin wrapper around the shadcn/ui Progress component that adds height variants and runtime color overrides via CSS custom properties. Used by Ripple's generative UI runtime to render determinate progress indicators from a JSON spec.
- [Progress Component Barrel — Public API Entry Point](./progress-component-barrel-public-api-entry-point.md) — The index module for the progress component exports the `Root` component under two names — `Root` and `Progress` — giving consumers the flexibility to use either a structural import style or a semantic one. It is the single entry point that downstream code should import from rather than reaching into the component directory directly.
- [Radio Group Barrel — Dual-Export Entry Point for Root and Item](./radio-group-barrel-dual-export-entry-point-for-root-and-item.md) — The index module for the radio group component exports both the `Root` container and `Item` option under structural and semantic names, enabling consumers to import as `{ RadioGroup, RadioGroupItem }` or `{ Root, Item }`. It establishes the two-component composition model that every radio group must follow.
- [Radio Group Item — Accessible Option with Visual Check Indicator](./radio-group-item-accessible-option-with-visual-check-indicator.md) — A styled radio button option that wraps bits-ui's `RadioGroup.Item` primitive, rendering a circular border that fills with the primary color when selected, with a `CircleIcon` indicator that appears conditionally via Svelte 5 snippets. It handles checked, focused, disabled, and invalid states through data attributes and ARIA-driven Tailwind variants.
- [Radio Group Root — Selection Container with Bindable Value](./radio-group-root-selection-container-with-bindable-value.md) — The root container for a radio group, wrapping bits-ui's `RadioGroup.Root` to expose a two-way bindable `value` prop defaulting to an empty string. It renders as a full-width CSS grid with 8px gaps and provides the selection context that child `RadioGroupItem` components read from.
- [RangeBar: Position Marker on a Min-Max Value Track](./rangebar-position-marker-on-a-min-max-value-track.md) — RangeBar renders a labeled track with a colored fill and a circular position marker showing where a current value falls within a min-max range — commonly used for 52-week price ranges, analyst target ranges, or any metric with natural bounds. It clamps the marker position defensively and falls back to the midpoint when min equals max.
- [Research Widget Barrel Export](./research-widget-barrel-export.md) — Central re-export index for all research-domain UI widgets in the Ripple library. Groups thirteen Svelte components under a single import path so consumers never depend on internal file layout.
- [Ripple — Main Entry Point and Rendering Orchestrator](./ripple-main-entry-point-and-rendering-orchestrator.md) — Ripple.svelte is the top-level component that accepts a UI spec or a streaming spec store, bootstraps all core services (state manager, event dispatcher, widget registry), and routes rendering to the correct sub-renderer based on spec intent and stream state.
- [Select Component Barrel — Full Composition API with 11 Sub-Components](./select-component-barrel-full-composition-api-with-11-sub-components.md) — The index module for the select component family exports all 11 sub-components under both structural (`Root`, `Item`, etc.) and semantic (`Select`, `SelectItem`, etc.) names. It defines the complete public API for the most compositionally complex form control in the Ripple library.
- [Select Content — Animated Floating Panel with Scroll Controls](./select-content-animated-floating-panel-with-scroll-controls.md) — The floating content panel for the select dropdown, composing a portal, scroll buttons, a viewport, and animated entry/exit transitions. It defaults to preventing page scroll when open (`preventScroll = true`) and uses a 4px side offset from the trigger, with directional slide animations driven by `data-[side]` attributes.
- [Select Dropdown Widget](./select-dropdown-widget.md) — A single-select dropdown widget that wraps shadcn/ui's Select primitive and normalizes a flexible options format — accepting either plain strings or `{value, label}` objects — into a consistent internal structure. Renders an optional label and surfaces the selected label text in the trigger button.
- [Select Group Heading — Accessible Label for Option Groups](./select-group-heading-accessible-label-for-option-groups.md) — A styled wrapper around bits-ui's `SelectPrimitive.GroupHeading` that renders small, muted text as a non-interactive section header within grouped select options. It links to its parent `SelectGroup` via ARIA and is excluded from the keyboard navigation sequence.
- [Select Group — Semantic Container for Grouped Options](./select-group-semantic-container-for-grouped-options.md) — A minimal wrapper around bits-ui's `SelectPrimitive.Group` that applies consistent internal padding and scroll margins, acting as the ARIA grouping container that associates a heading label with its child options. It renders no visual chrome beyond spacing.
- [Select Item — Selectable Option with Checkmark Indicator](./select-item-selectable-option-with-checkmark-indicator.md) — A styled selectable option for the select dropdown that renders a checkmark when selected and accepts either a `label` string, a `value` fallback, or a custom children snippet with access to `selected` and `highlighted` state. It handles all interactive states — hover, keyboard highlight, selected, disabled — through Tailwind data-attribute variants.
- [Select Label — Standalone Descriptive Label for Select Content Panels](./select-label-standalone-descriptive-label-for-select-content-panels.md) — A plain `div`-based label component for use within select content panels, rendering small muted text without any bits-ui backing. It differs from `SelectGroupHeading` in that it is not associated with a group via ARIA and serves as a freestanding visual annotation rather than a semantic section header.
- [Select Portal — DOM Escape Hatch for Dropdown Menus](./select-portal-dom-escape-hatch-for-dropdown-menus.md) — A thin wrapper around bits-ui's `Select.Portal` that teleports the select dropdown out of its DOM parent and into the document body. This prevents clipping caused by `overflow: hidden` or `z-index` stacking contexts on ancestor elements.
- [Select Root — Bindable State Entry Point for Dropdowns](./select-root-bindable-state-entry-point-for-dropdowns.md) — The top-level select component that owns `open` and `value` state as bindable props, enabling two-way data binding with parent components. It delegates all behavior to bits-ui's `Select.Root` while providing Svelte 5-idiomatic state management.
- [Select Scroll Down Button — Overflow Navigation Indicator](./select-scroll-down-button-overflow-navigation-indicator.md) — Renders a chevron-down affordance anchored to the bottom of a select listbox when the list overflows its container. It signals to the user that more options exist below the visible area and provides a scroll target managed by bits-ui.
- [Select Scroll Up Button — Top Overflow Navigation Indicator](./select-scroll-up-button-top-overflow-navigation-indicator.md) — Renders a chevron-up affordance pinned to the top of a select listbox when hidden options exist above the visible scroll area. It is the symmetric counterpart to `SelectScrollDownButton`, completing the bidirectional scroll navigation pattern.
- [Select Separator — Visual Divider Between Option Groups](./select-separator-visual-divider-between-option-groups.md) — A horizontal rule that visually segments groups of options inside a select dropdown. It reuses the shared Separator primitive with select-specific margins so dividers align flush with the popover edges.
- [Select Trigger — The Dropdown Open/Close Button](./select-trigger-the-dropdown-openclose-button.md) — The visible button element users click to open or close a select dropdown. It combines value display, size variants, validation states, and a chevron icon into a single polished interactive control that integrates with bits-ui's accessibility layer.
- [Separator Component — Accessible Horizontal and Vertical Divider](./separator-component-accessible-horizontal-and-vertical-divider.md) — A styled wrapper around bits-ui's `Separator.Root` that renders a 1px divider line in either horizontal or vertical orientation. It applies design-token colors and includes a deliberate deviation from the upstream shadcn/ui pattern to fix a self-stretch layout bug.
- [Sheet Close — Accessible Panel Dismiss Button](./sheet-close-accessible-panel-dismiss-button.md) — A minimal passthrough to bits-ui's `Dialog.Close` primitive that renders a dismiss button inside a sheet panel. It wires up the accessibility contract for closing the sheet via keyboard or click without requiring application-level event handling.
- [Sheet Component Index — Barrel Export for Slide-Out Panel System](./sheet-component-index-barrel-export-for-slide-out-panel-system.md) — The barrel export for Ripple's Sheet (slide-out panel) component family. It exports all ten sub-components under both short primitive names and longer semantic `Sheet*` names, giving consumers the flexibility to import either style while maintaining a single source of truth.
- [Sheet Content — Animated Side Panel with Side-Aware Layout](./sheet-content-animated-side-panel-with-side-aware-layout.md) — The core panel component of the Sheet system that renders a fixed-position drawer sliding in from any of the four viewport edges. It composes the portal, overlay, and close button into a single cohesive unit and applies side-aware entrance/exit animations via data attributes.
- [Sheet Description — Accessible Panel Summary Text](./sheet-description-accessible-panel-summary-text.md) — Renders the descriptive subtitle text inside a sheet panel with muted styling. It maps to bits-ui's `Dialog.Description`, which wires `aria-describedby` between this element and the sheet content so screen readers can announce the panel's purpose.
- [Sheet Footer — Action Button Region at Panel Bottom](./sheet-footer-action-button-region-at-panel-bottom.md) — A plain div-based layout region that anchors to the bottom of the sheet panel with `mt-auto`, providing a consistent placement for action buttons like Submit, Cancel, or Save. It is a pure layout primitive with no bits-ui dependency.
- [Sheet Header — Title and Description Container Region](./sheet-header-title-and-description-container-region.md) — A simple `<div>` layout region that groups the sheet's title and description into a consistently padded top section. Like `SheetFooter`, it is a pure layout primitive with no bits-ui dependency, relying on semantic composition rather than ARIA wiring.
- [Sheet Overlay — Backdrop Scrim with Blur Effect](./sheet-overlay-backdrop-scrim-with-blur-effect.md) — Renders a semi-transparent backdrop behind an open sheet panel that blocks interaction with underlying page content and provides a subtle blur effect when the browser supports it. It is always rendered inside `SheetContent` and delegates close-on-click behavior to bits-ui's `Dialog.Overlay`.
- [Showcase Index — Full Widget Catalog](./showcase-index-full-widget-catalog.md) — The primary showcase page for the Ripple widget library, rendering every core widget type through inline pocket specs that are fed to the Ripple component, serving as both a visual regression reference and a live usage guide.
- [Skeleton Loading Placeholder Widget](./skeleton-loading-placeholder-widget.md) — A lightweight shimmer placeholder shown by Ripple while a streaming spec has not yet produced a valid first parse. Provides four layout variants — card, dashboard, text, and none — so the UI can match the shape of the content it is waiting for.
- [Soul Status Display Widget](./soul-status-display-widget.md) — A compact or expanded display widget that surfaces an AI agent's soul state — including name, role, mood, energy level, memory count, and last action. Designed for dashboards that need to show multiple agents at a glance, with an inline energy bar and status dot in compact mode and a full detail panel in expanded mode.
- [SourceCard: Compact Source Attribution Card with Headline](./sourcecard-compact-source-attribution-card-with-headline.md) — SourceCard is a small card that pairs a favicon (with dot fallback) and source name with a headline — designed for horizontal citation strips where sources are surfaced as individual tappable cards. It follows the same favicon-error-fallback and conditional-interactivity patterns as Citation and NewsCard.
- [SourcesBar: Aggregated Sources Summary Bar with Copy and Share](./sourcesbar-aggregated-sources-summary-bar-with-copy-and-share.md) — SourcesBar renders a compact row showing stacked source favicons and a count label alongside copy and share action buttons — serving as a persistent summary of all sources cited in a research response. It integrates with the Ripple event system to dispatch toast notifications and supports the Web Share API with clipboard fallback.
- [Spec Showcase — Card + Chart + Stat Composition Demo](./spec-showcase-card-chart-stat-composition-demo.md) — A showcase page that renders four analytics dashboard cards (revenue, latency, signups, error share) using the Ripple spec format, demonstrating how Chart, Stat, and Card compose together with slot injection to build production-style metric panels.
- [Stat Metric Widget with Delta and Direction](./stat-metric-widget-with-delta-and-direction.md) — A data metric widget for dashboards that renders a labeled numeric value with optional change indicator (delta chip). Handles locale-aware number formatting across four modes — number, currency, percent, and compact — and supports a sophisticated direction/sentiment system that decouples visual arrow direction from color meaning.
- [Stat Showcase — Visual QA for Metric Display Widget](./stat-showcase-visual-qa-for-metric-display-widget.md) — A comprehensive visual QA page for the Ripple Stat widget, covering all three sizes, five number formats, the full direction-semantics matrix, label/alignment options, and Card composition.
- [Table Root Component](./table-root-component.md) — The root container for data tables, wrapping a native `<table>` in an overflow-safe scroll container. Applies consistent sizing and caption positioning while exposing a bindable DOM reference.
- [Table Row Component](./table-row-component.md) — A styled `<tr>` wrapper that provides hover, selection, and transition behaviors for table rows. Exposes a bindable ref and forwards all native HTML table row attributes.
- [Table Widget — Schema-Driven Data Grid with Row Interaction](./table-widget-schema-driven-data-grid-with-row-interaction.md) — A flexible data table widget that renders structured row/column data with automatic column detection, multiple visual variants, an optional status indicator column, and full Ripple event system integration for row-click actions. It normalizes several column definition formats to accommodate the range of shapes that AI-generated specs may produce.
- [Tabs Layout Widget](./tabs-layout-widget.md) — A tabbed panel widget wrapping shadcn Tabs that accepts tabs as either plain strings or `{value, label}` objects, normalizes them into a consistent shape, and manages active tab state with support for both internal control and external value binding. Each tab panel only renders its content when active, preventing hidden panels from running their child effects.
- [Terminal Widget — Read-Only and Interactive Log Display](./terminal-widget-read-only-and-interactive-log-display.md) — A styled terminal emulator widget for Ripple that renders sequential log lines with per-type color coding (stdout, stderr, info, command) and optionally exposes a command input field. Auto-scrolls to the bottom when new lines arrive, making it suitable for streaming agent output, build logs, or shell session playback.
- [Test Suite for Button Widget](./test-suite-for-button-widget.md) — Vitest + Testing Library test suite for the Button input widget covering rendering, variant and size data attributes, click event firing, disabled and loading state enforcement, spinner slot rendering, type attribute forwarding, and the data-state machine. Uses userEvent for realistic interaction simulation.
- [Test Suite for Input Widget](./test-suite-for-input-widget.md) — Comprehensive Vitest + Testing Library test suite for the Input text widget, covering rendering, label/id association, size and state data attributes, type forwarding, oninput/onchange event firing, error and helper message rendering, accessibility attributes (aria-invalid, aria-describedby), and disabled/required/readOnly attribute forwarding.
- [Test Suite for NodeRenderer Named Slot Routing](./test-suite-for-noderenderer-named-slot-routing.md) — This test suite verifies that `NodeRenderer` correctly routes child nodes to named slots (`header`, `footer`) or the default body slot based on each child's optional `slot` field. It covers default routing, named-slot placement, multi-child same-slot stacking, unknown slot silencing, and regression-guards for specs written before slots were introduced.
- [Test Suite for Stat Widget](./test-suite-for-stat-widget.md) — Comprehensive Vitest + Testing Library test suite for the Stat display widget covering number formatting, all direction/sentiment combinations, delta chip rendering, size variants, and Lucide icon selection. Uses data attributes as stable test hooks rather than class names.
- [Text Display Widget](./text-display-widget.md) — A foundational text rendering primitive that maps a flat set of typography props — size, weight, color, and inline mode — to semantic HTML elements. Renders as a `<p>` block element by default or an inline `<span>` when the `inline` prop is set.
- [Text Input Widget with Widget Registry Integration](./text-input-widget-with-widget-registry-integration.md) — A fully-featured text input widget that supports labels, error/helper messages, prefix/suffix icon slots, and size variants. Uniquely, it opts into Ripple's WidgetRegistry when given an `id`, enabling the `invoke` flow action to call `focus()` on the input remotely from a spec action handler.
- [Textarea Component Public API Index](./textarea-component-public-api-index.md) — The barrel file for the Textarea component, re-exporting the single `textarea.svelte` implementation under both `Root` and `Textarea` names. Provides the canonical import point for the component.
- [Textarea Input Widget](./textarea-input-widget.md) — A controlled multi-line text input widget that wraps the shadcn Textarea primitive with Ripple's prop and event conventions. It maintains local state synchronized with an incoming `value` prop and surfaces `onchange`, `onfocus`, and `onblur` callbacks for flow integration.
- [Ticker: Stock Ticker Strip with Symbol, Price, and Change](./ticker-stock-ticker-strip-with-symbol-price-and-change.md) — Ticker renders one or more stock quotes in a horizontally-scrollable pill strip — each showing symbol, price, and color-coded change value with optional percentage. It uses string prefix detection to determine bullish/bearish coloring, matching how financial data APIs deliver pre-formatted change strings.
- [Timeline: Chronological Event List with Color-Coded Dots](./timeline-chronological-event-list-with-color-coded-dots.md) — Timeline renders a vertical list of dated events connected by a continuous rail line — each event has a colored dot, date label, title, and optional detail text. It supports five semantic dot types plus custom color overrides, truncation via maxItems, and a trailing overflow indicator.
- [Toggle Switch Input Widget](./toggle-switch-input-widget.md) — A boolean toggle switch widget that wraps shadcn/ui's Switch primitive with an optional label layout and the same local state sync pattern used by the Checkbox widget. Provides a more visually distinctive alternative to Checkbox for on/off settings.
- [UISpec Schema — Gen 1 JSON UI Specification](./uispec-schema-gen-1-json-ui-specification.md) — UISpec is Ripple's original (Gen 1) schema for a complete JSON-described UI. It defines the recursive UINode tree, optional data fetchers and state, theme overrides, and the parse/safeParse entry points that validate LLM-generated output before it reaches the renderer.
- [Widget Registry — Central Component Map and Registration API](./widget-registry-central-component-map-and-registration-api.md) — The top-level widget registry for Ripple's generative UI runtime. It maintains a mutable map of string type names to Svelte components, provides a CRUD API for runtime registration, and serves as the single import point for all 40+ built-in widgets across layout, display, input, data, control, composite, overlay, research, workflow, and C4 categories.
- [Widget Type Registry — Enum and Category Map](./widget-type-registry-enum-and-category-map.md) — This module defines the `WidgetType` Zod enum (the complete set of string values the `type` field of a UINode may hold) and the `WIDGET_CATEGORIES` constant that organizes those types into named groups used by documentation, tooling, and the layout engine.
- [Workflow Widget Barrel Export](./workflow-widget-barrel-export.md) — The public API index for the workflow widget submodule, re-exporting the custom SvelteFlow node component, the auto-layout function, and the TypeScript types that consumers need to construct workflow specs.
- [Workflow Widget — Interactive Node-Based Diagram](./workflow-widget-interactive-node-based-diagram.md) — A Svelte 5 widget that renders interactive workflow diagrams using SvelteFlow (`@xyflow/svelte`). It accepts a simplified `WorkflowNodeData[]` and `WorkflowEdgeData[]` spec and handles auto-layout, type mapping, and interactivity controls transparently.
- [WorkflowNode — Custom SvelteFlow Node Component](./workflownode-custom-svelteflow-node-component.md) — A Svelte 5 component that renders a single node inside a SvelteFlow canvas, supporting six semantic node types with distinct colors and icons, five execution status states, and special two-handle branching for condition nodes.

### `widget-registry` — 1 article(s)

- [Text Input Widget with Widget Registry Integration](./text-input-widget-with-widget-registry-integration.md) — A fully-featured text input widget that supports labels, error/helper messages, prefix/suffix icon slots, and size variants. Uniquely, it opts into Ripple's WidgetRegistry when given an `id`, enabling the `invoke` flow action to call `focus()` on the input remotely from a spec action handler.

### `widget-system` — 3 article(s)

- [DashboardManager — Reactive Widget List State for Persistent Dashboards](./dashboardmanager-reactive-widget-list-state-for-persistent-dashboards.md) — DashboardManager is the reactive state container for a mutable dashboard widget list. It exposes a CRUD API (add, remove, update, move, reorder) plus an `onChange` subscription for notifying parent components when the spec changes, supporting persistence in platforms that save dashboard layouts across sessions.
- [Test Suite: WidgetRegistry — Register, Invoke, and Unregister Round Trips](./test-suite-widgetregistry-register-invoke-and-unregister-round-trips.md) — Unit tests for the WidgetRegistry class, verifying method registration, argument forwarding, idempotent unregistration, replacement safety, and edge case handling. These tests were written alongside Phase B flow actions to ensure the `invoke` action has a well-behaved backing registry.
- [WidgetRegistry — Runtime Widget Method Registry for Invoke Actions](./widgetregistry-runtime-widget-method-registry-for-invoke-actions.md) — WidgetRegistry is a per-Ripple-instance registry that allows widget components to expose named methods so declarative `invoke` flow actions can call them programmatically. It uses a nested `Map<string, Map<string, WidgetMethod>>` structure keyed by widget ID and method name, with safe unregister semantics designed for Svelte 5 `$effect` cleanup.

### `workflow` — 4 article(s)

- [autoLayout — BFS Workflow Node Positioning](./autolayout-bfs-workflow-node-positioning.md) — A pure TypeScript function that computes x/y coordinates for workflow nodes using breadth-first search from root nodes, assigning depth-based horizontal positions and vertically centering each depth column. Includes cycle detection to prevent infinite BFS loops in graphs with back-edges.
- [Workflow Widget Barrel Export](./workflow-widget-barrel-export.md) — The public API index for the workflow widget submodule, re-exporting the custom SvelteFlow node component, the auto-layout function, and the TypeScript types that consumers need to construct workflow specs.
- [Workflow Widget Type Definitions](./workflow-widget-type-definitions.md) — TypeScript interfaces and union types that define the data contract for the workflow widget's nodes and edges, enabling type-safe pocket spec authoring and enforcing the six valid node types and five execution status states.
- [WorkflowNode — Custom SvelteFlow Node Component](./workflownode-custom-svelteflow-node-component.md) — A Svelte 5 component that renders a single node inside a SvelteFlow canvas, supporting six semantic node types with distinct colors and icons, five execution status states, and special two-handle branching for condition nodes.

## Top Concepts (top 50 of 1630)

| Concept | Referenced by |
|---|---:|
| data-slot | 47 |
| bits-ui | 34 |
| Svelte 5 runes | 33 |
| WithElementRef | 25 |
| barrel export | 24 |
| cn utility | 24 |
| bindable ref | 14 |
| barrel file | 13 |
| Tailwind CSS | 12 |
| namespace import | 12 |
| Svelte 5 | 11 |
| tailwind-variants | 11 |
| EventDispatcher | 10 |
| StateManager | 10 |
| widget registry | 10 |
| C4NodeData | 9 |
| WidgetRegistry | 9 |
| re-export | 9 |
| Svelte context | 8 |
| accessibility | 8 |
| aria-invalid | 8 |
| public API | 8 |
| semantic HTML | 8 |
| table | 8 |
| tree-shaking | 8 |
| SvelteFlow | 7 |
| disabled state | 7 |
| testing-library | 7 |
| vitest | 7 |
| Handle | 6 |
| SvelteFlow custom node | 6 |
| accordion | 6 |
| aria-describedby | 6 |
| controlled component | 6 |
| dark mode | 6 |
| data attributes | 6 |
| index module | 6 |
| portal | 6 |
| $effect | 5 |
| C4Diagram | 5 |
| DashboardRenderer | 5 |
| RippleEvent | 5 |
| Root alias | 5 |
| StreamSpecStore | 5 |
| UniversalSpec | 5 |
| XSS prevention | 5 |
| compound component | 5 |
| drill-down | 5 |
| flow actions | 5 |
| sheet | 5 |

